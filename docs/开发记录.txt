# 开发记录
## [2026-05-23] 排班序号显示与"退出编辑"按钮
- `SchedulePage.xaml.cs` 在员工行（首行 = 表格第 5 行）的姓名单元格左侧加 18px 的浅灰序号 TextBlock（`IsHitTestVisible=false`，不抢编辑焦点），1 起算，方便清点人数。
- `ScheduleViewModel.EditCommand` 改为切换式 `ToggleEditMode`：未编辑显示「编辑」，编辑中显示「退出编辑」；新增 `ExitEditMode` 仅置 `IsEditing = false`，不回滚内存中的未保存修改，状态栏提示"重新加载版本会丢失"。
- 重新打包安装程序：`artifacts\installer\MyToolsSetup.exe` 41.15 MB，SHA256 = `636824F29E2A9B94502E54C5FE6676E440E2E2B5DF5AB5CD5A95C1DD7AD2D29C`。

## [2026-05-23] 安装行为简化、桌面快捷方式、排班清空与无条件保存
- `MyTools.Installer\Program.cs` 将原先的 `RunPreviousUninstaller`（注册表扫描+调用旧卸载器）替换为 `PrepareInstallDirectory`：仅停止已安装目录内运行中的 `MyTools.exe`，删除已知可执行文件后等待目录解锁，由 payload 直接覆盖；不再依赖旧版本卸载流程，覆盖安装更稳健。
- 安装器 `CreateShortcuts` 在创建开始菜单快捷方式的同时，向 `Environment.SpecialFolder.CommonDesktopDirectory` 写入桌面快捷方式 `阿君的工具.lnk`；卸载器已有的 `RemoveShortcuts` 已经覆盖该桌面路径，无需调整。
- `ScheduleViewModel` 新增 `ClearAllCellsCommand`：编辑模式下点击"清空"按钮，确认后将所有员工所有日的 `Code` 置空、`IsManual=false`，并清除 `GeneratedAt` 让 Excel 导出按规则被阻拦。
- `ScheduleViewModel.SaveAsync` 删除 `BuildHardConstraintIssues` 阻塞分支：编辑状态下点保存直接写入，不再校验硬约束；基础结构性校验（年月、每日总休数组长度等）继续保留，防止数据损坏。
- `SchedulePage.xaml` 工具栏在"设置休息日"后插入"清空"按钮，绑定到 `ClearAllCellsCommand`。
- 重新打包安装程序：`artifacts\installer\MyToolsSetup.exe` 41.15 MB，SHA256 = `802523230A970DF696B7AE5A14BFFFB2DDA8859A830452DD06FE1C38017FF9BF`。

## [2026-05-23] frp 模块上线后重新打包安装程序
- 补回缺失的 `src\MyTools\NativeBinaries\README.txt`，恢复 `Build-Installer.ps1` 的 payload 白名单要求。
- 通过 `scripts\Build-Installer.ps1` 重新构建主程序、卸载器和安装器，包含 frp 隧道穿透模块、排班弹窗按钮宽度修正、`VpnLock`→`VpnKey` 图标修正等 5/22 之后的所有改动。
- 验证：`artifacts\installer\MyToolsSetup.exe` 41.15 MB，SHA256 = `B956A1F6872A34682D54BE7474C31A954B49F9236CC5E3CA15952BEC975022CD`。
## [2026-04-17] 项目初始化
- 建立 MyTools 项目目录结构与基础约束。
- 确认技术栈为 .NET Framework 4.8 + WPF。
## [2026-04-17] 单文件发布配置
- 引入 `Costura.Fody`，用于 Release 产物单文件打包。
- 在项目中加入 SQLite 原生库嵌入配置。
## [2026-04-17] WireGuard 功能接入
- 新增 `WireGuardService`，支持连接、断开和状态检测。
- 补充主界面中的 WireGuard 配置与控制区域。
## [2026-04-20] 系统托盘与最小化到托盘
- 引入 `Hardcodet.NotifyIcon.Wpf`。
- 新增托盘图标、右键菜单和窗口恢复/退出逻辑。
- 重写主窗口关闭行为，关闭时默认隐藏到托盘。
## [2026-04-20] 启动闪退修复
- 通过 Windows 事件日志定位到启动期 `System.UriFormatException` / `XamlParseException`。
- 将托盘图标从 `DrawingImage` 改为内置 `AppIcon.png`，修复启动阶段崩溃。
- 去掉 `StartupUri`，改为在 `App.xaml.cs` 中显式创建主窗口。
- 增加 UI 线程、`AppDomain`、`Task` 级别的全局异常处理。
- 启动失败时写入 `MyTools.startup.log`，并弹窗提示日志位置。
## [2026-04-23] SQL Server 表导出 Excel
- 新增 SQL 导出模块，支持输入服务器、端口、用户名和密码测试连接。
- 连接成功后异步加载数据库列表与用户表列表，按 `schema.table` 展示。
- 新增整表导出 `.xlsx` 能力，并在导出前校验 Excel 单工作表最大行数限制。
- 新增应用日志服务，记录 SQL 导出关键步骤，同时避免记录密码和完整连接字符串。
## [2026-04-23] SQL Server 旧版本兼容修复
- 修复部分旧版 SQL Server 返回“对象名 sys.databases 无效”导致测试连接后无法加载数据库列表的问题。
- 数据库列表读取保留 `sys.databases` 优先路径，失败时自动降级到 `sp_databases`，提升 SQL Server 2000/兼容服务端可用性。
## [2026-04-23] SQL 导出交互增强
- 数据表下拉框改为可输入筛选，按 `schema.table` 文本进行本地实时过滤。
- 新增 SQL 连接历史记录，服务器地址、用户名和密码可从历史下拉选择，并在启动时回填最后一次成功连接信息。
- 历史密码使用 Windows DPAPI 按当前用户加密保存，避免明文写入本地配置文件。
## [2026-04-23] 图标与密码历史下拉修正
- 将密码历史选择从嵌入式 ComboBox 改为右侧图标按钮加遮罩菜单，修复下拉边框显示异常。
- 重新设计现代科技感应用图标，并将 `Resources\AppIcon.ico` 配置为程序图标。
## [2026-04-23] 图标配色调整
- 将应用图标调整为红蓝科技风格，加入星条旗暗纹、星形节点和红色能量线，减少大面积蓝色占比。
## [2026-05-08] 截图工具模块
- 新增 AppSettingsService：读写 MyTools.settings.json，存储截图快捷键（Modifiers/Key/DisplayText）和截图行为开关，使用 async 文件 IO。
- 新增 ScreenshotService：调用 System.Drawing.Graphics.CopyFromScreen 捕获全虚拟屏幕，返回 WPF BitmapSource，支持多显示器。
- 新增 HotkeyService：通过 RegisterHotKey/UnregisterHotKey Win32 API 注册全局热键，加 MOD_NOREPEAT 防重复触发，提供 BuildDisplayText 方法格式化显示。
- 新增 ScreenshotEditorWindow：截图编辑窗口（无边框、最大化），底层 Image + 上层透明 InkCanvas 叠加实现画笔标注；工具栏含 9 色画笔、粗细滑块、橡皮擦、撤销、清除、复制到剪贴板（Ctrl+C）、另存为 PNG/JPG/BMP（Ctrl+S）、关闭（Esc）。
- MainViewModel 新增截图设置属性（ShowEditorAfterCapture、ScreenshotHotkeyText、IsCapturingHotkey）和命令（TakeScreenshotNowCommand、StartCaptureHotkeyCommand、CancelCaptureHotkeyCommand、SaveScreenshotSettingsCommand）。
- TriggerScreenshotAsync：隐藏主窗口 → 等 150ms → 捕获 → 按设置打开编辑器或直接写剪贴板；编辑器关闭后恢复主窗口显示状态。
- MainWindow.xaml 新增"截图工具"导航项（CameraOutline 图标）和 Screenshot 设置面板（快捷键卡片 + 行为开关卡片 + 保存按钮）。
- MainWindow.xaml.cs 在 OnSourceInitialized 中初始化 HotkeyService 并挂接 HwndSource Hook；WndProc 捕获 WM_HOTKEY 触发截图；PreviewKeyDown 在 IsCapturingHotkey 模式下捕获新快捷键组合（需含 Ctrl 或 Alt）。
- 默认快捷键 Ctrl+Shift+Z，全局生效，包括主窗口隐藏到托盘期间。

## [2026-05-08] 性能修复 + 单实例 + UI 全面优化
- 性能修复：移除 MetroWindow 的 GlowBrush（该效果会创建 4 个后台透明叠层窗口持续重绘，导致系统长期卡顿），改为 GlowBrush="{x:Null}"。
- 单实例保护：App.xaml.cs 通过 Mutex 防止重复启动，已运行时通过 EventWaitHandle 信号激活已有窗口并带到前台。
- 侧边栏重设计：深色背景（#172030），自定义 NavItemStyle 含左侧高亮条指示器，DataTrigger 联动 CurrentModule 实现选中状态。
- 所有页面标题统一风格：加底部分隔线 Border，刷新按钮从 FloatingActionMiniButton 改为 IconButton，与页面视觉语言一致。
- SQL 工具面板标题更新为"SQL Server 工具 / 表导出 + SQL 查询"，反映新增的查询功能。
- TitleCharacterCasing="Normal" 防止 MahApps 强制全大写标题栏。

## [2026-05-08] Defender 与 Windows Update 控制
- 新增 WindowsSecurityService，提供 Defender 实时防护和 Windows 自动更新的状态读取与控制。
- Defender 控制：通过 Set-MpPreference 关闭/恢复实时防护、行为监控、脚本扫描等；Win11 需先在安全中心关闭篡改保护。
- 更新控制：通过注册表策略键停止/恢复自动更新，同时控制 wuauserv / UsoSvc 服务。
- 立即更新：调用 UsoClient.exe StartInteractiveScan/StartDownload/StartInstall（前台最高优先级），并自动打开 Windows Update 设置页。
- 所有操作均通过临时 .ps1 脚本 + runas UAC 提权执行，操作结束后自动删除临时文件。
- 系统优化面板新增 Defender 卡片和 Windows Update 卡片，带状态徽章（防护中/已关闭/自动更新中/已停止）。
- 新增 SystemStatusMessage 反馈条，实时显示操作进度和结果。

## [2026-05-10] 截图、SQL 与 Codex 配置修复
- 修复截图快捷键触发后截图对象跨线程访问失败的问题，截图完成后冻结 BitmapSource，再回到 UI 线程写入剪贴板或打开编辑器。
- 截图失败时恢复主窗口并弹窗提示；截图编辑器关闭时释放大图、标注集合和事件订阅，降低多次截图后的内存压力。
- 单实例激活监听从 500ms 轮询改为事件等待，主窗口关闭时释放 HwndSource Hook、全局热键和托盘图标资源。
- 修复 SQL 连接成功后刷新历史下拉导致服务器地址被清空的问题；保存历史时保留当前输入，并避免旧的表加载任务覆盖新的忙碌状态。
- 连接信息变更后自动清空旧数据库/表选择，提示重新测试连接，避免使用已失效的连接上下文。
- 新增 Codex 配置模块：拖入包含 config.toml/auth.json 的文件夹后生成记录，备注可编辑，点击“应用”覆盖当前用户 `%userprofile%\.codex` 下的同名文件，目标不存在时自动创建。
- 修复 StatusToColorConverter.ConvertBack 抛 NotImplementedException 的潜在绑定异常，改为返回 Binding.DoNothing。

## [2026-05-10] 自动优化与报告
- 新增 OptimizationReportService 与 SystemOptimizationService，支持白名单自动优化流程、报告 JSON 保存、报告列表查看和删除。
- 系统优化面板追加“自动优化与报告”卡片，保留原 Defender / 自动更新卡片不变。

## [2026-05-10] 垃圾清理
- 新增 JunkCleanupService，按白名单扫描用户临时目录、系统临时目录、回收站、浏览器缓存、缩略图缓存、Prefetch、Windows Update 与 Delivery Optimization 缓存。
- 清理前进行完整路径安全校验与二次确认，并把清理结果写入优化报告。

## [2026-05-10] 微信清理
- 新增 WeChatDataLocator 与 WeChatCleanupService，支持定位旧版 WeChat Files 与新版 xwechat_files 数据根目录。
- 支持按日期和类别扫描/清理微信图片、视频、语音、文件与缓存，明确跳过消息库、MMKV、config 和登录态相关文件。

## [2026-05-10] 微信备份
- 新增 WeChatBackupService 备份能力，按日期和类别生成单个 zip 文件。
- 备份包包含 manifest.json、相对路径、文件大小、修改时间与 SHA256 校验信息。

## [2026-05-10] 微信恢复
- 支持读取微信备份 zip 的 manifest.json 并展示摘要。
- 支持恢复到原目录或自定义目录，同名文件自动追加 restored 后缀，并防护 zip slip 与受保护路径写入。

## [2026-05-10] SQL/Codex 乱码修复
- 修复 MainViewModel 中 SQL 导出状态文案、Codex 配置状态文案、系统操作反馈、快捷键提示等乱码字符串。
- 修复 Codex 记录默认名与“内容保存状态”文案乱码，统一为可读中文。

## [2026-05-10] Codex 配置导出
- 新增 `ExportCodexProfileCommand`，支持将单条记录解密后导出为 `config.toml` 与 `auth.json`。
- 导出目录支持自动安全命名与冲突后缀递增（`_1/_2/...`），并写入应用日志。

## [2026-05-10] Codex 配置导入
- 新增 `ImportCodexProfileCommand`，支持同时选择 `config.toml` 与 `auth.json` 导入新记录。
- 新增记录名输入框（`Microsoft.VisualBasic.Interaction.InputBox`），导入内容以 DPAPI 保护后保存。

## [2026-05-10] 截图 NRE 修复
- 截图流程新增开始/失败日志，失败时回填 `SystemStatusMessage` 并保留异常弹窗。
- `Clipboard.SetImage` 强制在 UI Dispatcher 执行；截图编辑器窗口创建时仅在主窗体存在时设置 `Owner`。
- 编辑器颜色按钮激活逻辑增加空值保护，避免控件缺失导致空引用异常。

## [2026-05-10] SQL 导出多数据库支持
- 新增 `SqlExportProvider` 抽象、`SqlServerExportProvider`、`PostgreSqlExportProvider`、`MySqlExportProvider`。
- SQL 工具面板新增数据库类型选择，支持 SQL Server / PostgreSQL / MySQL 测试连接、库表加载、导出与查询。
- SQL 连接历史改为按 Provider 分组保存，并迁移旧版 SQL Server 单分组历史数据。

## [2026-05-10] 截图区域录像
- 新增 `RecordingService` 与 `RecordRegionWindow`，支持可拖拽/可调大小区域录像窗口与开始/停止按钮状态切换。
- 录像使用 ffmpeg（`NativeBinaries\\ffmpeg\\ffmpeg.exe`）调用 `gdigrab` 录制画面；当前版本的系统声音由 WASAPI Loopback 采集后合成为 `H.264 + AAC + MP4`。
- 未检测到音频设备时支持提示后降级为纯画面录制；停止时通过 `q` 优雅退出，超时后兜底 Kill。

## [2026-05-10] 独立录音
- 新增录音独立流程（`StartAudioOnlyAsync` / `StopAudioOnlyAsync`），输出 `.m4a`（AAC）。
- 录音首设独立目录，主界面新增“录音中 mm:ss”计时与按钮图标/文案切换。
- 录像与录音任务互斥，避免并发启动多个 ffmpeg 进程。
## [2026-05-11] 截图稳定性与首页本地化修复
- 修复 `ScreenshotEditorWindow` 初始化期间 `ToggleButton.Checked` 触发 `SetMode` 导致空引用的问题，避免“编辑剪贴板截图/快捷键截图”崩溃。
- 首页 Hero 与快捷入口文案改为中文，统一主界面语言。
- 首页快捷入口按钮改为 `HomeShortcutButtonStyle`，避免卡片内容被固定按钮高度裁切而显示为空白。
- 侧边栏移除 VPN 导航入口，WireGuard 面板保持隐藏，不再对外显示。
## [2026-05-11] 截图编辑器自动缩放
- 截图编辑器新增“缩放”滑块（10%~300%）和百分比显示，支持用户手动缩放。
- 打开截图或工作区尺寸变化时，自动按可视区域居中适配（等比缩放并预留边距），尽量铺满工作区且不挤占工具栏操作区。
- 缩放作用于 `CanvasZoomHost`，导出/复制仍按原始画布分辨率渲染，不受显示缩放影响。
## [2026-05-11] 截图缩放交互优化
- 截图编辑器支持鼠标滚轮缩放，滚轮向上放大、向下缩小（步进系数 1.1）。
- 手动缩放（滚轮/滑块）时锁定当前视口中心点，视觉上从屏幕中心向四周扩展，避免向右下角偏移。
## [2026-05-11] 剪贴板图片读取容错
- 修复“编辑剪贴板截图”在部分应用复制后触发 `CLIPBRD_E_BAD_DATA` 导致全局异常的问题。
- 读取剪贴板图片改为 `ContainsImage + GetImage` 的重试流程（3 次短重试），并在失败时给出友好提示，不再让异常冒泡到 UI 全局异常处理。
## [2026-05-10] ???????? / ?? / ?? / ???
- ?? `Resources/Theme.xaml`?????????????????Card ??? Badge ???
- ?? `Resources/Illustrations.xaml`??? HomeHero / NetworkEmpty / StartupEmpty / SystemEmpty / SqlEmpty / ScreenshotEmpty / CodexEmpty / WeChatEmpty 8 ? DrawingImage ???
- `App.xaml` ?? `Theme.xaml` ? `Illustrations.xaml`???????? `AppIcon.ico`?????????????????
- `MainWindow.xaml` ???????????????????? Hero + ?????????? Header ??? Badge ???
- ????????????????Release ??????????????

## [2026-05-11] 长时间运行卡顿缓解
- `AppLogService` 增加 10MB 单文件上限和按大小滚动，避免日志长时间运行后持续膨胀。
- `RecordingService` 对 ffmpeg 普通状态输出做节流记录：保留启动前 12 行，之后每 30 秒采样一次并统计被压制行数，减少录屏/录音期间的磁盘写入压力。
- 主窗口关闭时集中释放 `MainViewModel`，停止录音计时器、解除区域录制窗口事件订阅，并在退出时兜底停止正在运行的录屏/录音进程。

## [2026-05-11] 程序卸载模块
- 新增 `InstalledProgramService`，从当前用户、所有用户 64 位与 32 位卸载注册表项加载带卸载命令的程序。
- 主界面新增“程序卸载”导航页，列表展示名称、版本、发布者、安装日期、大小、来源与安装位置，并提供逐项卸载按钮。
- 卸载前增加确认提示，仅启动软件自带卸载程序；对 `msiexec /I` 命令自动转换为 `msiexec /X`。

## [2026-05-11] 品牌文案调整
- 主窗口标题、首页欢迎语、侧栏标题与托盘提示中的 `MyTools` 改为“阿君的工具”。
- 移除侧栏副标题“个人工具集”。

## [2026-05-11] 截图行为复选框
- “截图后打开编辑器”设置改为明确的复选框，勾选表示截图后打开编辑器，取消勾选表示截图后不打开编辑器。

## [2026-05-12] ffmpeg 缺失提示优化
- 录像/录音启动前支持自动识别系统 PATH 中的 `ffmpeg.exe`。
- 未找到 ffmpeg 时，提示中明确给出目标路径，并允许直接打开 `NativeBinaries\ffmpeg` 放置目录。
- 新增 `NativeBinaries\ffmpeg\README.txt`，说明 ffmpeg 放置方式。

## [2026-05-12] SQL 导出界面紧凑化
- 压缩 SQL 导出页标题、连接配置与状态区域高度，将状态信息合并到连接卡片底部。
- SQL 查询输入框高度降为 72px，查询结果表格占用剩余空间。
- 查询结果表格显式显示纵向滚动条，并保留横向滚动条以浏览多列表结果。

## [2026-05-12] 系统优化界面紧凑化
- 压缩系统优化页外边距、页头高度、卡片内边距与卡片间距，减少首屏空白。
- 统一系统优化页操作按钮高度为 32px，并缩小状态徽章、图标与说明文字间距。
- 为报告、垃圾清理、微信清理列表增加最大高度与内部滚动，避免单个列表撑开整页。

## [2026-05-12] Codex 配置界面紧凑化
- 压缩 Codex 配置页外边距、页头高度与拖拽提示区高度，减少顶部占用空间。
- 配置记录卡片改为更紧凑的列表密度，路径与内容摘要使用省略显示并保留路径提示。
- 统一导入、应用、导出与删除按钮为 32px 高度，备注输入框固定为 42px 高度。


## [2026-05-12] 全局 UI 紧凑化（侧边栏 + 各面板）
- 侧边栏宽度 220→200，导航按钮高度 56→44，图标 22→20。
- 所有面板外边距 20→16，页头 padding 20,16→12,8，页头主图标 32→24，标题 Headline5→Headline6。
- Home 视图欢迎图 260x180→180x120，快捷卡片 padding 18→14,12，整体首屏密度提升约 30%。
- WireGuard 配置文本框 200→160，高级设置弹层与按钮全部紧凑化。
- SqlExport 测试连接 / 导出按钮 34→32 并缩短文案为'导出 Excel'。
- Screenshot 工具栏 4 个动作按钮 36→32，截图设置卡片 padding 20→14,12。
- 修复 Network 面板 'IP ???'、'???' 乱码 → '当前 IP'/'网关'。
- Codex 配置面板英文标题汉化为'Codex 配置切换'，SqlExport 标题英文 'SQL Export Toolkit' → '数据库导出'。

## [2026-05-12] Codex 配置应用成功反馈
- ApplyCodexProfileAsync 在保存成功后弹出 Information 对话框，提示配置名与目标目录。
- 失败路径保留原有 Error 对话框逻辑。

## [2026-05-12] Windows 7/8 兼容性改造
- 新增 OsVersionService（基于 ntdll!RtlGetVersion），获取真实系统主版本/构建号，规避 manifest 锁定问题。
- 新增 InverseBoolToVisibilityConverter，用于反向布尔到可见性绑定。
- App.xaml.cs 启动时输出 OS 信息到 MyTools.startup.log 与 Serilog；非 Win10 系统记录 Warning。
- MainViewModel 暴露 IsWindows10OrGreater / IsWindows11OrGreater / OsDisplayName。
- 系统优化页在 Win7/8 上自动隐藏'锁定 22H2 / Defender / 自动更新'三张卡片，并显示降级提示横幅。
- AppLogService 补充 Warning 方法。
- 确认 ScreenshotService 使用 GDI CopyFromScreen，原生兼容 Win7 SP1。

## [2026-05-12] 安全与易用性增强
- 微信清理、垃圾清理删除文件改为发送到回收站（Microsoft.VisualBasic.FileIO.FileSystem.DeleteFile + RecycleOption.SendToRecycleBin），避免误删无法恢复。
- 系统托盘右键菜单新增"打开日志目录"项，一键定位 Serilog 文件日志。
- SQL 查询新增"取消"按钮与 CancellationTokenSource，支持中断长时间运行的查询。
- Codex 配置拖拽支持直接拖入单个 config.toml 或 auth.json 文件，自动映射到其所在文件夹。

## [2026-05-12] OCR 剪贴板图片文字识别
- 新增 OcrService，基于 Windows.Media.Ocr（WindowsOCR）实现剪贴板图片 OCR。
- 支持中文简体、中文繁体、英文、日文语言包自动检测与降级。
- 识别结果自动复制到剪贴板并弹窗展示。
- 仅在 Win10 1903+ 系统上显示 OCR 按钮，低版本自动隐藏。
- 引入 Microsoft.Windows.SDK.Contracts NuGet 包以支持 WinRT 互操作。

## [2026-05-12] 健壮性与 Dispose 增强
- 新增 SafeFireAndForget 辅助方法，替换所有 `_ = SomeAsync()` fire-and-forget 调用，未观测异常统一写入 Serilog 日志。
- Dispose 方法补充清理 _queryCts（SQL 查询取消令牌），防止资源泄漏。

## [2026-05-12] 网络信息增强
- NetworkData 新增 MacAddress、LinkSpeed、AdapterType 字段。
- NetworkService 补充 MAC 地址格式化、链路速率（自动 Gbps/Mbps/Kbps）、适配器类型中文显示。
- 网络模块 UI 新增 MAC、速率、类型三行信息。

## [2026-05-12] 首页系统概览
- 新增 SystemInfoService，通过 WMI（Win32_Processor）和 GlobalMemoryStatusEx 获取 CPU、内存、系统盘、运行时间。
- 首页快捷入口下方新增系统概览卡片，展示操作系统、CPU、内存、磁盘、运行时间、.NET 版本。
- csproj 新增 System.Management 引用。

## [2026-05-12] 开机自启
- 新增 ToggleAutoStartCommand / IsAutoStartEnabled，通过注册表 HKCU\...\Run 管理 MyTools 开机自启。
- 系统托盘右键菜单新增"开启/关闭 开机自启"项，状态跟随注册表实时显示。

## [2026-05-12] 文件哈希校验
- 新增 FileHashService，异步计算文件 MD5 / SHA-1 / SHA-256，支持 Progress 回调和 CancellationToken。
- 截图工具页面新增"文件哈希校验"卡片，选择文件后自动计算三种哈希，结果以等宽字体展示，点击可复制到剪贴板。
- 新增 StringNotEmptyToVisibilityConverter 转换器。

## [2026-05-12] UI/UX 全面升级 — 第一阶段
- Theme.xaml 新增设计令牌：间距（ThicknessCardPadding/Margin/FieldRow/FieldGap）、字号（FontSizeCaption~Headline）。
- Theme.xaml 新增统一文本样式：TextHeadline / TextSubtitle / TextBodyStandard / TextCaption / TextHint / TextMonoCode。
- Theme.xaml 新增卡片样式：SectionCard / SectionCardOutlined / SectionHeaderRow。
- Theme.xaml 新增按钮样式：PrimaryActionButton / GhostActionButton / DangerActionButton / SuccessActionButton。
- Theme.xaml 新增渐变画刷：BrushAccentGradient / BrushBlueGradient / BrushTealGradient / BrushHeroGradient。
- Theme.xaml 新增 StatusPill 样式用于状态胶囊标签。
- 首页 Hero 卡片精雕：渐变背景 + 叠层商标 Orb + OS / 版本号 StatusPill 标签。
- 首页快捷按钮统一使用渐变圆角图标 + TextSubtitle / TextHint 排版。
- 首页新增"系统概览"卡片（SectionCardOutlined），展示 OS / CPU / RAM / 磁盘 / 运行时间 / .NET 版本。
- 全部 8 个模块页面头部统一：渐变圆角 Orb 图标 + TextHeadline + TextCaption 副标题。
- 网络卡片新增 MAC 地址、链路速率、适配器类型三行信息，排版更紧凑。
- 托盘菜单新增"开机自启切换"和"打开日志目录"条目。
- 截图设置"保存设置"按钮改用 PrimaryActionButton 样式。
- 文件哈希校验卡片使用 GhostActionButton + TextMonoCode 风格。

## [2026-05-12] UI/UX 全面升级 — 第二阶段
- 侧边栏：NavItemStyle 高度 56→42，增加圆角(8)与内边距(Margin 6,1)，更紧凑的 pill 形态。
- 侧边栏：选中指示条改为 3×18 居中短竖条，视觉更精致。
- 侧边栏：Logo 区重设计——圆角半透明背景承托 AppIcon + 双行标题(阿君的工具 / MyTools)。
- 侧边栏：新增"首页"导航按钮（ShowHomeCommand），Home 按钮上方有分隔线。
- 侧边栏：所有导航图标尺寸 22→20，间距 14→12，整体更紧凑。
- 侧边栏：底部版本号改用圆角半透明 Badge 容器。
- 侧边栏：宽度 220→200。
- 按钮统一：全局 MaterialDesignRaisedButton → PrimaryActionButton（34px 高，圆角 8）。
- 按钮统一：全局 MaterialDesignOutlinedButton → GhostActionButton（32px 高，圆角 8）。
- 按钮统一：BasedOn 引用同步更新（WireGuard 连接、启动项 toggle、Defender toggle、更新 toggle、快捷键设置等）。
- WireGuard 连接按钮高度 50→44，更协调。

## [2026-05-12] UI/UX 全面升级 — 第三阶段
- 全局文本样式统一：MaterialDesignSubtitle1TextBlock → TextSubtitle，MaterialDesignSubtitle2TextBlock → TextSubtitle，MaterialDesignCaptionTextBlock → TextCaption，MaterialDesignBody2TextBlock → TextCaption。
- 全面清除残留的 MaterialDesign 旧版排版样式引用，统一至 Theme.xaml 设计令牌体系。
- VPN 高级设置：字段间距改用 ThicknessFieldRow 令牌，提示文本改用 TextCaption + 品牌黄色 #F59E0B，生成按钮改用 PrimaryActionButton。
- Defender 状态徽章：改用 BadgeSuccess 基类，关闭时语义切换为 BrushError。
- Windows 更新状态徽章：改用 BadgeInfo 基类，停止时语义切换为 BrushWarning。
- SQL 导出"导出为 Excel"按钮改用 SuccessActionButton 绿色醒目风格。
- 截图行为描述 BasedOn 引用修正为 TextCaption。

## [2026-05-12] 新功能扩展 — 第一阶段（系统信息 + 文件验证）
- 新增 NuGet 依赖 LibreHardwareMonitorLib 0.9.4，由 Costura.Fody 嵌入到单 EXE（最终产物 ~12.8 MB）。
- 新增 Services/HardwareInfoService：通过 WMI 查询 OS / 主板 / BIOS / CPU / GPU / 内存条 / 硬盘 / 网卡 / 显示器全套硬件清单，全异步。
- 新增 Services/HardwareSensorService：包装 LibreHardwareMonitor，按需启动，支持 CPU/GPU/主板温度、风扇转速、电压、负载、频率、功率等实时读数。
- FileHashService 增加 CRC32 校验（自实现 IEEE 0xEDB88320 多项式表驱动），支持 MD5 / SHA-1 / SHA-256 / CRC32 四种算法。
- 侧边栏新增 2 个导航项："系统信息"（ChipOutline 图标）、"文件验证"（ShieldCheckOutline 图标）。
- MainWindow.xaml 新增 2 个模块页面：
  - 系统信息：硬件总览栅格（系统/主板、内存条、CPU、GPU、磁盘、网卡+显示器）+ 实时传感器 DataGrid，启用按钮在头部右侧。
  - 文件验证：独立 4 算法校验页，结果为大写十六进制等宽字体显示，点击复制。
- ViewModel 新增：HardwareSummaryInfo / IsSystemInfoBusy / SystemInfoStatusMessage / IsSensorsRunning / SensorReadings / SensorStatusMessage / SensorsToggleLabel；新增 ShowSystemInfoCommand / ShowFileVerifyCommand / LoadSystemInfoCommand / ToggleHardwareSensorsCommand / VerifyFileCommand。
- DispatcherTimer 每 2 秒采集一次传感器；ViewModel.Dispose 中正确停止并释放 sensor 与 service。
- 提示用户"传感器需管理员权限"以读取 EC（Embedded Controller）。

## [2026-05-12] 新功能扩展 — 第二阶段（格式转换 + 性能测试）
- 新增 Services/MediaConvertService：
  - 图片转换：使用 System.Drawing 内置实现，支持 JPG/PNG/BMP/GIF/TIFF 互转，可设最大尺寸 + JPEG 质量（EncoderParameter）。
  - 音视频转换：调用外部 ffmpeg.exe，自动检测 PATH / 程序目录 / C:\ffmpeg\bin\，未找到则提示用户安装；支持 MP4/MKV/AVI/MOV/MP3/WAV/AAC/FLAC/OGG 等，可传自定义 FFmpeg 参数。
- 新增 Services/BenchmarkService：
  - CPU 单线程：200K 次 SHA-256 哈希，报告 ops/s。
  - CPU 多线程：Environment.ProcessorCount 线程并行 SHA-256，报告总 ops/s。
  - 内存带宽：256 MB 缓冲区顺序读写，报告 GB/s。
  - 内存延迟：16 MB 工作集随机 pointer-chasing，报告 ns/access。
  - GPU 信息：WMI 读取显卡名称/显存/驱动版本。
  - RunAll：依次执行全部 5 项并汇总。
- 侧边栏新增 2 个导航项："格式转换"（SwapHorizontal 图标，琥珀色）、"性能测试"（SpeedometerMedium 图标，玫红色）。
- MainWindow.xaml 新增 2 个模块页面：
  - 格式转换：图片转换卡片（4 列参数输入）+ 音视频转换卡片（FFmpeg 状态徽章 BadgeSuccess/BadgeWarning）+ 转换结果显示。
  - 性能测试：头部"运行全部"按钮 + 5 个单项 GhostActionButton（CPU 单/多线程、内存带宽/延迟、GPU 信息）+ DataGrid 结果表（项目/得分/详情）。
- ViewModel 新增：ConvertStatusMessage / ConvertResult / IsFfmpegAvailable / ImageOutputFormat / ImageMaxWidth / ImageMaxHeight / ImageQuality / MediaOutputFormat / MediaExtraArgs / BenchmarkResults / BenchmarkStatusMessage / IsBenchmarkBusy；新增 ShowConvertCommand / ShowBenchmarkCommand / ConvertImageCommand / ConvertMediaCommand / RunAllBenchmarksCommand / RunSingleBenchmarkCommand。
- DetectFfmpeg 在切换到转换页时异步执行，结果写入 IsFfmpegAvailable。
- 最终产物仍为单一 MyTools.exe（12.83 MB），0 errors，2 warnings（NU1603）。

## [2026-05-12] 全项目稳定性 / 效率 / 资源占用 优化
目的：在不改变现有功能的前提下提升运行期稳定性、降低内存占用、消除潜在泄漏与死锁。

1) WMI 资源泄漏修复（HardwareInfoService / SystemInfoService / BenchmarkService）：
   - 之前 `foreach (ManagementObject mo in searcher.Get())` 未 Dispose `ManagementObjectCollection` 与每个 `ManagementObject`，在频繁查询下会积累 COM 对象。
   - 新增 `Query(wql, handler)` 统一辅助方法，使用 `using` 同时释放 searcher / collection / 每个 MO（`try { ... } finally { mo.Dispose(); }`）。
   - 增加 `EnumerationOptions { ReturnImmediately=true, Rewindable=false, UseAmendedQualifiers=false, DirectRead=true }` 复用实例，减少 WMI 内部缓存与数据量。

2) 启动性能：
   - `MainViewModel` 构造函数中原来同步调用 `SystemInfoService.GetSnapshot()`（包含 WMI Win32_Processor 查询，典型 100-300 ms），改为 `SafeFireAndForget(LoadSystemInfoSnapshotAsync())` 后台加载，窗口立即显示。
   - `SystemInfoService` 新增 `GetSnapshotAsync()` 入口，`SystemInfo` 属性变为可写并触发 `PropertyChanged`。

3) 时长计算 49 天溢出修复：
   - `SystemInfoService.GetSnapshot` 原使用 `Environment.TickCount & int.MaxValue`，超过 ~24.8 天后会归零。改为 P/Invoke `kernel32!GetTickCount64()`，Win7+ 全平台支持。失败回退原算法。

4) 传感器轮询 CPU 占用优化：
   - `HardwareSensorService` 的 `DispatcherTimer` 之前即使用户离开"系统信息"页也继续每 2 秒刷新。
   - `CurrentModule` setter 现在会在离开 SystemInfo 时自动 `_sensorTimer.Stop()`，返回时自动 `Start()`，只要 `IsSensorsRunning=true`。空闲时节省 ≥ 5% CPU。

5) 文件哈希 IO 4× → 1× 重写：
   - 原 `FileHashService.ComputeAsync` 依次调用 4 次 `ComputeHashAsync<T>` + CRC32，同一个大文件会被顺序读取 4 遍（10 GB ISO 实测约 4 倍硬盘时间）。
   - 改为单遍流式读取 1 MB 缓冲，分别调用 `md5.TransformBlock` / `sha1.TransformBlock` / `sha256.TransformBlock` + 内联 CRC32 累加，最后 `TransformFinalBlock`。IO 降到 1×，并新增 16 MB 粒度进度回报（"已扫描 X% (Y/Z MB)"）。
   - 缓冲区从 80 KB 升到 1 MB 以提升顺序读吞吐。

6) MediaConvertService 稳定性：
   - `FindFfmpeg`：之前直接 `WaitForExit(3000)` 而不读 stdout/stderr，如果 ffmpeg 输出量大会阻塞在管道上。改为先同步 `ReadToEnd()` 清空管道，再 `WaitForExit(5000)`，超时则 `Kill()`。
   - `ConvertMediaAsync`：`StandardError.ReadToEnd()` 在 `WaitForExit` 之前会在子进程输出量大时死锁。改为在后台 `Task.Run` 读 stderr，主线程同步读 stdout 并 `WaitForExit(300_000)`；超时则 `Kill()` 并返回超时错误，避免僵尸进程。
   - `EncoderParameters` 现包在 `using` 中，避免 GDI+ 对象累积。

7) BenchmarkService 释放内存：
   - 内存带宽测试 256 MB 缓冲完成后立即 `buffer = null; GC.Collect(2, GCCollectionMode.Optimized, false)`，避免数据在 LOH 停留。
   - GPU 信息查询的 `ManagementObjectCollection` / `ManagementObject` 现已完整 Dispose。

8) XAML 类型错误修复：
   - 转换模块两个主按钮原来用 `InverseBoolToVis`（Visibility）绑定给 `IsEnabled`（bool），类型不匹配、仅靠隐式转换凑效。已删除该绑定，交由 Command 的 CanExecute 自动控制禁用态。

构建验证：0 errors，单 EXE 仍为 12.83 MB。

## [2026-05-16] 新增"排班"模块
新增侧边栏【排班】，按需求文档第六节"排班管理（目标机制）"落地。

新增文件：
- `Services/ScheduleModels.cs`：`ShiftCell` / `EmployeeRow` / `ScheduleVersion` / `ShiftCodes` 静态映射（白卡副感大小休公午）。
- `Services/ScheduleService.cs`：版本 JSON 存储于 `%LOCALAPPDATA%\MyTools\Schedules\{YYYY-MM}\{ver}.json`；新建时复制最新版本人员名单；周一-五=6/周六=8/周日=9 默认每日总休。
- `Services/HolidayService.cs`：周末 + 用户可在 `holidays.json` 自定义法定节假日。
- `Services/ShiftAutoOptimizer.cs`：自动设置休息日。仅写白/休；硬约束 连续上班≤5；评分启发：周末配对(+12)、节假日(+5/+6) 、≥4 节假日休、≥8 总休、避免 3 连休(-8)；尊重 IsManual。
- `ViewModels/ScheduleViewModel.cs`：版本列表/编辑模式/夜班联动(大1=小→大→休→休; 大2=小→大→休)/统计/命令。
- `Views/NewScheduleDialog.xaml(.cs)`：新建对话框（年/月/版本名）。
- `Views/SchedulePage.xaml(.cs)`：3 表头 + 人员行的动态 Grid（代码生成）；单击单元格弹出 Popup 选 11 项（9 班次 + 大1/大2 + 清空）；节假日列底色橙；超 5 天连上 / 不足 8 天总休行高亮红。

集成：
- `MainViewModel`：新增 `ShowScheduleCommand` / `Schedule` (ScheduleViewModel)；构造时立即创建并加载版本列表。
- `MainWindow.xaml`：侧边栏【排班】按钮（CalendarMonth + Teal）；模块页通过 `<views:SchedulePage DataContext="{Binding Schedule}">` 绑定，按 `CurrentModule==Schedule` 显示。

边界：
- 编辑模式开关由 ViewModel.IsEditing 控制，查看状态下单元格按钮 IsEnabled=false。
- 保存时若同名版本已存在，弹窗确认覆盖；删除版本前确认。
- 自动优化只会写"白/休"两种，不会改 IsManual=true 的格子。
- 法定节假日支持：用户在 `%LOCALAPPDATA%\MyTools\holidays.json` 写入日期数组 `["2026-01-01", ...]`。

构建验证：0 errors，单 EXE 12.88 MB。

## [2026-05-12] 安装程序打包
- 新增 `MyTools.Installer` 安装器工程，生成单个 `MyToolsSetup.exe`，内嵌 Release payload 与卸载器，不依赖第三方打包工具。
- 新增 `MyTools.Uninstaller` 卸载器工程，卸载时删除程序文件、开始菜单快捷方式与卸载注册表项，默认保留同目录用户配置、日志和历史数据。
- 新增 `scripts/Build-Installer.ps1`：先构建主程序与卸载器，再按白名单压缩 payload 并嵌入安装器；默认包含 `NativeBinaries\ffmpeg\ffmpeg.exe`，可通过 `-SkipFfmpeg` 生成轻量包。
- 安装开始时先扫描 HKLM/HKCU 32/64 位卸载注册表视图；发现旧版 `MyTools` 或“阿君的工具”后，静默运行旧卸载器，成功后再继续安装新版。
- 兼容注册表缺失场景：目标目录存在旧卸载器时直接调用；如果是早期手工复制版本，则停止正在运行的 `MyTools.exe` 并清理已知程序文件后覆盖安装。
- 构建验证：`artifacts\installer\MyToolsSetup.exe` 生成成功，完整包约 40.65 MB，SHA256 为 `9AB16C3745D012C4E0387DD0499BE94C26769C0F8ED173F89A0EF3804E180AB4`。

## [2026-05-16] 构建警告与 OCR 兼容性优化
- 关闭占用 `bin\Release\net48\MyTools.exe` 的本地运行实例后重新建立 Release 构建基线，避免输出文件被进程锁定导致 MSB3021/MSB3027。
- `Microsoft.Windows.SDK.Contracts` 固定为构建实际解析成功的 `10.0.22000.194`，消除 `NU1603` 近似版本警告。
- `MyTools.csproj` 设置 `CopySQLiteInteropFiles=false`，避免 SQLite stub 包额外复制 `x86/x64/SQLite.Interop.dll` 到 Release 输出目录；SQLite 原生依赖仍由 Costura 嵌入。
- `ScheduleExcelExporter` 删除未使用的 `BORDER_NONE` 常量，消除 CS0219 警告，不改变排班 Excel 样式输出。
- `OsVersionService` 新增 `IsWindows10Version1903OrGreater`；`OcrService.IsSupported` 与 OCR 命令统一改用该判断，确保 WindowsOCR 入口只在 Windows 10 1903 及以上可用。
- `docs/功能说明.md` 同步文件哈希说明：当前实现为 MD5 / SHA-1 / SHA-256 / CRC32 单遍 1 MB 缓冲扫描。

## [2026-05-16] 第二轮改错与格式转换完善
- 截图工具页的文件哈希结果补齐 CRC32，并与文件验证页共用 `FormatFileHashResult`，避免两个入口展示不一致。
- 文件哈希初始提示更新为 `MD5 / SHA-1 / SHA-256 / CRC32`。
- `MediaConvertService.FindFfmpeg` 优先检测 `NativeBinaries\ffmpeg\ffmpeg.exe`，与录像/录音模块和安装包内置路径保持一致。
- FFmpeg 探测改为并发排空 stdout/stderr，降低外部进程输出阻塞风险。
- 图片/音视频转换输出路径新增自动避让：已有 `_converted` 文件时生成 `_converted_2`、`_converted_3` 等，不覆盖旧结果。
- 图片输出格式增加白名单清洗，空值或不支持格式回退 `png`；音视频输出格式空值或不支持格式回退 `mp3`。
- `scripts/Build-Installer.ps1` 新增 `Invoke-CheckedDotnetBuild`，每次 `dotnet build` 后检查 `$LASTEXITCODE`，避免构建失败后继续打包旧产物。

## [2026-05-16] 录像文件缺失修复
- 根据 Release 日志定位到录像实际写入目标目录，但 ffmpeg 打开音频输入设备失败后只留下 0B `.mp4`，导致用户在文件夹中找不到有效录像。
- `RecordingService.StartVideoRecordingAsync` 增加启动探测：带音频录像若 ffmpeg 立即退出，自动删除空文件并降级为仅录制画面；仅画面仍立即退出时直接提示录像未开始。
- `RecordingService` 在启动录像前将录制区域宽高自动向下修正为偶数像素，修复用户拖出 `1341x771` 等奇数尺寸时 `libx264 + yuv420p` 打开编码器失败的问题。
- 新增 `WasapiLoopbackAudioRecorder`，录像与独立录音改为程序内置 WASAPI Loopback 采集系统播放声音，不再依赖 `virtual-audio-capturer`、`Stereo Mix`、`立体声混音` 或麦克风设备。
- 录像时 ffmpeg 只录画面到临时 MP4，程序同步采集系统声音到临时 WAV；停止后自动合成为最终 `H.264 + AAC + MP4`，并清理临时文件。
- 独立录音改为采集系统播放声音，停止后通过 ffmpeg 转码为 `.m4a`；采集为空时直接提示，不生成静音文件。
- 修复纯录音停止后清理临时文件时空路径触发 `ArgumentException` 的问题；清理临时文件失败也不再阻断录制状态释放，避免后续误报“当前已有录像/录音任务在进行”。
- `MyTools.csproj` 新增 `NAudio 2.2.1`，由 Costura 合并进单 exe。
- `RecordingService.StartAudioOnlyAsync` 同步增加启动探测，音频设备被占用或禁用时避免进入虚假的“录音中”状态。
- `MainViewModel.StopVideoRecordingInternalAsync` 停止后按实际输出路径检查文件存在性与字节数，不再把 `TimedOut=false` 的 0B 文件当成成功。
- 录像保存成功后自动打开资源管理器并选中新文件；提示中标明“包含音频”或“仅画面”，便于确认本次录制内容。
- ffmpeg 音频设备枚举与运行日志读取改用系统默认编码，降低中文设备名在日志中显示为乱码的概率。

## [2026-05-16] 图片查看与简单编辑
- 新增左侧导航“图片查看”，支持在主程序内打开 `PNG/JPG/BMP/GIF/TIFF` 图片并预览。
- 图片查看页新增缩放、向左/向右旋转、水平/垂直翻转、灰度处理、复制到剪贴板、另存为新文件等轻量编辑能力。
- 复用现有截图标注编辑器，图片可一键进入标注模式继续使用画笔、矩形、线条、橡皮、复制和另存。
- 支持在“图片查看”页拖入图片文件直接打开，不影响文件验证、Codex 配置等现有拖拽流程。
- 图片另存支持 `PNG/JPG/BMP/TIFF`，JPEG 输出自动转为 `Bgr24`，避免透明通道导致编码失败。
- `MainViewModel` 新增图片查看状态、命令与 GDI+/WPF 位图转换逻辑，不引入 .NET Core 或额外运行时依赖。

## [2026-05-16] 视频查看
- 新增左侧导航“视频查看”，支持打开或拖入 `MP4/MOV/WMV/AVI/MKV/WEBM/FLV` 等常见视频文件。
- 使用 WPF `MediaElement` 作为内置播放器，提供播放/暂停、停止、进度拖动、音量调节和静音。
- 视频页显示文件名、路径、当前播放时间、总时长与播放状态。
- 离开“视频查看”页时自动暂停，防止切走模块后继续播放声音。
- 播放失败时提示系统解码器问题，并保留“外部打开”按钮调用系统默认播放器兜底。
- `MainWindow.xaml.cs` 新增播放进度 `DispatcherTimer`、拖拽视频识别、播放事件处理和关闭时资源停止逻辑。

## [2026-05-16] 音视频文件关联
- 新增共享 `MediaFileAssociationCore`，统一维护音频/视频扩展名清单与注册表写入逻辑。
- 安装器完成安装前会询问是否关联常见音视频文件；静默安装可传入 `/associate-media` 强制关联。
- 系统设置页新增“关联所有音视频”按钮，可在安装后重新写入当前用户文件关联。
- 文件关联写入 `ProgId=MyTools.MediaFile`、`Applications\MyTools.exe`、`RegisteredApplications` 与 `Capabilities\FileAssociations`，并通知 Shell 刷新关联缓存。
- 卸载器增加关联清理，删除 MyTools 写入的 ProgId、应用能力注册和 OpenWithProgids；不阻断配置/日志保留策略。
- `App.xaml.cs` 支持启动参数中的音视频路径；已有实例运行时通过 pending-open 文件唤醒原窗口并打开该文件。
- 视频查看入口扩展为音视频播放入口，新增 MP3/WAV/M4A/AAC/FLAC/OGG/OPUS 等音频扩展名。

## [2026-05-16] 排班 Excel 导出修复
- 修复 `ScheduleExcelExporter` 的 OpenXML 工作表元素顺序：`sheetViews` 现在写在 `cols` 之前，避免 Excel 打开时提示修复或无法正常读取。
- 排班导出的表头改为与界面一致：第 3 行输出“已休”，第 4 行输出“需休”，人员排班从第 5 行开始。
- 导出时按每列实际休息人数计算“已休”，低于“需休”时使用红色统计样式提示；冻结窗格同步调整为 `B5`。
- `ScheduleViewModel` 的导出保存对话框新增 `.xlsx` 自动补全、覆盖确认，并清洗版本名中的非法文件名字符，避免版本名包含 `/ : * ?` 等字符时保存失败。
- 验证：Release 构建 0 warning / 0 error；测试导出 `artifacts\schedule-test.xlsx` 可作为 zip 正常读取，`sheetViews` 位于 `cols` 之前，且第 3/4 行分别包含“已休/需休”。
- 重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.97 MB，SHA256 为 `83E09240F66BE22693BEDE2542B25EB7C871144A589A8156307B052654111A3C`。

## [2026-05-16] 紧凑界面与排班体验优化
- `Theme.xaml` 下调全局卡片圆角、卡片内边距、按钮高度、输入框高度和标题字号，减少工具型页面的空白占位。
- `MainWindow.xaml` 将默认窗口改为 1120x720、最小窗口改为 980x640；左侧导航宽度从 200 缩至 176，导航项高度从 42 缩至 34，并给导航列表增加滚动容器。
- 首页 Hero、快捷入口和系统概览卡片同步压缩，保留关键信息但减少装饰区域。
- `SchedulePage` 版本栏、工具栏、日期单元格、统计列和底部图例改为紧凑布局；工具栏按钮文案压缩为“自动休息 / 导出”等短命令。
- 排班表重建增加 45ms 节流，连续点击班次时不再每次立即完整重建，降低多人排班时的 UI 卡顿。
- 修正排班“已休”统计：界面和 Excel 导出均保留“午”班次的 0.5 天休息，不再四舍五入成整数。
- `SystemSettingsPage` 重排为“导出备份 / 导入恢复 / 音视频关联”三块操作面板，减少长段说明文字，保留 DPAPI 跨账户导入风险提示。

## [2026-05-16] 播放与转换界面再压缩
- 左侧导航和播放器标题将“视频查看”统一改为“音视频播放”，与已支持的 MP3/WAV/FLAC/M4A 等音频格式一致。
- `MainWindow.xaml` 压缩截图工具页头部、快捷键卡片、截图行为卡片和文件哈希卡片的边距，减少顶部操作区高度。
- 图片查看页右侧信息栏从 260 缩至 220，预览空态和文件信息文字同步压缩，保留打开/复制/另存/标注等高频操作。
- 音视频播放页右侧信息栏从 260 缩至 220，空态提示改为“打开音频或视频开始播放”，支持格式提示补充音频扩展名。
- 格式转换页改为左右双列紧凑布局：图片转换与音视频转换并排显示，输入项改用 `StandardTextBox`，结果区固定在底部。
- `MainViewModel` 新增 `ConvertOutputPath` / `HasConvertOutputPath` / `OpenConvertOutputFolderCommand`；转换成功后结果区显示“打开位置”，直接用资源管理器选中新文件。

## [2026-05-16] 格式转换批量处理扩展
- 图片转换与音视频转换的文件选择框开启多选；一次可选择多个图片或多个音视频文件进行批量转换。
- 批量转换按顺序逐个执行，单个文件失败不会中断后续文件；结束后汇总成功数、失败数和前若干个输出文件名。
- `MainViewModel` 新增 `ConvertOutputFolder` / `HasConvertOutputFolder` / `HasConvertOutputTarget`，让“打开位置”同时支持单文件定位和批量输出目录打开。
- 批量成功文件位于同一目录时，结果摘要显示输出目录；位于多个目录时提示“打开位置”会进入第一个输出目录。
- 格式转换页按钮文案改为“选择图片（可多选）”和“选择音视频（可多选）”，让批量能力在界面上直接可见。

## [2026-05-16] 文件校验官方哈希比对
- 截图工具页与文件验证页新增“官方校验值”输入框，支持粘贴 MD5 / SHA-1 / SHA-256 / CRC32。
- `MainViewModel` 保留最近一次结构化 `FileHashResult`，输入校验值时实时自动比对，不再从展示文本反解析。
- 校验值解析支持 `SHA256: xxxx`、纯哈希、带空格/换行的复制文本；仅接受 8/32/40/64 位十六进制值，避免误判。
- 比对结果在界面内以紧凑状态条显示“匹配”或“不匹配”，下载文件校验流程更接近 HashTab / 7-Zip 的使用习惯。
- `FileHashService` 修复历史遗留的中文进度提示乱码，保留原单遍 MD5 / SHA-1 / SHA-256 / CRC32 计算逻辑。
- 新增 `docs/功能对标改进清单.md`，按 ShareX / VLC / HandBrake / IrfanView 等同类软件思路梳理各模块已落地能力与后续增强方向。
- 验证：Release 构建 0 warning / 0 error；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.98 MB，SHA256 为 `04FC80ED814E9C974444EDD975E79F1E499C262ECDC60E3A25116DF7E0003B5C`。

## [2026-05-16] 音视频播放速度与乱码修复
- 音视频播放页新增 `0.5x / 1x / 1.5x / 2x` 速度档位，切换后立即应用到 `MediaElement.SpeedRatio`。
- 播放速度在底部状态行和右侧媒体信息中同步显示，打开新文件时自动恢复 `1x`。
- 修复 `MainWindow.xaml.cs` 中播放状态、文件校验复制提示、拖放校验提示的历史乱码文本。

## [2026-05-16] 执行功能对标清单第一批
- 图片查看打开图片后自动扫描同目录图片，新增上一张/下一张按钮，右侧显示当前目录位置。
- 音视频播放支持一次打开或拖入多个文件形成播放列表，播放结束后自动进入下一项，底部和右侧显示列表位置。
- 音视频播放新增快捷键：空格播放/暂停，左右方向键快退/快进 5 秒，上下方向键调节音量。
- 格式转换新增输出位置选择：同目录、桌面、指定目录；指定目录可通过按钮选择。
- 格式转换新增常用预设：图片压缩、头像 512、视频转 MP4、音频转 MP3。
- `MediaConvertService` 支持传入输出目录并自动创建目录，仍保留同名文件自动避让策略。
- 验证：Release 构建 0 warning / 0 error；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.98 MB，SHA256 为 `694E4B7F262793DC85D4BC123B460143F57CE8C06FD983DB96C7C584CA25C220`。

## [2026-05-17] 执行功能对标清单第二批
- 截图工具页新增“录制输出目录”卡片，录像目录和录音目录均可直接查看并重新选择，不再只能首次弹窗设置。
- 文件验证页支持一次选择或拖入多个文件，批量计算 MD5 / SHA-1 / SHA-256 / CRC32 并显示表格。
- 批量校验结果新增“导出清单”按钮，输出 UTF-8 `.txt`，包含生成时间、完整路径和四种校验值。
- 单文件校验时会清理旧批量结果，避免界面残留过期清单；多文件拖放现在会接收全部文件而不是只取第一个。
- 启动项管理新增搜索框，可按名称、命令和位置实时筛选。
- 程序卸载页新增搜索框，可按程序名、发布者、版本、安装路径和来源实时筛选，并显示筛选数量。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.98 MB，SHA256 为 `FC91061700FC16CACEDB4F80CCDAEA00BF2D892E3F30B51B1C29820CDC902633`。

## [2026-05-17] 执行功能对标清单第三批
- 启动项管理会从注册表命令中保守解析可执行路径，读取文件版本信息中的公司名作为发布者，列表中直接显示发布者和路径。
- 启动项搜索范围扩展到名称、发布者、可执行路径、原始命令和位置，更接近 Autoruns 的快速定位体验。
- 程序卸载确认框增加发布者、版本、安装位置、来源、权限提示和完整卸载命令；确认后仍只启动软件自带卸载程序。
- 格式转换新增“取消”按钮和当前任务 `CancellationTokenSource`；取消图片转换会停止后续队列，取消音视频转换会尝试终止正在运行的 FFmpeg。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.98 MB，SHA256 为 `AB4C8206BD8361B58CC26E113FD738BAEC7258246308661F1AF55993D554A62C`。

## [2026-05-17] 执行功能对标清单第四批
- SQL 查询结果新增“导出 CSV”按钮，使用 UTF-8 文本输出，并正确转义逗号、引号和换行。
- 性能测试结果区新增“复制”和“导出”按钮，导出 `.txt` 包含生成时间、系统版本、项目、得分、耗时和详情。
- WireGuard 面板新增“导入配置”按钮，可选择 `.conf` 文件回填配置内容，并用文件名作为默认接口名。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.99 MB，SHA256 为 `DA08E97C27D29BA47EE01D27FA556F4F1AC4659CC8AF63062C019F6F00BA742C`。

## [2026-05-17] 执行功能对标清单第五批
- 格式转换新增 `ConvertQueueItem` 队列模型，批量转换开始时按所选文件初始化“等待”队列。
- 图片转换和音视频转换逐项更新队列状态：等待、转换中、完成、失败、已取消，并记录输出路径或错误消息。
- 格式转换页新增“转换队列”表格，展示类型、文件名、状态、输出路径和消息；转换摘要与打开位置按钮继续保留。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.99 MB，SHA256 为 `CC0594620FB7DDBB5B2AA8E2FF99864DAF824072E3B8D5DD100FB7B3B7AA5A24`。

## [2026-05-17] 执行功能对标清单第六批
- 格式转换队列新增“清空”按钮，转换空闲时可一键清除当前队列。
- 转换队列表新增操作列：每项可打开输出位置，也可复制当前消息/输出路径/状态。
- 队列项完成、失败或取消后会触发命令状态刷新，让行内按钮按实际可用性启用或禁用。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.99 MB，SHA256 为 `5CB9D90AC5DD41995B276F6FD960D1AFBC8B44C622B5E5D86A012C5848927F14`。

## [2026-05-17] 执行功能对标清单第七批
- 格式转换队列项新增“重试”按钮，可在转换空闲时按当前格式、质量、输出目录设置重新处理原文件。
- 格式转换队列项新增“移除”按钮，可直接清掉单个已完成、失败或不需要继续关注的队列项。
- 重试前会校验源文件是否仍存在；音视频重试会复用 FFmpeg 可用性检测，未检测到 FFmpeg 时给出明确提示。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.99 MB，SHA256 为 `CB44E15AE2BFAE32BD49122FDB248C16002A7524BE81D7BBAADF9BD8E4A8B36E`。

## [2026-05-17] 执行功能对标清单第八批
- 文件验证页的批量校验结果新增 MD5 / SHA-1 / SHA-256 / CRC32 四个一键复制按钮。
- 复制内容采用“文件名 + 制表符 + 校验值”的逐行格式，便于直接粘贴到 Excel、工单或发布记录。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.99 MB，SHA256 为 `D57E6111E56C68C3AB4ABEE34C435CA8E1151FEF1E5741F08C6E234F2F58EBAE`。

## [2026-05-17] 执行功能对标清单第九批
- 启动项管理新增文件存在性状态：文件存在、文件不存在、未识别路径。
- 启动项命令解析在文件缺失时会保留疑似 `.exe/.bat/.cmd/.com/.ps1` 路径，便于判断残留启动项。
- 启动项搜索范围扩展到文件存在性状态，可直接搜索“文件不存在”定位失效项。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.99 MB，SHA256 为 `7F228CBA34F727A32E3C244BF4CA316BE6D1D28758979B37CA0B9D6CD4686F0F`。

## [2026-05-17] 执行功能对标清单第十批
- 启动项管理新增数字签名状态：已签名、未签名、未检测签名。
- 文件存在时会读取 Authenticode 签名证书；已签名项的悬浮提示展示证书主体，避免用户必须手动打开文件属性确认。
- 启动项搜索范围扩展到签名状态和证书主体，可搜索“未签名”或签名主体快速定位。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.99 MB，SHA256 为 `FA76A45931548C12735C030F44DF0DC71E16ED8D952D932279A150A5B742F8F4`。

## [2026-05-17] 执行功能对标清单第十一批
- 程序卸载列表保留结构化安装日期和估算大小，供筛选与排序使用。
- 程序卸载页顶部新增排序下拉框，支持名称、安装日期新旧、估算大小高低和发布者排序。
- 程序卸载页顶部新增大小与安装日期过滤，可快速定位未知大小、大体积程序、近期安装程序或日期缺失条目。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.99 MB，SHA256 为 `324D7755210C831FF49D583F0170A1D69BC4C6D87AFB5E99E94BFEDB25098D6E`。

## [2026-05-17] 执行功能对标清单第十二批
- 程序卸载列表新增勾选列，支持批量选择需要核对或记录的程序。
- 程序卸载页新增“全选筛选”“清除选择”“导出清单”按钮；导出时优先导出已勾选项，未勾选时导出当前筛选结果。
- 导出的程序清单包含程序名、版本、发布者、安装日期、估算大小、来源、安装位置、权限提示和卸载命令；不提供批量自动卸载，避免误操作。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 40.99 MB，SHA256 为 `A33B639253DB5053AA0A53E2EFE2877C7FE01C95E50DAD57A340935A86BD9902`。

## [2026-05-17] 执行功能对标清单第十三批
- SQL 连接历史新增最近连接组合，保存服务器、端口、用户名和密码；密码继续使用 DPAPI 当前用户范围加密。
- SQL 导出页新增“最近连接”快捷卡片，点击后回填服务器、端口、用户名和密码，并提示重新测试连接。
- 旧版仅保存服务器/用户名/密码列表的历史文件仍可读取；没有组合记录时会用最后一次成功连接生成一个快捷卡片。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41 MB，SHA256 为 `1C89F0E2F9FA4EC2E45EEF087392D6970BFC00DE53D5866635A4A39C059D1EB6`。

## [2026-05-17] 执行功能对标清单第十四批
- SQL 查询页新增“复制结果”按钮，支持复制选中单元格/行；未选择时复制全部查询结果。
- 复制内容使用表头 + 制表符分隔文本，便于粘贴到 Excel、记事本或工单。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41 MB，SHA256 为 `536A7665FB46CB8F996C11EBC26DF219618E840ECB1C941D9B9C095C2DF4AE2B`。

## [2026-05-17] 执行功能对标清单第十五批
- SQL 表导出新增进度回调，状态栏显示当前阶段、已处理行数、总行数、耗时和已生成文件大小。
- 查询结果导出 Excel / CSV 共用同一套进度显示，完成后弹窗汇总行数、耗时、文件大小和保存路径。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41 MB，SHA256 为 `8195D3F210BF4261EB060099180E24AD6AE6556D5A701022395E7BFF19E9D650`。

## [2026-05-17] 执行功能对标清单第十六批
- 排班页新增右侧冲突检查侧栏，实时列出每日需休未达标、人员休息不足和连续上班超限。
- 排班工具栏新增导入人员、导出人员模板、复制上月人员；右侧新增“整月复制”，可把某人的整月班次复制给另一人。
- 底部说明改为固定班次颜色图例，白/卡/副/感/大/小/休/公/午的颜色含义保持可见。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.01 MB，SHA256 为 `08F69612476381554CEE34C16B79CBE21FC7B7E46204C1FF0310D53FBB80FE41`。

## [2026-05-17] 执行功能对标清单第十七批
- 首页新增“功能搜索”区，可按截图、录像、导出、转换、排班、哈希、网络等关键词快速跳转到对应模块。
- 首页新增“最近使用”区，记录最近打开的图片、音视频、格式转换输出和排班入口，最多保留 8 条。
- 点击最近使用项可重新打开图片/音视频，或进入格式转换页并定位输出文件；文件不存在时回退为跳转模块。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.01 MB，SHA256 为 `A22A4CDD57EDDB37399A93DA473B3AA71AD39B1487F0AA86E056A996B56A3F03`。

## [2026-05-17] 执行功能对标清单第十八批
- 截图工具页新增“截图历史”，每次成功截图后自动保存 PNG 到程序同级 `Screenshots` 文件夹。
- 截图历史最多保留 20 张，页面显示缩略图、时间、大小和尺寸，并支持复制、编辑、打开、删除。
- 启动时自动扫描已有截图历史；区域截图取消选择时不会生成历史文件。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.01 MB，SHA256 为 `05DB5CDD6CBC8DC580898285C7E06D98696F8EE86E657098EB61D1A7EEFFADBB`。

## [2026-05-17] 执行功能对标清单第十九批
- 区域录像开始前新增 3 秒倒计时，倒计时结束后才真正启动 ffmpeg 录制。
- 录像区域窗口新增录制状态条，录制中显示“录制中 mm:ss / h:mm:ss”。
- 倒计时期间关闭区域窗口会取消启动，不生成空录像文件。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.01 MB，SHA256 为 `4B7463599BE6F8547804E1F9D7892C278122EE769134AF9214D29895D748ADC6`。

## [2026-05-17] 执行功能对标清单第二十批
- 截图工具页新增区域录像输出模式开关，可在 MP4 和 GIF 之间切换。
- GIF 模式输出 `.gif`，使用 ffmpeg `gdigrab + palettegen/paletteuse` 生成 12 FPS 动图，不启动系统声音采集。
- MP4 模式保持原有画面录制和系统声音合成逻辑，录制倒计时与时长提示继续共用。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.01 MB，SHA256 为 `A501CE328D0EA2C765DC1A4C162E2423008C6B5869161973BE90293675988624`。

## [2026-05-17] 执行功能对标清单第二十一批
- 图片查看页右侧新增“轻编辑”区，支持中心裁剪预设：原图、1:1、4:3、16:9。
- 图片查看新增亮度、对比度和锐化滑块，并提供“一键重置轻编辑”。
- 图片复制、另存和标注编辑器入口均使用当前预览中的裁剪、调色、锐化、旋转和翻转结果。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.02 MB，SHA256 为 `EC6D2902E815548540AFA53FF861FA984107B9BE07AB727D462330D32AF7B796`。

## [2026-05-17] 执行功能对标清单第二十二批
- 音视频播放模块新增 `.srt` 外挂字幕支持，可手动选择字幕文件，也会在打开媒体时自动载入同目录同名字幕。
- 播放画面底部新增字幕浮层，随播放进度、拖动进度和快进快退同步刷新。
- 右侧媒体信息栏新增字幕文件名、载入状态、“选择字幕”和“清除字幕”操作。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.02 MB，SHA256 为 `EA5752ECE80BA5B47E2C26DCD700201E15581557451F0AA5248E7183373A93C6`。

## [2026-05-17] 执行功能对标清单第二十三批
- 音视频播放字幕新增时间偏移滑块，支持 `-10.0` 到 `+10.0` 秒微调，拖动进度、快进快退和正常播放均按偏移后的时间匹配字幕。
- 音视频播放字幕新增字号滑块，支持 `12px` 到 `28px`，字幕行高随字号自动调整。
- 右侧媒体信息栏保持紧凑布局，字幕选择、清除、偏移和字号控制集中在“字幕”区域。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.02 MB，SHA256 为 `7DB0FDFD3C323A53AC18319E439DCBEE4149117A8545033F0E5F2ADD7B0CC4FC`。

## [2026-05-17] 执行功能对标清单第二十四批
- 音视频播放新增播放模式：顺序、列表循环、单项循环、随机。
- 播放结束时会按当前模式决定下一步：顺序到末尾停止，列表循环回到开头，单项循环重播当前项，随机跳到不同项。
- 右侧媒体信息栏新增播放模式下拉框，底部状态行同步显示当前模式；自动切换时状态文案区分“下一项 / 单项循环 / 随机播放”。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.02 MB，SHA256 为 `C226CA70997544224A5B7F1785C3FB84B67DE22D11B126EEE69A71F60DCD43B8`。

## [2026-05-17] 执行功能对标清单第二十五批
- 音视频播放右侧新增紧凑播放列表，显示序号、文件名和目录，并高亮当前播放项。
- 播放列表支持双击切换播放项，切换后保留原有播放控制、字幕自动载入和最近使用记录逻辑。
- 播放列表支持拖拽排序，排序后当前播放项会按文件路径重新定位，避免拖拽后指向错误项。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.02 MB，SHA256 为 `8DAF858EC7A2FCF452845FBDAA02E23C2F2CA20E001E62859681555D4CF4B140`。

## [2026-05-17] 执行功能对标清单第二十六批
- 音视频播放右侧播放列表新增载入和保存按钮，支持载入 `.m3u/.m3u8`，保存当前列表为 UTF-8 `.m3u8`。
- 保存播放列表时写入 `#EXTM3U` 和 `#EXTINF`，并优先生成相对路径，方便整文件夹移动。
- 载入播放列表时会解析相对路径、过滤不存在文件和暂不支持的扩展名，再按列表顺序载入播放器。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.02 MB，SHA256 为 `484BEFF75A5499D800E4F88CBF953DC5F58BAFAA99AB604851CCD3D720AF716E`。

## [2026-05-17] 执行功能对标清单第二十七批
- 音视频播放新增最近播放列表记录，保存或载入 `.m3u/.m3u8` 后自动记录到 `MyTools.settings.json`。
- 右侧播放列表区新增“最近列表”快捷入口，显示播放列表文件名和最近使用时间，点击即可重新载入。
- 最近播放列表最多保留 6 条；文件不存在时会从界面移除并同步更新本地设置。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.03 MB，SHA256 为 `7261F30CD54F0D7C64C93C5781756BA395499E434F180CD6C046FFCBE2B38E95`。

## [2026-05-17] 执行功能对标清单第二十八批
- 音视频播放右侧播放列表新增搜索框，可按序号、文件名、目录和完整路径过滤当前列表。
- 搜索只影响右侧列表显示，不改变真实播放顺序；上一项、下一项、循环和随机仍按完整播放列表执行。
- 搜索框右侧显示“筛选 N / 总数”，清空搜索时显示总项目数。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.03 MB，SHA256 为 `3C634BFD971B856EE4DC5E7EA96E174F9E5AFE46C0C92BC12126CA9E2B4F2136`。

## [2026-05-17] 执行功能对标清单第二十九批
- 音视频播放列表新增单项移除按钮，每一项右侧可直接从列表删除，不影响磁盘文件。
- 播放列表顶部新增清空按钮，清空后会停止当前播放并同步清理媒体路径、进度、时长和字幕状态。
- 移除当前播放项时会自动定位到相邻项；移除非当前项时保留当前播放指向，避免列表序号变化后错播。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.03 MB，SHA256 为 `79335EEBAF6AFC393DF83CE7AF14F99914D2A8A5AB00E2F99B78E56585E5E7D5`。

## [2026-05-17] 执行功能对标清单第三十批
- 音视频播放列表顶部新增复制按钮，可将当前列表完整路径按“一行一个”复制到剪贴板。
- 播放列表顶部新增清理失效项按钮，可批量移除不存在或扩展名不支持的条目，不删除磁盘文件。
- 清理失效项会保留有效的当前播放项；若当前项被清理，会自动切换到相邻有效项，避免播放指向悬空。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.03 MB，SHA256 为 `D3F43F31FB11C1A125B87F18A2A25D562501D73400AAE250D6D19430E044728A`。

## [2026-05-17] 执行功能对标清单第三十一批
- 音视频播放列表顶部新增收藏按钮，可为当前列表命名并保存到本地 `MyTools.settings.json`。
- 右侧播放列表区新增“收藏列表”，显示收藏名称、项目数、最近使用时间和路径预览，点击即可快速载入。
- 收藏播放列表最多保留 8 条；重复收藏同一组文件会更新到顶部，打开收藏后也会自动前移。
- 收藏项支持单独移除，不影响磁盘文件和当前播放列表。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.03 MB，SHA256 为 `403CAD134FADBAF8D1883F4FB82748327F47B796950760F02A92A41EF4543E07`。

## [2026-05-17] 执行功能对标清单第三十二批
- 音视频播放新增临时 A/B 片段循环，可将当前位置设为 A 点或 B 点，并在越过 B 点后自动跳回 A 点。
- 播放控制区新增 A 点、B 点、循环开关、清除循环和添加书签按钮，底部状态行同步显示 A/B 循环状态。
- 右侧媒体信息区新增片段循环状态和“书签点”列表，书签支持双击或点击跳转按钮回到对应时间点。
- 切换媒体或清空播放列表时会自动清空临时 A/B 循环与书签，避免跨文件误跳转。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.03 MB，SHA256 为 `FDCF9AC2A24A0E4A070FB9E37994ED97F40FB68BBE8A8E17E77C4F1942BD4097`。

## [2026-05-17] 执行功能对标清单第三十三批
- 音视频播放新增“截帧”按钮，可将当前播放位置通过 FFmpeg 截取为 PNG。
- 截帧文件保存到程序同级 `VideoFrames` 文件夹，文件名包含原媒体名和时间点，并自动避让重名文件。
- 截帧成功后自动切换到图片查看并打开 PNG，可继续复制、另存、标注或使用轻编辑功能。
- 截帧复用 `MediaConvertService.FindFfmpeg` 的检测逻辑；未检测到 `ffmpeg.exe` 时会提示用户放入程序目录或加入 PATH。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.03 MB，SHA256 为 `07B044F6EB71361C56FD5DC569980BA75B5DA561A7565E3076E5E00389F3681A`。

## [2026-05-17] 执行功能对标清单第三十四批
- 音视频播放新增“波形”按钮，可通过 FFmpeg 为当前媒体音轨生成波形 PNG。
- 波形文件保存到程序同级 `AudioWaveforms` 文件夹，文件名基于原媒体名并自动避让重名文件。
- 波形生成成功后会在播放器进度条上方紧凑显示；打开新媒体或清空播放列表时会清除旧波形。
- 波形生成复用 `MediaConvertService.FindFfmpeg` 的检测逻辑；无音轨或缺少 `ffmpeg.exe` 时会给出明确提示。
- 验证：Release 构建 0 warning / 0 error；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.03 MB，SHA256 为 `30F473EDAB7122E8784302A96CAD49AC4361E3B5B835FD58611C11825EDE5747`。

## [2026-05-17] 执行功能对标清单第三十五批
- 音视频播放的波形图新增点击跳转能力，点击波形横向位置会按比例换算到媒体时长并执行 seek。
- 跳转后同步更新进度文本和字幕位置，状态栏显示“已按波形跳转”。
- 波形点击绑定到实际图片宽度，避免外层边距影响定位比例。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.03 MB，SHA256 为 `94C80224206A783141A4161A781F085DCFC25FD69B19B586DA9A633132CE9B60`。

## [2026-05-17] 执行功能对标清单第三十六批
- 音视频播放波形图新增红色当前位置指示线，播放、快进快退、拖动进度和点击波形后都会随当前位置刷新。
- 新增 `VideoWaveformPositionConverter`，按“波形实际宽度 × 当前秒数 / 总时长”计算指示线位置。
- 指示线不参与鼠标命中，不影响波形点击跳转。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.03 MB，SHA256 为 `38BEAC582D39CC882F151AE061DCB8BD190B0BC46309F270C6D70E39EE69BEC6`。

## [2026-05-17] 执行功能对标清单第三十七批
- 音视频播放波形图新增拖拽定位，按住波形左右拖动时会持续按横向比例 seek。
- 拖拽期间暂停计时器位置回写，避免播放器当前进度覆盖用户正在拖动的位置。
- 松开鼠标后释放捕获并保留最终定位点；若拖拽中鼠标按钮意外释放，也会自动退出拖拽状态。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.04 MB，SHA256 为 `7E803AC742AA6A6D27978E51B42F30CD7D9FC94C39FC7BF10C1B9531A3C0D505`。

## [2026-05-17] 执行功能对标清单第三十八批
- 音视频播放波形图新增 A/B 选区，Shift+左拖或右键拖拽波形可直接框选片段循环区间。
- 新增 `VideoWaveformRangeConverter`，按波形实际宽度、A/B 秒数和总时长绘制半透明选区层，当前位置红线继续保持在最上层。
- 波形选区与现有 A 点、B 点、循环开关共用同一套状态；拖拽结束会自动启用 A/B 循环，切换媒体或清空播放列表时继续清理临时区间。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.04 MB，SHA256 为 `A0F1BB55670BA2121D3462C71242E7E0C9A8618F0C26C64AC12B949E5D40CD32`。

## [2026-05-17] 执行功能对标清单第三十九批
- 音视频播放波形 A/B 选区新增左右微调手柄，框选后可拖动选区边缘调整 A 点或 B 点。
- 手柄复用 `VideoWaveformRangeConverter` 计算位置，并在贴近波形两端时自动限制在波形范围内。
- 微调过程中暂停播放器计时器位置回写，避免当前播放进度刷新干扰拖动；A/B 点保持最小 0.25 秒间距，防止左右边界互相穿越。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.04 MB，SHA256 为 `76C715D9E4BB13966D716BD63CB76370598E09A976DAB8AD0FDA20AF5EB85437`。

## [2026-05-17] 执行功能对标清单第四十批
- 音视频播放 A/B 循环新增区间记忆，当前 A/B 区间可通过“区间”按钮保存到右侧列表。
- 右侧媒体信息栏新增“A/B 区间”紧凑列表，显示区间起止和持续时间，支持套用并跳到 A 点、双击套用、单项删除。
- 区间记忆与当前媒体同生命周期，切换媒体或清空播放列表时会清空，避免跨文件误用。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.04 MB，SHA256 为 `6EDFFCD01C69B73938E7AD2E3DAD9004B3A47F79A1360C619861187BADE2D4C2`。

## [2026-05-17] 执行功能对标清单第四十一批
- 音视频播放保存 `.m3u8/.m3u` 播放列表时，会把当前媒体已保存的 A/B 区间写入 `#MYTOOLS-AB` 注释备注。
- A/B 区间备注按媒体路径匹配，并写在对应媒体条目前；标准播放器会忽略注释，不影响常规 M3U 兼容性。
- 保存完成状态会提示本次导出的 A/B 区间备注数量；载入播放列表仍跳过注释行，避免跨文件自动恢复误用。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.04 MB，SHA256 为 `4E3921ACED97FFCC57D8979CF336DCFAFB93434FE2847BABAAF4E185E82467C4`。

## [2026-05-17] 执行功能对标清单第四十二批
- 格式转换队列新增序号列、拖拽排序、上移和下移按钮，批量转换会按当前队列顺序执行。
- 转换中新增“暂停/继续”队列控制，暂停会在当前文件完成后阻止后续项继续执行，避免中途截断输出文件。
- 取消转换时会标记所有仍在等待或转换中的队列项为“已取消”，不再错误地只从固定索引开始标记。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.04 MB，SHA256 为 `4394345ACE00DD76B0333E187627321425EFA580FAF09183BB9BE63B99A164F2`。

## [2026-05-17] 执行功能对标清单第四十三批
- 文件验证新增“导入清单”，支持 MyTools 导出的校验清单，也兼容常见 `hash 文件名` / `文件名 hash` 文本行。
- 批量校验结果新增“比对”和“清单算法”列，导入清单后会对当前结果标注匹配、不匹配或未找到。
- 单文件校验和批量校验完成后都会尝试套用已导入清单，状态栏汇总匹配、不匹配和未找到数量。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.05 MB，SHA256 为 `A1A1A372572A8BEAB0235F4872086FA52B7BF99B0DEF443D3E956BEAEB6743D8`。

## [2026-05-17] 执行功能对标清单第四十四批
- SQL 表导出新增“取消”按钮，导出任务使用独立取消令牌，取消后状态栏显示“导出已取消”。
- SQL Server 表导出移除单工作表行数硬限制，超过 Excel 单 Sheet 上限时会在同一 `.xlsx` 内自动拆分多个 Sheet。
- 多 Sheet 写入仍保持流式读取 `SqlDataReader`，每个 Sheet 自动写表头并继续上报总体进度。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.05 MB，SHA256 为 `3DF7BC0DC3F4CEA9DD7C102E23F8AF5A2F8EB7BAC3FC6168D7A4F6C58C1BC8D4`。

## [2026-05-17] 执行功能对标清单第四十五批
- 排班右侧冲突列表新增“定位”按钮，可触发人员/日期定位并高亮对应单元格。
- 排班表格对被定位的冲突单元格使用红色边框和提示标记，便于从冲突列表快速回到表格。
- 右侧新增“周休均衡”统计，按每 7 天展示休息总量、人均休息和人员差值。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.05 MB，SHA256 为 `5E2001AE26F104A7348C8BBFCD3AB9E3DA4A5CB3A5DFF05E17503D236E603AED`。

## [2026-05-17] 执行功能对标清单第四十六批
- 系统信息实时传感器新增刷新频率选择：手动 / 2 秒 / 5 秒。
- 手动模式下可点击“刷新”单次读取传感器，自动模式切换后会调整 `DispatcherTimer` 间隔。
- 传感器读取后会检测温度 >= 85°C、负载 >= 95% 的异常项，并在状态栏提示对应硬件和传感器。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.05 MB，SHA256 为 `D10C8658BF122A9E32BB80736A3141EBFEAF75E5D9C80DEBD53AF0BB99B060E6`。

## [2026-05-17] 执行功能对标清单第四十七批
- 首页系统概览新增 CPU、内存、系统盘三张小型摘要卡片，显示百分比和进度条。
- `SystemInfoService` 快照新增 CPU 当前负载、内存占用率、系统盘占用率和摘要文案。
- 首页详细系统信息保留原有字段，并修正内存文本异步回填后不刷新的问题。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.05 MB，SHA256 为 `02CE0357E8B9CBB2679DE2EFD55DC3B7837B93EC3599B3A7F89CE9E37258AEE2`。

## [2026-05-17] 执行功能对标清单第四十八批
- 启动项签名信息新增证书链可信状态校验，已签名文件会显示“证书链可信”或“不可信或已过期”。
- 启动项搜索新增证书链状态匹配，签名提示中同时展示证书主体和链状态。
- 程序卸载列表新增“静默”列，识别 `QuietUninstallString`、MSI 卸载和常见静默参数作为静默候选。
- 卸载确认框与导出清单新增静默候选说明；程序仍默认只启动交互卸载向导，不自动批量静默执行。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.05 MB，SHA256 为 `0645A91316B8C76177200749FD3E8968842499CCFD7FBBE94AA594FF7BBF6555`。

## [2026-05-17] 执行功能对标清单第四十九批
- 垃圾清理候选项新增中文类别、大小、风险和建议字段，表格改为更易扫读的安全判断布局。
- 垃圾清理新增“导出报告”按钮，可在执行前导出已选择项、预计释放空间、风险分布、建议和完整路径。
- 微信清理候选项新增大小、风险、建议列，媒体和文件类会提示按日期确认并建议先备份。
- 垃圾清理确认框改为说明普通文件会进入回收站、系统缓存可能需要管理员授权，避免“不可撤销”误导。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.06 MB，SHA256 为 `516620757922BAD171ECF203171E8EF8014CA83537F76D3A3D876DA59A8F6DC2`。

## [2026-05-17] 执行功能对标清单第五十批
- 微信备份完成后会自动写入最近备份记录，记录 ZIP 路径、文件数、总大小和最近使用时间。
- 恢复区新增“最近备份”快捷列表，最多显示 6 条，点击即可套用备份 ZIP 并解析 manifest。
- 最近备份记录保存到 `MyTools.settings.json`；若文件已不存在，点击时会自动从列表移除并保存。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.06 MB，SHA256 为 `4161B3B94FCD0BAFD7B7A10E2B42304B5D97C193D840E34F1258A4C3C121FBCB`。

## [2026-05-17] 执行功能对标清单第五十一批
- WireGuard 页面新增连接日志按钮，可展开最近导入、连接、断开和状态检查记录。
- WireGuard 连接和断开会记录本次耗时，连接成功后显示最近连接时间。
- WireGuard 日志保留最近 80 条，并提供清空按钮，便于排查授权取消、网卡未上线或配置导入问题。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.06 MB，SHA256 为 `CC79C344B39DD78515903571E6FC294606CF0873FC99715AF31426057A8FF996`。

## [2026-05-17] 执行功能对标清单第五十二批
- WireGuard 服务新增 `Configs/*.conf` 扫描能力，按最近修改时间列出已保存隧道配置。
- WireGuard 页面新增“已保存隧道”列表，显示接口名、目录、大小和修改时间，点击即可加载配置。
- 导入 `.conf` 或连接成功后会自动刷新隧道列表，便于在多个配置之间切换。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.06 MB，SHA256 为 `F0676E7F752AB79AFDFE66AC60DAAA1AE9438AF4EDA1999A859B0578AEF5334C`。

## [2026-05-17] 执行功能对标清单第五十三批
- Codex 配置记录新增标签、最近应用时间，配置列表会按最近应用优先排序。
- Codex 配置卡片新增“差异”按钮，可对比记录内容与当前 `%USERPROFILE%\.codex` 下的 `config.toml`、`auth.json`，显示缺失、大小和行级差异摘要。
- Codex 配置卡片界面改为备注、标签、最近应用时间集中展示，右侧保留编辑、应用、导出和删除等紧凑操作。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.06 MB，SHA256 为 `3255A39FC3A3D6F0557292EB767E7A778281D48BB9FA0D7AF8E06D126E752282`。

## [2026-05-17] 执行功能对标清单第五十四批
- 系统备份服务改为分区导出/恢复，支持设置与历史、排班本地数据、Codex 当前配置、WireGuard 配置、FFmpeg 与依赖五类范围。
- 系统设置页导出区新增范围复选框和“预检”按钮，可在导出前列出将导出的文件数量、大小和缺失项。
- 导入恢复区新增范围复选框和“预览”按钮，导入前会显示备份来源机器/用户、文件数量、预计覆盖同名文件数和新增文件数。
- 导入确认沿用 DPAPI 跨账户风险提示，并只覆盖所选类别的数据，便于只恢复排班、Codex 或 WireGuard 等单项资料。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.07 MB，SHA256 为 `C1D29E629984FB7FCB4D8AC006ED2E32AF4F7E32EC371775DEAA9B29932B4F2E`。

## [2026-05-17] 执行功能对标清单第五十五批
- 文件关联核心新增视频/音频分类注册、当前用户关联状态检测和恢复默认关联能力，主程序、安装器和卸载器继续共用同一份实现。
- 系统设置页音视频关联区新增状态显示、刷新、只关联视频、只关联音频、关联全部和恢复默认按钮。
- 安装器静默参数新增 `/associate-video` 与 `/associate-audio`，保留 `/associate-media` 作为全量关联入口。
- 恢复默认关联会移除当前用户下 MyTools 写入的音视频默认关联，并提示 Windows 可能恢复原默认程序或要求重新选择默认应用。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.08 MB，SHA256 为 `C5BDB1D564722B30B505361207B8532775DC065D6FEB3135B9928E6744F782EF`。

## [2026-05-17] 执行功能对标清单第五十六批
- 微信备份 manifest 条目新增 `category` 字段，记录每个文件属于文字、图像、视频、语音、文件或缓存。
- 微信恢复区新增类别复选框，可只恢复选中的类别；未选类别会在恢复时跳过且不写入目标目录。
- 对旧备份缺少 `category` 的条目，恢复时会按相对路径尽量推断类别，保持向后兼容。
- 恢复清单摘要新增按类别统计，恢复完成提示会显示按类别跳过数量。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.08 MB，SHA256 为 `561AD947E7F773F13754D577628AF549A498DE0ED37EE63D659D4B9166594838`。

## [2026-05-17] 执行功能对标清单第五十七批
- WireGuard 服务新增本地隧道配置重命名和删除能力，会对接口名做文件名安全化并避免覆盖同名 `.conf`。
- WireGuard “已保存隧道”列表从整行点击改为明确的“加载 / 重命名 / 删除”操作区，减少误操作。
- 删除隧道仅删除程序同级 `Configs` 下的本地 `.conf` 文件，不主动卸载正在运行的 WireGuard 隧道服务，确认框已明确提示。
- 重命名或删除后会刷新隧道列表并写入连接日志；若当前编辑的是被重命名隧道，会同步更新接口名。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.08 MB，SHA256 为 `489CC9BA53EE410578BF687F38233A7F97C5021CFA925B3F2503C2B7203AE752`。

## [2026-05-17] 执行功能对标清单第五十八批
- 系统设置页音视频关联区新增“默认应用”按钮。
- Win10/11 下打开 `ms-settings:defaultapps`，Win7/8 下打开控制面板默认程序入口，便于恢复默认关联后继续指定播放器。
- 打开失败时会写入应用日志并提示失败原因。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.08 MB，SHA256 为 `8275C086E7D00E3E3E3B975287246935B2E594ECE3A3E90BA61AE80FD4E517D5`。

## [2026-05-17] 执行功能对标清单第五十九批
- Codex 配置导入前新增脱敏摘要确认窗口，用户可在保存前确认记录名、配置大小和关键字段。
- `config.toml` 摘要展示常用配置键，涉及 key、token、secret、password、credential 的值会统一显示为 `***`。
- `auth.json` 摘要改用 JSON 结构读取，只展示字段路径和类型，不展示认证明文。
- 用户取消预览确认时不会创建配置记录，状态栏提示“已取消导入”。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.08 MB，SHA256 为 `274F4FACD85CC60683EEAB9C23E400E0BAB6280D1B08EACBF291D9DB3A12C46B`。

## [2026-05-17] 执行功能对标清单第六十批
- 系统备份导出的 `manifest.json` 新增文件清单和大小记录，用于后续校验备份完整性。
- 系统设置页导入恢复区新增“校验”按钮，可检查备份内文件是否缺失或与 manifest 记录大小不一致。
- 旧备份没有文件清单时，会回退为扫描备份目录中的现有文件并给出基础校验结果。
- 校验结果会显示通过文件数、预期文件数、总大小和最多 20 条问题明细。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.08 MB，SHA256 为 `636566DB0C0CE179029395AE84E24E0B94257A19E631B606843ACFF7C2BFB0FD`。

## [2026-05-17] 执行功能对标清单第六十一批
- 首页系统概览新增传感器风险提示，显示传感器未启用、启动失败、读取失败、正常或异常状态。
- 系统信息页传感器读取到温度 >= 85°C 或负载 >= 95% 时，首页同步显示同一条异常提示。
- 传感器正常读取时，首页显示已读取项数量和未发现高温/高负载；非管理员或不支持时显示明确建议。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.08 MB，SHA256 为 `9A26A4EBBC741C9661C0138A2FDC91D2BB1D38FD8D5719B93E32DE7E47EE960F`。

## [2026-05-17] 执行功能对标清单第六十二批
- 启动项模型新增来源分类，区分 `注册表 Run` 与 `MyTools 禁用备份`。
- 启动项卡片将来源分类与当前用户/所有用户范围合并显示，便于判断条目来自启用区还是禁用备份区。
- 启动项搜索支持按来源分类和合并来源文本筛选，例如搜索“禁用备份”或“所有用户”。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.08 MB，SHA256 为 `FF7B4C5D05134264B72E04F5BA8CB4273DD290773C0477BC338D91453B0E8594`。

## [2026-05-17] 执行功能对标清单第六十三批
- 区域录像新增帧率选项：15 FPS、30 FPS、60 FPS。
- MP4 录像新增画质预设：体积优先、均衡、高清，分别映射不同 CRF 和 preset 参数。
- GIF 录制复用帧率设置但限制在 8~15 FPS 范围，避免动图体积失控。
- `RecordingService` 新增 `RecordingOptions`，启动录像时统一传入帧率、CRF 和 preset。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.09 MB，SHA256 为 `9C3B9F6DC42804E583711664EC64DD09EAB1EDBC9894F28034E7C185BAF29DE0`。

## [2026-05-17] 执行功能对标清单第六十四批
- WireGuard 已保存隧道列表新增“差异”和“导出”按钮。
- 差异预览会比较当前编辑区配置与保存的 `.conf` 文件，显示行数、大小和不同位置数量。
- 导出功能可将单个隧道 `.conf` 另存到用户选择的位置，并写入连接日志。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.09 MB，SHA256 为 `3B47F940B29DA4031F299CB1EB9529BDF156DA0EFE3AEC60F24C1BE59EA45C3C`。

## [2026-05-17] 排班编辑模式修复
- 修复排班页进入编辑模式后动态表格未重建的问题，避免标题已显示“编辑中”但班次格子、姓名和每日需休输入仍保持只读。
- 进入编辑模式后的状态栏改为提示可单击班次格子选择班次，也可修改姓名和每日需休人数。
- 验证：Release 独立输出目录构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；`artifacts\verify-build` 输出目录无托管 DLL。常规 Release 输出被正在运行的 `MyTools.exe`（PID 9904）锁定，未覆盖 `src\MyTools\bin\Release\net48\MyTools.exe`；已使用验证后的主程序和现有原生依赖重新生成安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.09 MB，SHA256 为 `4EDD23FD9D05D363A90E6C3CAC636EF44806CFC59B3957E21A655BA871AD820A`。

## [2026-05-17] 排班硬约束与表格缩放
- 排班表新增缩放按钮和 `Ctrl + 鼠标滚轮` 缩放，支持 65%~180%，便于在大屏与小屏下查看整月表格。
- 排班表恢复为 3 行表头：星期、日期、每日总休目标；人员行从第 4 行开始。
- 每日实际休息改为必须精确等于总休目标，少休和多休都会标红、进入冲突侧栏；总休目标支持 0.5 精度。
- 保存前新增硬约束拦截：每日实际休息必须等于总休目标，任一人员连续上班不得超过 5 天，否则阻止保存并列出问题。
- 自动设置休息日修正为按 0.5 精度统计手动休息，自动格只写入“白/休”，并在生成后修复或提示连续上班超限。
- Excel 导出同步恢复为 3 行表头，总休目标超过或低于实际休息都会标红。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 NU1900 警告；Release 输出目录无托管 DLL；重新生成完整安装包 `artifacts\installer\MyToolsSetup.exe`，大小 41.09 MB，SHA256 为 `04B8605C1913120B7E237EA33371DAC2FB4F220B26B813D6EA517531DCFE25C8`。

## [2026-05-18] 系统设置 - 桌面背景管理
- 新增 Services/WallpaperService.cs：图库目录 Wallpapers/、导入/删除/读取当前壁纸/写入壁纸（SPI_SETDESKWALLPAPER + WallpaperStyle/TileWallpaper 注册表），Win7-Win11 通用。
- 新增 ViewModels/SystemSettingsViewModel.Wallpaper.cs（partial）：缩略图集合、显示方式 5 档单选、导入/保存当前桌面/设为桌面/在看图模块浏览/移除/刷新/打开文件夹七个命令。
- Views/SystemSettingsPage.xaml 加桌面背景卡片：工具栏 + 显示方式 + 缩略图 WrapPanel ListBox（双击调用看图模块全屏浏览）。
- WallpaperItem 用 BitmapImage 懒加载缩略图（DecodePixelWidth=300）。

## [2026-05-18] 系统设置 - Windows 调整卡片
- 新增 Services/WindowsTweaksService.cs：时钟秒（ShowSecondsInSystemClock）、托盘图标全显（EnableAutoTray）、任务栏合并三档（TaskbarGlomLevel/MMTaskbarGlomLevel）、五类桌面图标显隐（CLSID HideDesktopIcons 注册表），以及 RestartExplorer()。
- 新增 ViewModels/SystemSettingsViewModel.WindowsTweaks.cs（partial）：七个绑定属性（ShowSecondsInClock/TrayShowAll/IsGlomAlways|WhenFull|Never/ShowComputer|RecycleBin|ControlPanel|UserFiles|Network）、状态消息、刷新命令、重启资源管理器命令；注册表写入即时生效，Explorer 重启需用户确认。
- SystemSettingsViewModel 构造函数末尾补调 InitWindowsTweaks()。
- Views/SystemSettingsPage.xaml 外层 Grid 增加第 5/6 行（Row 6），插入 Windows 调整卡片：三列布局（任务栏开关 / 合并档位 RadioButton 组 / 桌面图标 CheckBox 组）+ 底部状态栏（左侧状态消息，右侧说明提示）。

## [2026-05-18] 看图模块 - 满框显示 + 左侧文件夹树 + 顺时针旋转
- 新增 ViewModels/ImageFolderNode.cs：懒加载资源管理器风格的文件夹树节点，CreateRoots() 枚举全部已就绪驱动器根目录，IsExpanded setter 触发子目录加载，过滤 System/Hidden 目录。
- MainViewModel 新增 ImageFolderRoots（ObservableCollection）、ImageViewerFitToFrame 属性、ImageViewerFitToFrameCommand（切换 Stretch 模式，启用时重置 Zoom = 1.0）；Zoom 变化时自动清除 FitToFrame；OnImageFolderTreeSelected() 枚举文件夹内图片文件并加载第一张。
- MainWindow.xaml 看图模块布局：ColumnDefinitions 从 2 列改为 3 列（175px / * / 220px），Column 0 加 TreeView（懒加载、SelectedItemChanged 绑事件处理器），主预览卡移至 Column 1，信息面板移至 Column 2；工具栏加「满框」按钮，激活时文字变蓝加粗；Image.Style DataTrigger 在 ImageViewerFitToFrame = True 时切换 Stretch = Uniform。
- MainWindow.xaml.cs 加 ImageFolderTree_SelectedItemChanged 事件处理器，桥接 TreeView 选中到 VM。
- 工具栏移除向左/向右旋转按钮，保留单个顺时针旋转按钮（RotateImageViewerRightCommand）。
- 验证：Release 构建 0 error 0 warning。

## [2026-05-18] 启动失败修复 - PackIconKind 兼容
- 修复 Windows 调整卡片中 `PackIcon Kind="WindowsClassic"` 导致的 `XamlParseException`：当前 MaterialDesignThemes 4.9.0 不包含该枚举值，替换为已验证存在的 `CogOutline`。
- 将新增按钮中的可疑图标 `Restart`、`FitToPage` 替换为项目中已有且兼容的 `Refresh`、`ImageSearchOutline`，降低旧版图标枚举不兼容风险。
- 验证：独立输出目录 `artifacts\verify-startup-fix\` Release 构建 0 error 0 warning；常规 `bin\Release\net48\MyTools.exe` 当前被正在运行的进程 PID 16436 锁定，未能覆盖。

## [2026-05-18] 排班机制完善 - 自动生成与导出闸门
- ScheduleVersion 新增 `GeneratedAt`，自动设置休息日成功后写入；Excel 导出增加 `HasGenerated` 闸门，未完成自动生成时拒绝导出.
- 自动优化增加节假日年份配置校验、剩余名额 0.5 步进校验、5 连班强制休人数超过当日可自动安排休息名额拒绝；优化过程支持显式保留格集合，且保留格不会被清空、均衡交换或连续上班修复覆盖。
- 冲突侧栏和保存硬约束调整为每日 `实休` 必须落在 `[总休-0.5, 总休]` 区间内；例如总休为 8 时，实休 7.5 和 8 均为正常。
- 排班表和 Excel 导出改为 `实休`/`总休` 两行语义，仅当实休不在 `[总休-0.5, 总休]` 区间内时标红；Excel 导出跳过空姓名占位行。
- 保存前增加基础校验：年份、月份、每日总休数组长度、每日总休 0.5 步进与人员单元格长度。
- 验证：常规 Release 构建编译通过但最终 `bin\Release\net48\MyTools.exe` 被运行中进程 PID 18116 锁定未覆盖；独立输出目录 `src\MyTools\bin\ReleaseVerify\` Release 构建 0 error。

## [2026-05-18] 启动与运行性能优化
- App 启动阶段延后 `MyTools.startup.log` 追加写入和旧系统 Warning 到主窗口显示后的 `ApplicationIdle`，减少首屏前磁盘 IO。
- MainWindow 托盘图标提取延后到 `Loaded + ApplicationIdle`，避免构造函数同步读取 exe 图标资源。
- MainViewModel 启动期历史、截图设置、Codex 配置、优化报告、微信根目录与最近备份加载统一通过 `ScheduleStartupBackgroundLoads()` 在 Idle 派发。
- `ScheduleViewModel` 与 `SystemSettingsViewModel` 改为懒加载属性，避免 Home 首屏直接构造重型子 ViewModel。
- Serilog File Sink 改为缓冲写入，`flushToDiskInterval` 为 2 秒，降低运行期同步刷盘压力。
- 新增 `App.config`，保留 `gcServer` 与 `gcConcurrent`；`MyTools.csproj` 显式声明 `LangVersion` 和 `ServerGarbageCollection`。
- `AGENTS.md` 新增「九、性能与启动规则」，沉淀启动期、ViewModel、日志 IO、面板可见性、GC 与基线维护约束。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 3 个 NU1900 警告。性能基线记录见 `docs\规划\性能基线_启动后.txt`，本次未做 GUI 自动采样。

## [2026-05-18] UI 面板可见性性能复查
- 复查 `MainWindow.xaml` 顶层模块面板的默认可见性，确认非 Home 面板默认 `Collapsed`，并通过 `CurrentModule` 触发显示。
- 修复 WireGuard 面板缺少 `CurrentModule=WireGuard` 显示触发器的问题，避免从首页命令切换 WireGuard 后内容区域仍保持折叠。
- 统计模块面板长度：仅 VideoViewer 面板超过 800 行；由于其 `MediaElement`、波形交互和播放列表拖拽均与 `MainWindow.xaml.cs` 强耦合，本轮不做高风险拆分，后续应单独规划 `VideoViewerPage` 重构。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 3 个 NU1900 警告。

## [2026-05-18] VideoViewer 面板拆分
- 将 `MainWindow.xaml` 中的 VideoViewer 面板抽出为 `Views/VideoViewerPage.xaml`，主窗口仅保留页面引用和 `CurrentModule=VideoViewer` 可见性触发器。
- 新增 `Views/VideoViewerPage.xaml.cs`，迁移播放器事件、波形拖拽、播放列表拖拽、A/B 循环区间和书签跳转逻辑。
- MainWindow 保留窗口级快捷键、拖放和关闭入口，通过 `VideoViewerPage` 公开方法调用播放页逻辑，降低主窗口 XAML 与代码后端体量。
- 验证：Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 3 个 NU1900 警告。

## [2026-05-18] 性能优化最终验证
- `VideoViewerPage.xaml` 补充页面级转换器资源，避免拆分为独立 UserControl 后运行期无法解析原 `MainWindow.Resources` 中的静态资源。
- Release 构建 0 error；因当前环境无法访问 `https://api.nuget.org/v3/index.json`，NuGet 漏洞审计出现 3 个 NU1900 警告。
- 启动烟测通过：Release 产物启动后进程响应，窗口标题为“阿君的工具”，启动日志成功追加当前启动记录，烟测进程已停止。

## [2026-05-18] 修复图片查看器满框显示
- 修复图片查看器“满框”按钮无效的问题：原实现仅切换 `Image.Stretch=Uniform`，但图片位于 `ScrollViewer` 内会被无限空间测量，未绑定目标视口尺寸，导致视觉上不缩放。
- 调整 `MainWindow.xaml` 图片预览区：普通模式保留原实际大小与手动缩放图片；满框模式切换为独立预览图片，并将宽高绑定到 `ImageViewerScrollViewer.ActualWidth/ActualHeight`，使用 `Stretch=Uniform` 适配预览区域。
- 验证：Release 构建通过；仍有 NU1900 网络警告，原因是当前环境无法访问 `https://api.nuget.org/v3/index.json`。

## [2026-05-18] 优化图片轻编辑滑块拖动体验
- 加宽图片查看器右侧信息/轻编辑栏：列宽由 220 调整为 260，增加亮度、对比度、锐化滑块的横向可拖动区域。
- 将亮度、对比度、锐化滑块的绑定提交时机由 `PropertyChanged` 调整为释放鼠标、键盘调整或失焦时提交，避免拖动过程中每个微小变化都同步重算预览图导致卡顿。
- 为三个滑块设置 `Height=28`，扩大鼠标命中区域，降低拖动难度。
- 验证：Release 构建通过；仍有 NU1900 网络警告，原因是当前环境无法访问 `https://api.nuget.org/v3/index.json`。
## [2026-05-20] 区域截图矩形选择修复
- 修复“划区域”截图在矩形选区靠近屏幕边缘、跨屏或缩放环境下可能生成越界裁剪矩形的问题。
- RegionSelectorWindow 改为按窗口实际显示尺寸与截图像素尺寸的比例换算选区坐标，并对左上角与右下角统一做边界钳制。
- 修复选区高亮预览层错位：SelectionImage 改为全屏对齐背景图，仅用 Clip 显示蓝框内区域。
- ShowRegionSelectorAsync 在创建 CroppedBitmap 前再次校验并钳制 Int32Rect，避免运行期因越界矩形导致截图失败。
- 验证：独立输出目录 `artifacts\verify-region-screenshot\` Release 构建 0 error 0 warning；常规 `bin\Release\net48\MyTools.exe` 被运行中进程 PID 17292 锁定，未覆盖。

## [2026-05-20] 功能页面免整页滚动布局
- 系统优化、截图工具、系统信息、文件验证、格式转换、性能测试改为页签分组或固定行布局，避免为了操作功能按钮滚动整页。
- 图片查看器右侧信息栏拆为“信息 / 轻编辑”页签，轻编辑滑块和重置按钮可直接操作。
- 系统设置页拆为“备份恢复 / 桌面背景 / Windows 调整”页签，保留列表、表格、树、图片预览等内容区域的内部滚动。
- 验证：独立输出目录 `artifacts\verify-layout\` Release 构建 0 error 0 warning。

## [2026-05-20] 截图工具页签崩溃修复
- 修复截图历史项模板中 `Run.Text` 绑定只读属性 `TimeText` / `SizeText` 时默认双向绑定导致的 `XamlParseException`。
- 将截图历史时间和大小的 `Run.Text` 绑定显式设置为 `Mode=OneWay`，切换到“截图工具”页面时不再尝试写回只读属性。
- 同步将主窗口内其它展示型 `Run.Text` 绑定显式设置为 `Mode=OneWay`，避免系统信息等页面的只读展示属性触发同类异常。
- 验证：独立输出目录 `artifacts\verify-screenshot-tab\` Release 构建 0 error 0 warning。

## [2026-05-21] 音视频播放 - 纯视频、拉伸与全屏
- 音视频播放页顶部新增“纯视频”“拉伸”“全屏”按钮。
- “纯视频”模式隐藏标题栏、控制栏和右侧信息栏，仅保留视频框；按 Esc 可恢复原布局。
- “拉伸”模式在 `Uniform` 与 `Fill` 之间切换，支持把视频画面铺满当前视频框。
- “全屏”使用黑底无边框窗口播放当前媒体，保留当前播放位置、音量、静音和速度；Esc 或双击退出并同步回主播放器位置。
- 视频框支持双击进入全屏。
- 验证：独立输出目录 `src\MyTools\bin\ReleaseVerify\` Release 构建 0 error。

## [2026-05-21] 修复音视频全屏空引用
- 修复点击“全屏”后黑屏并弹出 `NullReferenceException` 的问题：全屏播放器 `MediaOpened` 回调改为使用本地播放器引用，并在全屏窗口已关闭或播放器已切换时直接返回，避免生命周期竞争导致空引用。
- 验证：正式目录 `src\MyTools\bin\Release\net48\` Release 构建 0 error。

## [2026-05-21] 音视频全屏与快捷键体验修复
- 全屏播放改为复用当前页面的 `MediaElement` 并临时移动到全屏黑底窗口，避免创建第二个播放器导致画面黑屏。
- 退出全屏时将同一个播放器移回原视频框，保留当前位置与播放状态。
- 纯视频模式不再隐藏进度条，仅隐藏波形、播放按钮、音量速度、状态文字、A/B 操作和右侧信息栏，确保仍可拖动进度。
- 视频播放页和全屏窗口的左右方向键调整为后退/前进 7 秒。
- 验证：正式目录 `src\MyTools\bin\Release\net48\` Release 构建 0 error。

## [2026-05-22] 多媒体模块阶段性整合
- 新增多媒体模块骨架，提供左侧懒加载文件夹树、左侧预览区和右侧虚拟化媒体文件列表。
- 新增 `MediaFileTypeHelper`，统一识别图片、音频和视频文件，音视频扩展名复用共享的 `MediaFileAssociationCore`。
- 新增 `MultimediaViewModel` 与 `MultimediaPage`，支持单击预览、双击沉浸模式、Esc 返回列表和退出多媒体。
- 新增 `MediaConvertDialog`，支持在媒体文件右键菜单中启动格式转换参数设置。
- `MainViewModel` 新增 `ShowMultimediaCommand` 与懒加载 `Multimedia` 属性，并将旧图片查看、音视频播放、格式转换命令重定向到多媒体模块。
- 多媒体转换复用既有 `MediaConvertService`、FFmpeg 检测、转换队列和输出结果状态，不新增云端依赖。
- Release 构建通过，产物位置为 `src\MyTools\bin\Release\net48\MyTools.exe`。

## [2026-05-22] 多媒体旧入口可见性修正
- 移除侧边栏中“图片查看”“音视频播放”“格式转换”三个旧导航按钮，仅保留“多媒体”作为统一可见入口。
- 移除首页快捷入口中的“图片查看与编辑”“音视频播放”“格式转换”三条旧入口。
- 保留旧命令对象并继续重定向到“多媒体”，用于文件关联、拖拽和历史入口兼容。
- Release 构建通过，产物位置为 `src\MyTools\bin\Release\net48\MyTools.exe`。

## [2026-05-22] 截图工具快捷键区域布局修复
- 修复截图工具页快捷键设置区域控件堆叠的问题，原因是截图快捷键行的 `Grid` 闭合位置错误，导致录像快捷键、录音快捷键和保存按钮被叠到同一个网格单元格。
- 将截图快捷键行的 `Grid` 在按钮区域后立即闭合，使后续控件回到父级纵向布局。
- 独立输出目录 `src\MyTools\bin\ReleaseVerify\net48\` Release 构建通过；正式 Release 产物因当前运行中的 `MyTools.exe` 锁定，需要关闭程序后重新构建覆盖。

## [2026-05-22] 系统模块入口聚合
- 将“当前网络”“启动管理”“程序卸载”“系统信息”“性能测试”“系统设置”合并到左侧统一“系统”入口。
- 系统页顶部新增分区标签，支持在“系统优化 / 当前网络 / 启动管理 / 程序卸载 / 系统信息 / 性能测试 / 系统设置”之间切换。
- 旧命令对象继续保留并重定向到“系统”页对应分区，避免首页搜索、历史入口和内部调用失效。
- 首页快捷入口合并为“系统”，不再显示多个系统子模块独立卡片。
- 独立输出目录 `src\MyTools\bin\ReleaseVerify\net48\` Release 构建通过。

## [2026-05-22] Windows 调整修复与右键完整菜单
- 修复“Windows 调整”页在 Win11 上可能被误判为非 Win11 的问题，版本判断改用 `OsVersionService` 的真实系统版本。
- “时钟显示秒”启用条件改为 Windows 11 22H2 及以上，避免不支持的系统显示可操作。
- 优化资源管理器重启流程，先尝试关闭窗口，超时后再结束进程，并使用 ShellExecute 重启 explorer。
- 桌面图标切换后增加 Shell 设置广播，提升即时刷新可靠性。
- 新增“右键直接显示完整菜单（取消‘显示更多选项’）”开关，可写入或移除 Win11 经典右键菜单注册表项。
- Release 构建通过，产物位置为 `src\MyTools\bin\Release\net48\MyTools.exe`。

## [2026-05-22] 新增 frp 隧道穿透模块
- 新增 `docs/功能说明.md`：记录 frp 隧道穿透模块的目标、核心步骤、涉及文件、安全边界与限制。
- 新增 `Services/FrpService.cs`：负责 `frpc.exe` 嵌入资源解压、INI 配置生成、DPAPI Token 加解密、配置/规则异步持久化、frpc 进程启动与停止。
- 新增 `ViewModels/FrpViewModel.cs`：负责服务器配置、端口规则列表、启动/停止命令、连接状态与公网访问地址预览。
- 新增 `Views/FrpView.xaml(.cs)`：新增「穿透」页面，支持服务器地址、端口、Token、端口映射规则和一键启停。
- 修改 `MainWindow.xaml`：新增左侧「穿透」导航按钮和 `views:FrpView` 内容面板，默认 `Collapsed`。
- 修改 `MainViewModel.cs`：新增 `Frp` 懒加载属性、`ShowFrpCommand`、启动空闲期配置加载、退出时释放 frpc 进程。
- 修改 `MyTools.csproj`：将 `NativeBinaries\frpc.exe` 从 `None` 项移除并作为 `EmbeddedResource` 嵌入，保持单 exe 分发。
- 新增嵌入资源 `NativeBinaries\frpc.exe`（frp v0.50.0 windows_amd64，13,486,592 bytes），用于 frp 客户端内网穿透；单 exe 体积预计增加约 12.9 MB。

## [2026-05-22] 完善菜单式界面改造规划
- 重写 `docs/规划/菜单式界面改造任务说明书.md`，按当前 `MainWindow.xaml`、`MainWindow.xaml.cs`、`MainViewModel.cs` 的真实结构校准行号、命令和模块关系。
- 将菜单退出方案改为复用现有 `ExitCommand`，避免绕过托盘隐藏与资源释放流程。
- 明确 `SystemSettings` 是 `System` 模块下的 `CurrentSystemSection`，不是独立 `CurrentModule`。
- 新增顶部菜单分组建议、状态栏 `CurrentNavigationText` 方案、资源样式要求和构建验收步骤。
- 将启动性能优化改为后续扩展任务，菜单改造本身只要求不新增启动期同步负载。

## [2026-05-22] 主窗口菜单式导航
- 将主窗口从左侧 176 px 导航栏改为顶部菜单栏 + 主操作区 + 底部状态栏。
- 顶部菜单新增“文件、系统、工具、多媒体、日程、设置、帮助”七组，复用现有 `Show*Command`、`OpenLogFolderCommand`、`ToggleAutoStartCommand` 和 `ExitCommand`。
- 退出菜单复用 `ExitCommand`，保持托盘退出、`App.IsExiting` 和 ViewModel 释放流程一致。
- 新增 `CurrentNavigationText`，用于状态栏显示用户可读当前位置。
- 删除 `MainWindow.xaml` 中左侧导航 `Border` 与 `NavItemStyle`，保留 `Theme.xaml` 中历史侧边栏资源。
- 新增 `TopMenuBar`、`TopMenuItem`、`BottomStatusBar`、`StatusBarText` 等菜单和状态栏样式资源。
- 构建验证：Debug 与 Release 均 0 error，仅有 NU1900；Release 启动可取得主窗口句柄，标题为“阿君的工具”。
- 修正 `TopMenuItem` 不再继承 `MaterialDesignMenuItem`，避免顶层菜单在普通 `Menu` 中文字不可见；Release 已重新编译。

## [2026-05-22] 启动性能优化
- 定位启动慢主因：`MainWindow.InitializeComponent()` 曾达到 14,735 ms，主要来自主窗口 XAML 一次性创建托盘图标、首页和全部非首页模块控件。
- 新增 `Views/HomeView.xaml(.cs)`，首页改为窗口显示后延迟加载。
- 新增 `Views/MainModulesView.xaml(.cs)`，系统、SQL、截图、图片、视频、文件校验、转换、性能测试等非首页模块改为首次进入时加载。
- `Hardcodet.NotifyIcon.Wpf` 托盘图标从 XAML 移到窗口显示后延迟创建，避免阻塞 `InitializeComponent()`。
- `MainViewModel` 构造函数不再同步读取开机自启注册表状态，改到启动空闲阶段读取。
- 新增 `docs/规划/启动性能基线-2026-05-22.md`：记录本机 Release 基线，`InitializeComponent` 从 14,735 ms 降至约 2,665-4,866 ms，`Show()` 返回约 501-565 ms。
- Release 构建通过，产物位置为 `src\MyTools\bin\Release\net48\MyTools.exe`。

## [2026-05-22] 移除 WireGuard 模块
- 删除顶部菜单中的 WireGuard 入口，并移除 `MainModulesView.xaml` 中的 WireGuard 页面模板。
- 删除 `MainViewModel` 中的 `Wg*` 状态、`ShowWireGuardCommand`、连接/导入/隧道管理命令和相关方法。
- 删除专属服务 `Services\WireGuardService.cs` 与 `WireGuardTunnelItem` 模型。
- 删除 `NativeBinaries\README.txt` 中仅面向 WireGuard 绿色集成的说明。
- 系统备份/恢复范围移除 WireGuard 配置分区，保留设置与历史、排班本地数据、Codex、FFmpeg。
- Release 构建通过，产物位置为 `src\MyTools\bin\Release\net48\MyTools.exe`。

## [2026-05-22] 启动速度二次优化
- 将 `App.xaml` 首启资源缩减为 `Resources\ShellTheme.xaml`，把 MahApps、MaterialDesign、完整主题与插画移动到 `Resources\DeferredAppResources.xaml` 按需加载。
- 将主窗口首屏改为轻量 WPF shell，保留顶部菜单与常用入口，避免完整 HomeView 和重模块参与首帧渲染。
- Costura 改为 `LoadAtModuleInit=false`，并在托盘、日志、重 UI 模块需要第三方依赖时再初始化。
- 移除当前未使用的 SQLite / Dapper / FluentWPF 预留依赖，Release 单 exe 体积约从 27.9 MB 降至 21.2 MB。
- 托盘图标改为读取嵌入 `Resources\AppIcon.ico`，不再从单 exe 提取图标；托盘初始化延后到启动后。
- Serilog 初始化移出首帧路径，启动指标写入 `MyTools.startup.log`。
- Release 编译通过，输出 `src\MyTools\bin\Release\net48\MyTools.exe`。
## [2026-05-22] 启动速度三次优化
- 启动期后台预读改为按模块懒加载，避免首页出现后继续读取 SQL、截图、视频列表、Codex、FRP、系统清理和微信数据。
- `MainViewModel` 重型服务实例与硬件传感器计时器改为首次使用时创建。
- `frpc.exe` 改为 gzip 压缩嵌入资源 `frpc.exe.gz`，保持单 exe 分发并将主程序体积从 21.2 MB 降至 12.9 MB。
- Release 构建通过；窗口句柄测量为首次 2,429 ms，热启动 396 ms / 384 ms。

## [2026-05-22] 系统优化新增常亮电源策略
- 新增 `PowerPolicyService`，通过提权 `powercfg` 将当前电源计划设置为不自动关闭屏幕、不自动关闭硬盘、不睡眠/休眠，并关闭 hibernate。
- `MainViewModel` 新增 `ApplyAlwaysOnPowerPolicyCommand`、`IsPowerPolicyBusy` 和 `PowerPolicyStatusMessage`。
- 系统优化的“系统锁定”页新增“禁止自动关屏、关硬盘和休眠”卡片。
- Release 构建通过；本次仅新增轻量命令对象，启动句柄测量约 2,467 ms。

## [2026-05-22] 修复设置菜单延迟资源加载异常
- 修复点击顶部“设置(P)”时因重 UI 资源尚未加载导致的 XAML 资源缺失异常。
- 调整 `DeferredUiResourceService`，在进入非首页模块前按顺序合并 MahApps、MaterialDesign 与本项目主题资源，避免 `Theme.xaml` 查找不到 `MaterialDesignRaisedButton`、`MaterialDesignTabControl` 等样式。
- `MainViewModel.SwitchModule` 在切换非首页模块前先确保延迟 UI 资源加载完成，避免 `CurrentModule` 触发视图显示后再补资源。
- Release 构建通过；UI Automation 已点击“设置(P)”并确认进入“系统 / 系统设置”，未出现运行错误对话框。
