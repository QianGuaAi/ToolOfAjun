# 启动基线说明 - Codex 429 自动轮换

## 变更范围

- `MainViewModel` 构造函数新增一个 `DispatcherTimer` 对象和 `Tick` 事件订阅，用于 Codex 配置页加载成功后再启动 429 自动轮换监控。
- 构造函数中未读取 `%USERPROFILE%\.codex\logs_2.sqlite`，未解密 Codex 档案，未访问注册表、WMI 或网络。
- SQLite 日志读取由 `LoadCodexProfilesAsync()` 成功后触发，先读取 `MAX(id)` 作为基线，后续每 20 秒只读增量最多 50 行。

## 基线说明

- 本次已执行 `dotnet build src\MyTools\MyTools.csproj -c Release`，构建通过，0 warning / 0 error。
- 未单独测量首帧时间：当前机器已有安装版 `C:\Program Files\AjunTools\MyTools\MyTools.exe` 在运行，本次不强制关闭用户正在使用的窗口做冷启动测量。
- 风险判断：新增构造期工作仅为托管对象创建和事件绑定，不包含磁盘 IO；符合启动期黄金 500 ms 原则。
