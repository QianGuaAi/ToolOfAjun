# Codex 本地中转连接失败修复诊断记录 2026-06-10

## 1. 本地中转设置

- 设置文件：`C:\Users\QianGua\AppData\Local\MyTools\Codex\LocalRelay\local-relay.json`
- `Enabled`：`true`
- `Port`：`48176`
- `LocalBaseUrl`：`http://127.0.0.1:48176/v1`
- `UpstreamBaseUrl`：`https://mdkj.lol`
- `WireApi`：`responses`

## 2. relay 存活与健康检查

- `Test-NetConnection 127.0.0.1 -Port 48176`：`TcpTestSucceeded=True`
- `Invoke-WebRequest http://127.0.0.1:48176/health`：HTTP `200`
- `/health` 正文：`MyTools Codex local relay is running.`
- 监听进程：`C:\Program Files\AjunTools\MyTools\MyTools.exe`

判定：relay 当前存活，分支 A 当前未命中。

## 3. MyTools.log 证据

未找到可读取的 `MyTools.log`。

已检查路径：

- `C:\qgt\src\MyTools\bin\Release\MyTools.log`
- `C:\qgt\src\MyTools\bin\Release\net48\MyTools.log`
- `C:\Program Files\AjunTools\MyTools\MyTools.log`
- `C:\Users\QianGua\AppData\Local\MyTools`
- `C:\qgt`
- `C:\Users\QianGua\AppData\Roaming\MyTools`
- `C:\Program Files (x86)\AjunTools\MyTools`

按可用日志文件统计：

- `Codex local relay upstream returned HTTP`：`0`
- `Codex local relay request failed`：`0`
- `Unauthorized` 或 `401`：`0`

## 4. Codex config.toml 核对

配置文件：`C:\Users\QianGua\.codex\config.toml`

- 根级 `model_provider == "mytools_local_relay"`：通过
- `[model_providers.mytools_local_relay].base_url == LocalBaseUrl`：通过
- `[model_providers.mytools_local_relay].wire_api == WireApi`：通过
- `[model_providers.mytools_local_relay.auth]` 小节存在：通过

判定：当前配置四项核对通过，分支 C 当前未按现场条件命中。

## 5. token helper 核对

- 脚本：`C:\Users\QianGua\AppData\Local\MyTools\Codex\LocalRelay\ReadCodexLocalRelayToken.ps1`
- token 文件：`C:\Users\QianGua\AppData\Local\MyTools\Codex\LocalRelay\local-token.dpapi`
- 退出码：`0`
- 耗时：约 `173-189 ms`
- stdout 非空：是
- stderr：空

说明：未记录 token 明文。

判定：token helper 当前可在 3 秒内输出非空结果，分支 B 当前未命中。

## 6. Codex 日志证据

检查路径：

- `C:\Users\QianGua\.codex\log`：不存在
- `C:\Users\QianGua\.codex\sessions`：存在
- `C:\Users\QianGua\.codex\logs_2.sqlite`：存在

`logs_2.sqlite` 中未找到明确的 `retry / stream disconnected / connection` 错误级记录。当前时间段可见 Codex Desktop 实际请求本地 relay：

- `POST http://127.0.0.1:48176/v1/responses`
- `Request completed ... status=200 OK`
- 响应头含 `content-type: text/event-stream` 与 `connection: close`

直接用本地 relay token 对 `http://127.0.0.1:48176/v1/responses` 发送 `stream=true` 最小请求：

- HTTP 状态：`200 OK`
- 首包耗时：约 `2625 ms`
- `Content-Type`：`text/event-stream`
- 首次读取：`2048` bytes
- SSE 事件：包含 `response.created`

判定：当前可通过 relay 收到流式响应，分支 D 与分支 E 当前未复现。

## 7. 分支判定

当前机器未复现用户描述的“Codex 重试 5 次连接失败”。按 A → B → C → D → E 顺序检查：

- 分支 A：未命中，relay 端口和 `/health` 正常。
- 分支 B：未命中，token helper 当前 3 秒内非空输出，Codex 当前请求返回 `200 OK`。
- 分支 C：当前现场未命中，但代码存在固定配置误判缺口：旧 `IsCodexConfigPinnedToLocalRelay` 只检查根级 provider、本地 base_url 和 `disable_response_storage=false`，未核对 `wire_api`、`[model_providers.mytools_local_relay.auth]`、`command`、`args`、`timeout_ms`、`refresh_interval_ms`。旧坏配置或半写入配置可能被误判为“已固定”，导致“重启 Codex 使用中转”不再备份和重写配置。
- 分支 D：未命中，未见上游 HTTP 4xx/5xx 证据，直接 relay 探测为 `200 OK`。
- 分支 E：当前未命中，Codex 日志显示真实 Codex 请求 `200 OK`，直接 relay SSE 探测也能读到事件。

本次修复选择：按分支 C 的自愈缺口实施最小修复，收紧 `IsCodexConfigPinnedToLocalRelay`，让旧坏/半坏配置触发备份并重写为完整本地中转配置。

## 8. 修改与验证计划

- 修改文件：`src\MyTools\Services\CodexLocalRelayService.cs`
- 修复点：固定配置判定必须同时核对 provider、`base_url`、`wire_api`、auth command、auth args、`timeout_ms`、`refresh_interval_ms`。
- 不修改 `App.OnStartup`、`MainWindow` 构造函数或 `MainViewModel` 构造函数。
- 不新增 NuGet 包。
- 不写入 token、上游 key、请求正文或响应正文。

## 9. 构建结果

执行命令：

```powershell
.\.dotnet\dotnet.exe build src\MyTools\MyTools.csproj -c Release
```

结果摘录：

```text
MyTools -> C:\qgt\src\MyTools\bin\Release\net48\MyTools.exe
已成功生成。
    0 个警告
    0 个错误
```

## 10. 验证结果

当前运行中的 Codex Desktop 已使用本地 relay，`logs_2.sqlite` 记录到 3 次真实流式请求成功：

- `2026-06-10 19:54:18`：`POST http://127.0.0.1:48176/v1/responses`，`200 OK`
- `2026-06-10 19:54:37`：`POST http://127.0.0.1:48176/v1/responses`，`200 OK`
- `2026-06-10 19:54:45`：`POST http://127.0.0.1:48176/v1/responses`，`200 OK`

补充验证：

- 直接对 `http://127.0.0.1:48176/v1/responses` 发送本地授权的 `stream=true` 最小请求，HTTP `200 OK`，响应为 `text/event-stream`，首包包含 SSE `response.created`。
- 文档与日志脱敏扫描未发现 token、上游 key 或 Bearer 明文。
- Release 输出目录未出现散落托管 DLL 或 `SQLite.Interop.dll`。
