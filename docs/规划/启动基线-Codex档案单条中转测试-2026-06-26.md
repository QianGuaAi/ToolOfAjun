# 启动基线说明 - Codex 档案单条中转测试

日期：2026-06-26

## 变更范围

- `MainViewModel` 构造函数新增 `TestCodexProfileRelayCommand` 命令对象创建与公开属性绑定。
- `MainModulesView.xaml` 的 Codex 档案卡片新增“测试”按钮。
- 未在 `App.OnStartup`、`MainWindow` 构造函数或 `ScheduleStartupBackgroundLoads()` 中新增同步 IO、网络请求、WMI、注册表查询或后台批量任务。

## 启动影响判断

- 构造期新增内容仅为一个 `AsyncRelayParameterCommand` 对象和属性引用，属于启动期允许的命令对象创建。
- 单条中转测试的档案解密、网络探测和档案库保存均只在用户点击档案行“测试”后执行，不在启动期执行。
- Codex 面板 XAML 仍由 `CodexProfilesDeferredTemplate` 按 `CurrentModule=CodexProfiles` 延迟挂载，不增加 Home 首屏模块渲染负担。

## 验证与未实测原因

- 已执行 `powershell -ExecutionPolicy Bypass -File scripts\codex-eval.ps1 -Quick`，包含启动响应守卫与 Release build，结果通过。
- 本次为窄范围行级 UI/命令变更，未启动桌面程序采集首帧计时；若后续同时调整 `App.OnStartup`、`MainWindow` 构造函数或 `MainViewModel` 启动加载调度，应重新采集首帧、稳态私有工作集和单 exe 体积。
