# Native 重写交接 - 2026-06-30

## 当前总状态

- 工作目录：`C:\ToolOfAjun`
- 分支：`main...origin/main`
- 当前没有未关闭的 sub-agent。
- Native 重写还没有完成到可发布状态；当前处于 Codex Profiles 阶段 3 显式动作迁移中。
- 本机缺少 CMake/MSVC Build Tools，`scripts\native-eval.ps1 -Build` 不能作为已完成验证；目前主要依赖 `-Quick` 静态守卫和 WPF Release build。

## 已完成且 reviewer 通过

### 1. active profile 改名同步 active.json

已完成内容：
- `Rename first profile...` 不再阻断当前 active profile 改名。
- `CodexProfileModule` 会把 `active_json_path` 和 `active_display_name` 传给 `CodexProfileEditService`。
- `CodexProfileEditService` 先写回 DPAPI 保护的 `profiles.json`，如果目标是当前 active profile，再同步更新 `active.json` 的 `ActiveDisplayName`。
- 如果 `profiles.json` 已写回但 `active.json` 同步失败，会返回局部失败提示，并让 UI 刷新摘要。
- 主窗口仍不直接写 `active.json`，也不直接调用 `CodexProfileEditService`。

验证：
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\native-eval.ps1 -Quick` 通过。
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\codex-eval.ps1 -Quick` 通过。
- 专用 `mytools-reviewer` 复查通过，无阻断问题。

剩余风险：
- 未做真实菜单点击、弹窗输入和写盘 smoke。
- 未跑 Native CMake build，原因是本机缺 CMake/MSVC。

## 刚接入但尚未最终 reviewer 的内容

### .codexbox 导入冲突策略选择

当前已改代码：
- `src\MyTools.Native\ui\modal_dialogs.h/.cpp`
  - 新增 `ChooseCodexBoxConflictPolicy()`。
  - 新增自定义三按钮弹框 `.codexbox import conflicts`。
  - 按钮语义：`Rename`、`Skip`、`Replace`；关闭或 Cancel 代表取消整个导入，不等于 Skip。
- `src\MyTools.Native\ui\main_window.h/.cpp`
  - `Import .codexbox...` 流程变为：确认 -> 选择 `.codexbox` 文件 -> 选择冲突策略 -> 输入密码 -> 进入 module action。
  - 删除了 `.codexbox conflict choice dialog pending` 灰置菜单项。
  - 导入确认文案已改为提示会先选择冲突处理方式。
- `src\MyTools.Native\modules\codex_profiles\codex_profile_module.h/.cpp`
  - `CodexProfileActionOptions` 新增 `box_conflict_policy`，默认 `Rename`。
  - `ImportBox` 分支改为 `request.conflict_policy = options.box_conflict_policy`，不再硬编码 `CodexProfileBoxConflictPolicy::Rename`。
  - 状态页 bullet 改为导入时会询问同名冲突处理方式。
- `scripts\native-eval.ps1` 和 `scripts\codex-eval.ps1`
  - 新增 `.codexbox` 冲突策略守卫。
  - 要求 UI 收集策略、module 传 options、service 保留 Skip/Replace/Rename 统计。
  - 禁止旧 pending 灰项和 `request.conflict_policy = CodexProfileBoxConflictPolicy::Rename;` 回归。

已跑验证：
- 在代码与脚本接入后，`native-eval -Quick` 通过。
- 在代码与脚本接入后，`codex-eval -Quick` 通过。

交接时还没完全收口的事：
- `AGENTS.md`、`docs\功能说明.md`、`docs\程序逻辑.md`、`docs\场景驱动开发\控件交互逻辑说明.md` 已开始同步 `.codexbox` 冲突策略事实。
- 仍建议回家后先检查并补齐：
  - `src\MyTools.Native\README.md`：移除 Not implemented 里的 `.codexbox` conflict dialog，改为已接入策略选择。
  - `docs\开发记录.txt`：追加 “Native Codex .codexbox 导入冲突策略选择接入” 条目。
  - 搜索残留 `conflict choice dialog pending`、`自动重命名`、`auto-rename`、`renamed automatically`、`冲突选择弹框仍待迁移`，确认只保留历史说明且不误导当前事实。
- 补齐后重跑 `native-eval -Quick`、`codex-eval -Quick`、`git diff --check`。
- 然后再开专用 `mytools-reviewer` 只读审查 `.codexbox` 冲突策略切片。

## 需要特别注意的工作树状态

- `src\MyTools.Native\` 是未跟踪目录，普通 `git diff` 不会显示其中内容。
- `scripts\native-eval.ps1` 也是未跟踪文件，提交或交付时不要漏掉。
- `.codex\config.toml` 是本机 Codex 配置，当前未跟踪，不要误提交。
- 工作树里还有很多 WPF 体积缩减相关改动，例如 SQL 导出、Codex 轮换、Ollama 导入、硬件传感器删除，`ffmpeg.exe` 删除等；不要回滚这些已有改动，后续提交前需要单独确认范围。

## 回家继续的建议顺序

1. 先补齐 `.codexbox` 冲突策略的 README 和开发记录。
2. 跑残留扫描：
   ```powershell
   Select-String -Path 'AGENTS.md','docs\功能说明.md','docs\程序逻辑.md','docs\场景驱动开发\控件交互逻辑说明.md','src\MyTools.Native\README.md','docs\开发记录.txt','src\MyTools.Native\**\*.cpp','src\MyTools.Native\**\*.h' -Pattern 'conflict choice dialog pending|自动重命名|auto-rename|renamed automatically|冲突选择弹框仍待迁移'
   ```
3. 跑验证：
   ```powershell
   powershell -NoProfile -ExecutionPolicy Bypass -File scripts\native-eval.ps1 -Quick
   powershell -NoProfile -ExecutionPolicy Bypass -File scripts\codex-eval.ps1 -Quick
   git diff --check
   ```
4. 开专用 `mytools-reviewer` 只读审查 `.codexbox` 冲突策略切片。
5. 如果 reviewer 过，再继续下一个切片；优先候选是 Codex Profiles 多档案选择，或 relay test。FRP 属于更大的模块，建议等 Codex Profiles 收干净后再切。

## 已知验证限制

- Native 编译验证命令：
  ```powershell
  powershell -NoProfile -ExecutionPolicy Bypass -File scripts\native-eval.ps1 -Build
  ```
  当前会因为缺少 CMake/MSVC Build Tools 失败。不能把 `-Quick` 通过等同于 Native 编译通过。

- 真实 UI smoke 还没做：
  - `.codexbox` 导入冲突弹框三按钮。
  - active profile 改名写 `active.json`。
  - note/remark/tags 清空、超长、控制字符和正常写回。
  - 普通文件导出和 `.codexbox` 实际导入/导出写盘。

## 不要忘记

- 不要提交 `.codex\config.toml`。
- 不要用 `git checkout --` 或 `git reset --hard` 回滚现有改动。
- 继续遵守 Loop Engineering：实现后跑验证、修复、专用 reviewer 把关、同步活文档。
