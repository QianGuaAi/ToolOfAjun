# 控件交互逻辑说明

> 文档定位：本文是场景驱动开发范式自动生成并持续维护的用户界面事实说明书。它记录用户可见页面、弹框、控件、按钮、筛选项、表格列、提示文案、禁用状态、校验规则、跳转逻辑和权限展示。
> 本文件使用新项目推荐文件名；请按项目实际页面、弹框、控件和权限展示补齐界面事实。
> 本文只回答“用户看得见、点得到、会收到什么反馈”，不记录完整业务流程或实现状态。

## 一、维护规则

1. 用户输入 `开qg` 并确认执行后，若本轮新增、删除或编辑任何用户可见控件，必须同步维护本文。
2. 控件说明必须来自源码、运行界面、截图、测试或用户确认，禁止凭空补写不存在的控件。
3. 删除控件时，必须同步删除或标注对应说明，避免留下孤儿控件说明。
4. 控件权限、禁用状态、校验规则和跳转逻辑必须和真实代码行为一致。
5. 每次只修改相关页面或控件条目；除非用户明确要求重建模板，不得整文件重写。

## 二、格式规范

所有控件说明按以下层级编号格式编写：

```text
X.业务域或端
X.X模块名
模块功能描述。
X.X.X页面或弹框名
页面或弹框功能描述。
X.X.X.X"控件名"控件类型，功能是XXX。
```

示例：

```text
1.系统管理
1.1用户管理
维护系统用户、角色和启用状态。
1.1.1用户列表页
展示用户列表并提供查询、新建、编辑和禁用入口。
1.1.1.1"关键词"输入框，功能是按用户名、姓名或角色筛选用户列表。
1.1.1.2"查询"按钮，功能是按当前筛选条件刷新用户列表。
```

## 三、控件记录字段

复杂控件、权限控件或高风险控件必须记录以下字段：

```text
控件编号：
控件名称：
控件类型：
所在页面或弹框：
可见条件：
启用条件：
点击或输入行为：
成功反馈：
失败反馈：
校验规则：
跳转逻辑：
权限边界：
关联接口或命令：
关联业务场景：
证据来源：
待确认项：
```

## 四、控件类型参考

常见控件类型包括：

1. 输入框。
2. 文本域。
3. 下拉选择。
4. 单选组。
5. 复选框。
6. 开关。
7. 日期或时间选择器。
8. 文件上传。
9. 表格。
10. 表格列。
11. 分页条。
12. 标签页。
13. 弹框。
14. 抽屉。
15. 图标按钮。
16. 文本按钮。
17. 主按钮。
18. 危险操作按钮。
19. 菜单项。
20. 跳转链接。
21. 状态标签。
22. 提示文案。
23. 空状态。
24. 加载状态。
25. 错误提示。

## 五、权限与状态说明规则

涉及权限或状态的控件必须说明：

1. 什么角色或权限可见。
2. 什么角色或权限可点击。
3. 无权限时隐藏、禁用还是展示只读提示。
4. 什么业务状态下可点击。
5. 什么业务状态下禁用。
6. 禁用时显示什么提示。
7. 后端是否仍会独立校验。

禁止只写“按权限控制”而不说明具体表现。

## 六、页面章节索引

按项目实际端和业务域拆分章节。没有对应页面时，不要创建空章节；有真实控件后再追加。

| 编号 | 端或业务域 | 模块 | 页面或弹框 | 关联场景 | 状态 |
|---|---|---|---|---|---|
| 1 | [待确认] | [待确认] | [待确认] | QG-001 | 待核对 |

## 七、控件说明正文

### 1. 待整理界面

1.1待确认模块

模块功能描述：[待确认]

1.1.1待确认页面

页面功能描述：[待确认]

1.1.1.1"待确认控件"控件类型，功能是[待确认]。

控件编号：1.1.1.1
控件名称：待确认控件
控件类型：[待确认]
所在页面或弹框：待确认页面
可见条件：[待确认]
启用条件：[待确认]
点击或输入行为：[待确认]
成功反馈：[待确认]
失败反馈：[待确认]
校验规则：[待确认]
跳转逻辑：[待确认]
权限边界：[待确认]
关联接口或命令：[待确认]
关联业务场景：QG-001
证据来源：[待确认]
待确认项：[待确认]

## 八、与其它活文档的同步

1. 本文新增或调整控件后，必须确认对应业务流程清单是否需要同步入口、步骤或异常路径。
2. 本文新增或调整控件后，必须确认实现核查台账是否需要同步证据、状态、缺口或验证结果。
3. 实现核查发现页面真实行为与本文不一致时，必须更新本文或记录代码缺口，不能让控件说明停留在旧状态。

## 九、自检清单

每次修改本文后，自检以下事项：

- [ ] 新增控件有清晰编号。
- [ ] 控件名称和界面展示一致。
- [ ] 控件类型准确。
- [ ] 可见条件和启用条件明确。
- [ ] 成功反馈和失败反馈明确。
- [ ] 校验规则和后端校验边界明确。
- [ ] 跳转逻辑明确。
- [ ] 权限展示和后端授权边界明确。
- [ ] 关联业务场景已写入。
- [ ] 没有把计划中但未落地的控件写成事实。

## 十、变更日志

- 2026-06-08 · v1 · 按通用场景驱动开发范式重建为可复用控件交互逻辑说明模板，后续由 `开qg` 场景执行实时维护。
