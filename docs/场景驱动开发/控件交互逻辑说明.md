# 控件交互逻辑说明

> 文档定位：本文是场景驱动开发范式自动生成并持续维护的用户界面事实说明书。它记录用户可见页面、弹框、控件、按钮、筛选项、表格列、提示文案、禁用状态、校验规则、跳转逻辑和权限展示。
> 本文件使用新项目推荐文件名；请按项目实际页面、弹框、控件和权限展示补齐界面事实。
> 本文只回答“用户看得见、点得到、会收到什么反馈”，不记录完整业务流程或实现状态。

## 一、维护规则

1. 用户输入 `开qg` 并确认执行后，若本轮新增、删除或编辑任何用户可见控件，必须同步维护本文。
2. 控件说明必须来自源码、运行界面、截图、测试或用户确认，禁止凭空补写不存在的控件。
3. 删除控件时，必须同步删除或标注对应说明，避免留下孤儿控件说明。
4. 控件权限、禁用状态、校验规则和跳转逻辑必须和真实代码行为一致。
5. 每次只修改相关页面或控件条目；除非用户明确要求重建模板，不得整文件重写。

## 二、格式规范

所有控件说明按以下层级编号格式编写：

```text
X.业务域或端
X.X模块名
模块功能描述。
X.X.X页面或弹框名
页面或弹框功能描述。
X.X.X.X"控件名"控件类型，功能是XXX。
```

示例：

```text
1.系统管理
1.1用户管理
维护系统用户、角色和启用状态。
1.1.1用户列表页
展示用户列表并提供查询、新建、编辑和禁用入口。
1.1.1.1"关键词"输入框，功能是按用户名、姓名或角色筛选用户列表。
1.1.1.2"查询"按钮，功能是按当前筛选条件刷新用户列表。
```

## 三、控件记录字段

复杂控件、权限控件或高风险控件必须记录以下字段：

```text
控件编号：
控件名称：
控件类型：
所在页面或弹框：
可见条件：
启用条件：
点击或输入行为：
成功反馈：
失败反馈：
校验规则：
跳转逻辑：
权限边界：
关联接口或命令：
关联业务场景：
证据来源：
待确认项：
```

## 四、控件类型参考

常见控件类型包括：

1. 输入框。
2. 文本域。
3. 下拉选择。
4. 单选组。
5. 复选框。
6. 开关。
7. 日期或时间选择器。
8. 文件上传。
9. 表格。
10. 表格列。
11. 分页条。
12. 标签页。
13. 弹框。
14. 抽屉。
15. 图标按钮。
16. 文本按钮。
17. 主按钮。
18. 危险操作按钮。
19. 菜单项。
20. 跳转链接。
21. 状态标签。
22. 提示文案。
23. 空状态。
24. 加载状态。
25. 错误提示。

## 五、权限与状态说明规则

涉及权限或状态的控件必须说明：

1. 什么角色或权限可见。
2. 什么角色或权限可点击。
3. 无权限时隐藏、禁用还是展示只读提示。
4. 什么业务状态下可点击。
5. 什么业务状态下禁用。
6. 禁用时显示什么提示。
7. 后端是否仍会独立校验。

禁止只写“按权限控制”而不说明具体表现。

## 六、页面章节索引

按项目实际端和业务域拆分章节。没有对应页面时，不要创建空章节；有真实控件后再追加。

| 编号 | 端或业务域 | 模块 | 页面或弹框 | 关联场景 | 状态 |
|---|---|---|---|---|---|
| 1 | [待确认] | [待确认] | [待确认] | QG-001 | 待核对 |
| 2 | 桌面端 | 截图工具 | 截图工具页顶部操作区 | 录音实时反馈 | 已落地 |
| 2 | 桌面端 | 截图工具 | 截图设置标签页 | 截图工具设置持久化 | 已落地 |
| 3 | 桌面端 | 日程 / 排班 | 排班工具栏 / 排班表格 | 排班 Excel 导入、排班单元格编辑 | 已落地 |
| 4 | 桌面端 | 系统信息 | 硬件概览标签页 | 按需查看硬件摘要 | 已落地 |
| 5 | 桌面端 | Codex 账号切换器 | 本地中转控制区 / 档案列表 | 本地中转接入 Cursor 与 OpenAI-compatible 客户端 | 已落地 |
| 6 | 桌面端 | 系统优化 | 系统维护卡片 / 自动更新行 | 停止或恢复 Windows 自动更新 | 已落地 |
| 7 | 桌面端 | 系统优化 | 自动优化页 / 扫描结果表 | 先扫描再勾选执行自动优化 | 已落地 |
| 8 | 桌面端 | 系统优化 | 垃圾清理页 / 扫描结果表 | 先扫描再确认清理白名单垃圾 | 已落地 |
| 9 | 桌面端 | SQL 导出工具 | 顶部菜单 / SQL 导出页 | 取消不再需要的数据库导出与查询工具 | 已移除 |

## 七、控件说明正文

### 1. 待整理界面

1.1待确认模块

模块功能描述：[待确认]

1.1.1待确认页面

页面功能描述：[待确认]

1.1.1.1"待确认控件"控件类型，功能是[待确认]。

控件编号：1.1.1.1
控件名称：待确认控件
控件类型：[待确认]
所在页面或弹框：待确认页面
可见条件：[待确认]
启用条件：[待确认]
点击或输入行为：[待确认]
成功反馈：[待确认]
失败反馈：[待确认]
校验规则：[待确认]
跳转逻辑：[待确认]
权限边界：[待确认]
关联接口或命令：[待确认]
关联业务场景：QG-001
证据来源：[待确认]
待确认项：[待确认]

### 2. 截图工具

2.1截图 / 录像 / 录音模块

模块功能描述：提供全屏截图、区域录像和独立录音入口，并在录音期间显示实时状态。

2.1.1截图工具页顶部操作区

页面功能描述：截图工具页顶部展示模块标题、录音状态条和截图/录像/录音/剪贴板编辑按钮。

2.1.1.1"录音中"状态条，控件类型为状态标签 + 波形指示器，功能是展示独立录音是否正在进行、录音时长和实时音量波形。

控件编号：2.1.1.1
控件名称：录音中状态条
控件类型：状态标签 + 波形指示器
所在页面或弹框：截图工具页顶部操作区
可见条件：`HasAudioRecordingIndicator` 为 true，即独立录音正在进行。
启用条件：只读状态控件，不可点击。
点击或输入行为：无。
成功反馈：显示红色圆点、"录音中"文案、`mm:ss` 或 `hh:mm:ss` 录音时长，以及 28 根随系统声音短时峰值滚动变化的波形柱。
失败反馈：无独立失败反馈；录音启动或停止失败由"开启录音/停止录音"按钮流程弹窗提示。
校验规则：波形仅展示 WASAPI loopback 的实时采样峰值，不写入日志，不展示或保存音频内容。
跳转逻辑：无。
权限边界：本地桌面功能，无角色权限；录音本身依赖 Windows 默认播放设备和可写输出目录。
关联接口或命令：`MainViewModel.AudioRecordingWaveformBars`、`MainViewModel.AudioRecordingDurationText`、`RecordingService.GetActiveAudioLevelSnapshot()`。
关联业务场景：录音实时反馈
证据来源：`src/MyTools/Views/MainModulesView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/RecordingService.cs`
待确认项：无。

2.1.2截图设置标签页

页面功能描述：截图工具页的"截图设置"标签提供快捷键、截图范围、区域录像输出模式、录像帧率、MP4 画质、录音格式、截图后打开编辑器和录制目录设置。

2.1.2.1"截图行为设置组"控件类型为单选组 + 开关 + 下拉选择 + 复选框，功能是维护截图与录制默认行为。

控件编号：2.1.2.1
控件名称：截图行为设置组
控件类型：单选组 + 开关 + 下拉选择 + 复选框
所在页面或弹框：截图工具页 / 截图设置标签页
可见条件：进入"工具 / 截图工具"并停留在"截图设置"标签时可见。
启用条件：本地桌面功能，无角色权限；控件常驻可编辑。
点击或输入行为：截图范围单选可切换"全屏 / 划区域 / 当前窗口"；"区域录像输出"开关可在 MP4 与 GIF 间切换；"录像帧率"下拉可选 15 FPS / 30 FPS / 60 FPS；"MP4 画质"下拉可选体积优先 / 均衡 / 高清；"录音格式"下拉可选 WAV / M4A / MP3；"截图后打开编辑器"复选框控制截图后弹出编辑器或直接写入剪贴板。
成功反馈：截图范围、编辑器开关、区域录像输出模式、录像帧率、MP4 画质和录音格式变更后异步保存到程序同目录 `MyTools.settings.json`；重启后由截图设置加载流程自动回填。点击"保存设置"成功后弹出"设置已保存"。
失败反馈：设置文件写入异常时写入应用日志，不弹出阻塞提示；保存快捷键失败时沿用"快捷键不可用"弹窗。
校验规则：截图模式只接受 `FullScreen / Region / Window`，非法值回退 `FullScreen`；录像帧率非法值回退 `30 FPS`；MP4 画质非法值回退"均衡"；录音格式非法值由 `AudioRecordingFormat.NormalizeExtension` 回退为 `wav`。
跳转逻辑：无跨页面跳转。
权限边界：设置只写入当前程序同目录 `MyTools.settings.json`，不写注册表，不访问云端；录制执行时仍依赖本机 `ffmpeg.exe` 和可写输出目录。
关联接口或命令：`IsScreenshotModeFullScreen`、`IsScreenshotModeRegion`、`IsScreenshotModeWindow`、`IsGifRecordingMode`、`RecordingFrameRateOption`、`RecordingQualityOption`、`AudioRecordingFormatOption`、`ShowEditorAfterCapture`、`SaveScreenshotSettingsCommand`、`AppSettingsService.UpdateAsync()`。
关联业务场景：截图工具设置持久化
证据来源：`src/MyTools/Views/MainModulesView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/AppSettingsService.cs`
待确认项：未在当前运行安装版执行真实重启回填人工验证；当前验证为 Release build 与 `scripts/codex-eval.ps1 -Quick` 静态/构建回归。

### 3. 日程 / 排班

3.1排班模块

模块功能描述：维护月份排班版本、人员、每日总休目标和班次单元格。

3.1.1排班工具栏

页面功能描述：排班页顶部提供新建、保存、人员导入、排班 Excel 导入导出、清空和缩放等高频操作入口。

3.1.1.1"导入排班"控件类型为文本按钮，功能是导入排班 Excel 并替换当前表格内容。

控件编号：3.1.1.1
控件名称：导入排班
控件类型：文本按钮
所在页面或弹框：排班工具栏
可见条件：排班页可见时常驻显示，位置在"复制上月"和"导出"之间。
启用条件：始终可点击；没有当前排班时，导入文件名需包含可识别的 `YYYY-MM` 年月；已有当前排班时可使用当前年月兜底。
点击或输入行为：点击后弹出 `.xlsx` 文件选择框；若当前已有排班，会先提示导入将替换当前表格内容；导入成功后将 Excel 中的年月、每日总休、人员和班次载入表格，并询问是否立即保存为本地排班版本。
成功反馈：状态栏显示导入年月、版本名、人员数量和文件名；导入成功的表格进入可保存、可导出状态；若选择立即保存，保存成功后刷新左侧版本列表并显示保存路径。
失败反馈：文件结构缺失、日期列不连续、年月无法识别、总休非 0.5 步进或班次不在白名单时弹出错误提示，并在状态栏显示失败原因。
校验规则：A1 必须为"姓名"，A2 必须为"日期"，A4 必须为"总休"；B2 起必须是连续日期；班次支持"白 / 卡 / 副 / 感 / 大 / 小 / 休 / 公 / 产 / 午"，旧"夜 / 大夜"归一化为"大"，"产假"归一化为"产"；"午"虽不在弹出菜单中显示，仍允许导入并按 0.5 天休息统计；仅当日期后统计列表头为"上班"时，才按该列把导出时显示为空白的白班还原为"白"。
跳转逻辑：无跨页面跳转；导入成功后停留在当前排班页并重建表格。
权限边界：本地桌面功能，无角色权限；保存动作写入当前 Windows 用户的 `%LOCALAPPDATA%\MyTools\Schedules`。
关联接口或命令：`ScheduleViewModel.ImportExcelCommand`、`ScheduleExcelImporter.ImportAsync()`、`ScheduleService.SaveAsync()`。
关联业务场景：排班 Excel 导入
证据来源：`src/MyTools/Views/SchedulePage.xaml`、`src/MyTools/ViewModels/ScheduleViewModel.cs`、`src/MyTools/Services/ScheduleExcelImporter.cs`
待确认项：无。

3.1.1.2"导出"控件类型为文本按钮，功能是将当前排班表格内容导出为 Excel。

控件编号：3.1.1.2
控件名称：导出
控件类型：文本按钮
所在页面或弹框：排班工具栏
可见条件：排班页可见时常驻显示，位置在"导入排班"右侧。
启用条件：已加载当前排班版本；不要求先执行自动设置休息日，`GeneratedAt` 为空时也可点击。
点击或输入行为：点击后弹出 `.xlsx` 保存对话框，默认文件名按当前排班年月和版本名生成；用户确认保存路径后，按当前界面表格内容写入 Excel。
成功反馈：状态栏显示导出文件路径；弹窗提示导出成功并询问是否打开该文件。
失败反馈：保存路径无效、文件被占用或导出异常时弹出错误提示，并在状态栏显示失败原因。
校验规则：导出内容来自当前排班版本的年月、每日总休、人员和班次单元格；班次显示与界面一致，旧"夜 / 大夜"归一化为"大"，"白"在 Excel 中显示为空白；人员汇总列包含"上班 / 休息 / 连上 / 末休 / 可上"，"末休"统计当前月份内周六和周日休息天数，"可上"统计当前行空白单元格与已设置上班单元格的合计。
跳转逻辑：无跨页面跳转；用户选择打开文件时交由系统默认程序打开 `.xlsx`。
权限边界：本地桌面功能，无角色权限；只写入用户在保存对话框中选择的文件。
关联接口或命令：`ScheduleViewModel.ExportExcelCommand`、`ScheduleExcelExporter.ExportAsync()`。
关联业务场景：排班 Excel 导出
证据来源：`src/MyTools/Views/SchedulePage.xaml`、`src/MyTools/ViewModels/ScheduleViewModel.cs`、`src/MyTools/Services/ScheduleExcelExporter.cs`
待确认项：无。

3.1.2排班表格

页面功能描述：按月份日期展示姓名、星期、日期、实休、总休、人员班次格和行末汇总，支持点击单元格弹出班次菜单。

3.1.2.1"班次单元格"控件类型为表格单元格按钮，功能是展示并编辑某人员某日期的班次。

控件编号：3.1.2.1
控件名称：班次单元格
控件类型：表格单元格按钮
所在页面或弹框：排班表格
可见条件：排班版本已加载，且对应人员行位于当前滚动可视区域或缓冲区域内。
启用条件：排班版本已加载；人员行和日期列存在。
点击或输入行为：单击后当前格进入选区并弹出班次菜单，可选择"白 / 卡 / 副 / 感 / 大夜 / 小 / 休 / 公 / 产假 / 小1 / 清空"；菜单隐藏"午"，已有"午"数据仍可显示和统计；按住鼠标拖拽可选择矩形区域，`Shift` 点击按源格到目标格扩展矩形选区，`Ctrl` 点击追加或取消单格选择；选区存在时按 `Delete` 清空选中班次格。
成功反馈：单元格文字立即刷新；空白格显示"·"，"白"不显示文字，旧"夜"归一化显示为"大"，"产假"写入后显示为"产"；"大 / 小 / 副"使用白字深底，"休 / 卡 / 感 / 公 / 产 / 午"使用各自班次底色，其中"公"和"午"底色一致，"产"使用浅橙色；周末和其它节假日的空白格与"白"格使用蓝色列底色，已有专用班次色的格子不被节假日列底色覆盖；蓝色边框表示选中单元格，绿色边框表示填充源单元格；行末汇总显示"上班 / 休息 / 连上 / 末休 / 可上"，其中"末休"为当前月份周六和周日休息天数合计，"可上"为空白单元格与已设置上班单元格合计。
失败反馈：无独立失败反馈；保存、导出或自动生成失败由对应按钮流程提示。
校验规则：班次代码必须来自 `ShiftCodes` 支持的集合；界面刷新时按 `ShiftCodes.Normalize` 归一化旧"夜"；"产"计入 1 天休息，不计入上班和连续上班。
跳转逻辑：冲突侧栏点击定位时可滚动并高亮对应班次单元格。
权限边界：本地桌面功能，无角色权限；数据保存到当前用户本地排班目录。
关联接口或命令：`ScheduleViewModel.SetCell()`、`ScheduleViewModel.ApplySmallNight1()`、`ScheduleViewModel.FillCellsFromSource()`、`ScheduleViewModel.ClearCells()`、`SchedulePage.ResolveCellBg()`、`SchedulePage.ResolveCellFg()`。
关联业务场景：排班单元格编辑
证据来源：`src/MyTools/Views/SchedulePage.xaml.cs`、`src/MyTools/Services/ScheduleModels.cs`
待确认项：无。

3.1.2.2"填充"控件类型为工具栏按钮，功能是用源单元格班次批量填充当前选区。

控件编号：3.1.2.2
控件名称：填充
控件类型：工具栏按钮
所在页面或弹框：排班工具栏
可见条件：排班页面可见。
启用条件：按钮常驻可点击；无选区时点击会提示先选择单元格。
点击或输入行为：读取绿色边框源单元格的班次代码，将该代码写入当前选区内所有有效班次单元格；源格为空白时，目标选区同步清空。
成功反馈：状态栏显示已填充的单元格数量；目标格文字、底色、手动标记、上班/休息/连上/末休/可上统计和冲突侧栏随局部刷新更新。
失败反馈：未选择单元格时弹出提示"请先在排班表中选择要填充的单元格。"；源坐标无效时弹出提示"请先选择一个源单元格。"。
校验规则：只接受排班表中有效的人员行和日期列；非空代码写入后标记为 `IsManual=true`，空白写入后标记为 `IsManual=false`。
跳转逻辑：无跨页面跳转。
权限边界：本地桌面功能，无角色权限；数据保存到当前用户本地排班目录。
关联接口或命令：`SchedulePage.FillSelectedCells_Click()`、`ScheduleViewModel.FillCellsFromSource()`。
关联业务场景：排班单元格批量编辑
证据来源：`src/MyTools/Views/SchedulePage.xaml`、`src/MyTools/Views/SchedulePage.xaml.cs`、`src/MyTools/ViewModels/ScheduleViewModel.cs`
待确认项：无。

### 4. 系统信息

4.1硬件摘要模块

模块功能描述：展示本机系统、主板、内存、CPU、GPU、磁盘、网卡和显示器摘要；实时传感器功能已移除，不再依赖 LibreHardwareMonitor。

4.1.1硬件概览标签页

页面功能描述：系统信息页的"硬件概览"标签展示刷新按钮、管理员重启按钮、状态文本和硬件摘要卡片。

4.1.1.1"硬件摘要刷新控件组"控件类型为按钮 + 状态标签，功能是按需读取并反馈硬件摘要。

控件编号：4.1.1.1
控件名称：硬件摘要刷新控件组
控件类型：按钮 + 状态标签
所在页面或弹框：系统信息页 / 硬件概览标签
可见条件：进入"系统信息"模块后可见。
启用条件："刷新硬件"按钮绑定 `LoadSystemInfoCommand`，读取中禁用；非管理员时显示"以管理员重启"按钮。
点击或输入行为：进入"系统信息"模块或点击"刷新硬件"时异步执行 `LoadSystemInfoAsync()` 读取硬件摘要；点击"以管理员重启"时通过 `RestartAsAdminCommand` 以 runas 重启当前程序。
成功反馈：状态文本显示已读取的 CPU/GPU/内存/磁盘数量，硬件概览卡片刷新。
失败反馈：读取失败时状态文本显示失败原因；取消 UAC 时显示已取消管理员授权。
校验规则：硬件摘要读取不得放入启动期同步执行；实时传感器、刷新模式、传感器读数表和 LibreHardwareMonitor 权限提示不再显示。
跳转逻辑：无跨页面跳转。
权限边界：本地桌面功能，无角色权限；管理员重启只在用户确认 UAC 后执行。
关联接口或命令：`ShowSystemInfoCommand`、`SwitchModule("SystemInfo")`、`LoadSystemInfoCommand`、`LoadSystemInfoAsync()`、`RestartAsAdminCommand`。
关联业务场景：按需查看硬件摘要
证据来源：`src/MyTools/Views/SystemInfoView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`
待确认项：无。

### 5. Codex 账号切换器

5.1本地中转模块

模块功能描述：管理 Codex 账号档案、本地 relay 启停、通过本地中转切换上游，以及为 Cursor / OpenAI-compatible 客户端提供模型列表和 Chat Completions 兼容兜底。

5.1.1本地中转控制区

页面功能描述：Codex 账号切换器顶部展示本地中转状态圆点、状态文本、启动本地中转、停止使用本地中转和重启 Codex 使用中转按钮；档案列表中的切换按钮在本地中转模式下显示为"本地切换"，每条档案提供"测试"按钮单独检测该档案中转是否可达。

5.1.1.1"本地中转控制区"控件类型为状态标签 + 文本按钮组，功能是启停本地中转并切换 Codex 档案到本地中转上游。

控件编号：5.1.1.1
控件名称：本地中转控制区
控件类型：状态标签 + 文本按钮组
所在页面或弹框：Codex 账号切换器顶部控制区
可见条件：进入"Codex 账号切换器"模块后常驻可见。
启用条件："启动本地中转"始终可点击；"停止使用本地中转"仅 `CanDisableCodexLocalRelay` 为 true 时可点击；"重启 Codex 使用中转"仅本地中转状态为绿色且不在检测中时可点击；档案行按钮在本地中转已启用时显示"本地切换"；档案行"测试"按钮在未进行批量中转测试、且该档案未处于中转测试或切换执行中时可点击。
点击或输入行为：点击"启动本地中转"会从当前 Codex 配置生成本地 relay 设置，启动 relay 并通过 relay 测试当前 NewAPI；点击"停止使用本地中转"会停用 MyTools 本地 relay 设置并把档案按钮恢复为普通"切换"；点击"重启 Codex 使用中转"会把 Codex 配置固定到本地 relay 后重启 Codex App；点击档案行"测试"只测试当前档案中转，更新该档案中转灯号、测试时间和摘要，不切换、不备份、不覆盖当前 `~/.codex`；点击档案行"本地切换"会先测试目标 NewAPI，测试通过后只切换本地中转上游，不覆盖当前 `~/.codex`。
成功反馈：状态圆点按未知/测试/成功/失败显示不同颜色；状态文本显示"本地中转：..."和检查时间；档案行"测试"通过后该档案中转灯号变绿并在 `CodexProfilesStatusMessage` 显示通过摘要；启动成功后状态消息提示当前本地固定连接与测试结果；本地切换成功后弹窗提示 Codex 固定连接、当前上游和是否需要重启 Codex。
失败反馈：启动、停用、配置固定或目标上游测试失败时，状态圆点转失败色，状态文本和 `CodexProfilesStatusMessage` 显示失败原因，并弹出对应错误或警告提示；档案行"测试"失败时只把该档案中转灯号转红并显示失败摘要。
校验规则：本地中转请求需要本地 token 授权；日志只记录主机、路径、状态码和错误类型，不记录上游 key、本地 relay token、请求体或响应正文；OpenAI-compatible 客户端访问 `/v1/models` 时可返回本地模型列表，访问 `/v1/chat/completions` 且上游拒绝或返回非 OpenAI 内容类型时可转换为 `/v1/responses` 并把 JSON 或 SSE 结果转回 Chat Completions 格式。
跳转逻辑：无跨页面跳转；"重启 Codex 使用中转"会关闭并重新打开 Codex App；本地切换成功后当前 Codex 后续请求使用新的上游。
权限边界：本地桌面功能，无角色权限；会读取和写入当前用户 `~/.codex` 配置及 MyTools 本地 relay 设置，敏感配置内容按既有 DPAPI 保护规则保存。
关联接口或命令：`EnableCodexLocalRelayCommand`、`DisableCodexLocalRelayCommand`、`RestartCodexWithLocalRelayCommand`、`SwitchCodexProfileCommand`、`TestCodexProfileRelayCommand`、`CodexLocalRelayService`、`CodexLocalRelayCompatibility`。
关联业务场景：本地中转接入 Cursor 与 OpenAI-compatible 客户端
证据来源：`src/MyTools/Views/MainModulesView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/CodexLocalRelayService.cs`、`src/MyTools/Services/CodexLocalRelayCompatibility.cs`
待确认项：按用户要求未停止或替换当前正在使用的安装版 local relay 做真实 Cursor 端到端接管实测；当前验证为 Release build 与 `scripts/codex-eval.ps1` 程序集级兼容回归。

5.2已移除控件

模块功能描述：Codex 账号切换器中原批量轮换和本地模型自动导入控件已取消，当前用户不能通过界面触发账号轮换池或读取本机 Ollama 模型列表。

5.2.1 Codex 轮换与本地模型导入入口
页面功能描述：顶部状态区和档案卡片不再出现轮换池或导入本地模型相关控件。

5.2.1.1"Codex 轮换控件组"控件类型为已删除控件组，包含原“立即轮换”“全选轮换”“反选轮换”“轮换后通知”“无可用轮换目标”“加入轮换”和“优先级”。
可见条件：无。
启用条件：无。
点击或输入行为：无。
成功反馈：无。
失败反馈：无。
校验规则：`MainModulesView.xaml` 不再包含上述显示文本或绑定；`MainViewModel` 不再暴露对应轮换命令；`scripts/codex-eval.ps1 -Quick` 守卫该移除状态。
跳转逻辑：无。
权限边界：无。
关联接口或命令：无。
关联业务场景：取消不再需要的 Codex 账号轮换池操作。
证据来源：`src/MyTools/Views/MainModulesView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`scripts/codex-eval.ps1`。
待确认项：无。

5.2.1.2"导入本地模型"控件类型为已删除按钮，原“导入 Ollama 模型”按钮和自动读取本机模型列表功能已取消。
可见条件：无。
启用条件：无。
点击或输入行为：无。
成功反馈：无。
失败反馈：无。
校验规则：`MainModulesView.xaml` 不再绑定 `ImportCodexOllamaProfilesCommand`，`CodexOllamaProfileService.cs` 已删除，程序不再请求 `127.0.0.1:11434/api/tags`。
跳转逻辑：无。
权限边界：无。
关联接口或命令：无。
关联业务场景：取消不再需要的本地模型自动导入。
证据来源：`src/MyTools/Views/MainModulesView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`scripts/codex-eval.ps1`。
待确认项：无。
### 6. 系统优化

6.1系统维护模块

模块功能描述：在系统优化页的“系统维护”标签中集中展示系统锁定、安全防护和自动更新状态，并提供需要 UAC 授权的系统级操作。

6.1.1自动更新行

页面功能描述：自动更新行展示 Windows Update 状态徽章、“停止更新 / 恢复更新”按钮和“立即更新”按钮。

6.1.1.1"停止更新 / 恢复更新"控件类型为文本按钮，功能是停止或恢复 Windows 自动更新。

控件编号：6.1.1.1
控件名称：停止更新 / 恢复更新
控件类型：文本按钮
所在页面或弹框：系统优化页 / 系统维护卡片 / 自动更新行
可见条件：进入“系统优化”模块并停留在“系统维护”标签时可见。
启用条件：按钮始终绑定 `ToggleAutoUpdateCommand`；具体是否能修改系统取决于用户是否同意 UAC 授权和当前 Windows 策略权限。
点击或输入行为：状态为自动更新中时，按钮显示“停止更新”，点击后请求 UAC 授权，写入停止更新策略，禁用常见 Windows Update 计划任务，并停止、禁用 `wuauserv`、`UsoSvc`、`BITS`、`DoSvc`、`WaaSMedicSvc` 等更新相关服务；状态为已停止时，按钮显示“恢复更新”，点击后移除停止策略，把服务启动类型和计划任务恢复为 MyTools 默认更新配置并启动必要服务。
成功反馈：停止成功时徽章显示“已停止”，按钮变为“恢复更新”，`SystemStatusMessage` 显示“自动更新已停止，Windows Update 相关服务和计划任务已禁用。”；恢复成功时徽章显示“自动更新中”，按钮变为“停止更新”，`SystemStatusMessage` 显示“自动更新已恢复为 MyTools 默认更新服务配置。”。
失败反馈：用户取消 UAC 时显示“操作已取消（UAC 未授权）。”；命令异常时弹出“操作失败”；停止后未读回策略和核心服务禁用状态时显示“已提交停止命令，但未读回完整停止状态”；恢复后仍读回停止策略或禁用服务时显示“已提交恢复命令，但自动更新策略或服务仍处于停止状态”。
校验规则：状态刷新读取 `NoAutoUpdate` 策略、核心更新服务启动类型和声明处理的 Windows Update 计划任务状态；停止成功提示必须满足停止策略存在、核心服务启动类型为 Disabled 且计划任务读回为 Disabled；恢复成功提示必须满足停止策略已移除、核心更新服务未保持 Disabled 且计划任务读回为 Enabled。
跳转逻辑：无跨页面跳转。
权限边界：本地桌面功能，无角色权限；修改 HKLM 策略、Windows 服务和计划任务需要管理员 UAC 授权。
关联接口或命令：`ToggleAutoUpdateCommand`、`WindowsSecurityService.SetAutoUpdateAsync()`、`WindowsSecurityService.IsAutoUpdateStopEnforced()`、`WindowsSecurityService.IsAutoUpdateRestoreReady()`。
关联业务场景：停止或恢复 Windows 自动更新
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/WindowsSecurityService.cs`
待确认项：未在当前机器执行真实 UAC 停止/恢复 Windows Update，以避免改变用户系统更新状态；当前验证为 Release build 和脚本级快速检查。

6.2自动优化模块

模块功能描述：在系统优化页的“自动优化”标签中先生成可审计扫描计划，再由用户勾选要执行的白名单优化项。

6.2.1扫描按钮

页面功能描述：点击“扫描”后只读取系统状态，生成包含执行勾选、风险、边界和证据的优化计划。

6.2.1.1"扫描"控件类型为文本按钮，功能是生成自动优化计划。

控件编号：6.2.1.1
控件名称：扫描
控件类型：文本按钮
所在页面或弹框：系统优化页 / 自动优化标签 / 自动优化与报告卡片
可见条件：进入“系统优化”模块并停留在“自动优化”标签时可见。
启用条件：`IsAutoOptimizeBusy=false` 时可点击。
点击或输入行为：调用 `ScanAutoOptimizeCommand`，后台执行 `SystemOptimizationService.ScanAsync`；扫描阶段不删除文件、不清空回收站、不重启 Explorer、不启停服务、不执行 UAC 提权脚本、不写入系统设置。
成功反馈：`AutoOptimizeStatusMessage` 显示可优化数量、默认勾选数量、需手动确认数量和预计释放空间；“本次扫描结果”表格刷新为 14 步计划。
失败反馈：扫描异常时弹出“自动优化扫描失败”，状态文本显示失败原因，扫描结果清空。
校验规则：扫描计划只允许白名单步骤；递归扫描跳过 junction/symlink，并确认枚举路径始终在批准根目录内。
跳转逻辑：无跨页面跳转。
权限边界：扫描不请求 UAC，不修改系统。
关联接口或命令：`ScanAutoOptimizeCommand`、`SystemOptimizationService.ScanAsync()`。
关联业务场景：先扫描再勾选执行自动优化
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/SystemOptimizationService.cs`
待确认项：未在当前机器执行真实系统清理，仅验证 Release build 和脚本级快速检查。

6.2.2本次扫描结果表

页面功能描述：表格展示每个优化项的执行勾选、状态、项目、风险、类型、预计释放空间、安全边界、扫描证据和说明。

6.2.2.1"本次扫描结果"控件类型为数据表格，功能是让用户选择本次实际执行的优化项。

控件编号：6.2.2.1
控件名称：本次扫描结果
控件类型：数据表格
所在页面或弹框：系统优化页 / 自动优化标签 / 自动优化与报告卡片
可见条件：进入“系统优化”模块并停留在“自动优化”标签时可见；无扫描结果时显示空表头。
启用条件：每行“执行”复选框仅在 `CanOptimize=true` 时可勾选；不可优化项禁用勾选。
点击或输入行为：用户勾选或取消勾选“执行”列后，`HasRunnableAutoOptimizePlan` 立即重算并刷新“优化”按钮启用状态。
成功反馈：已勾选行状态显示“已选择”；未勾选可执行行状态显示“可选择”；不可执行行状态显示“无需处理”。
失败反馈：无独立失败弹框；执行前未勾选任何可优化项时，状态文本提示先扫描并确认存在可优化项目。
校验规则：低风险且收益明确的项目可默认勾选；中高风险、会重启 Explorer、清理诊断证据、清空回收站、优化磁盘或执行 DISM 的项目默认不勾选。
跳转逻辑：无跨页面跳转。
权限边界：勾选本身不请求 UAC；实际执行时由对应步骤决定是否需要 UAC。
关联接口或命令：`AutoOptimizePlanItems`、`OptimizationPlanItem.IsSelected`、`StartAutoOptimizeCommand`。
关联业务场景：先扫描再勾选执行自动优化
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/SystemOptimizationService.cs`
待确认项：无。

6.2.3优化按钮与确认弹窗

页面功能描述：点击“优化”后仅执行已勾选且可优化的白名单步骤，并在执行前展示风险摘要。

6.2.3.1"优化"控件类型为文本按钮，功能是执行本次已勾选的自动优化步骤。

控件编号：6.2.3.1
控件名称：优化
控件类型：文本按钮 + 确认弹窗
所在页面或弹框：系统优化页 / 自动优化标签 / 自动优化与报告卡片
可见条件：进入“系统优化”模块并停留在“自动优化”标签时可见。
启用条件：`IsAutoOptimizeBusy=false` 且扫描结果中至少 1 行满足 `CanOptimize=true && IsSelected=true`。
点击或输入行为：弹出确认框，汇总执行数量、预计释放空间、需 UAC 数量、中高风险数量；存在高风险项时列出具体项目名称。用户确认后调用 `SystemOptimizationService.RunAsync(..., enabledStepKeys)`，仅传入已勾选步骤键。若包含缩略图缓存项，会再询问是否允许临时重启 Explorer；选择“否”时只跳过该步骤。
成功反馈：状态文本显示自动优化完成摘要和释放空间；历史报告列表新增本次 `OptimizationReportItem`；本次扫描结果清空。
失败反馈：执行异常时弹出“自动优化失败”并保留错误状态文本；用户取消确认时显示“已取消自动优化。”；用户取消 UAC 时对应步骤在报告中标记 Failed。
校验规则：提权 PowerShell 脚本必须在超时内退出且退出码为 0 才视为成功；脚本超时、退出码非 0 或 UAC 取消均不能记为成功；提权清理脚本也必须跳过 junction/symlink，并在删除前校验目标仍位于批准根目录内。
跳转逻辑：无跨页面跳转。
权限边界：不同步骤可能请求管理员 UAC；高风险项目必须由用户在表格中手动勾选，并在确认弹窗中再次看到项目名称。
关联接口或命令：`StartAutoOptimizeCommand`、`SystemOptimizationService.RunAsync()`、`ElevatedScriptRunner.RunElevatedScriptAsync()`、`OptimizationReportService.SaveAsync()`。
关联业务场景：先扫描再勾选执行自动优化
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/SystemOptimizationService.cs`
待确认项：未在当前机器执行真实清理、UAC 授权或 Explorer 重启，以避免改变用户系统状态；当前验证为 Release build 和脚本级快速检查。

6.3垃圾清理模块

模块功能描述：在系统优化页的“垃圾清理”标签中先扫描白名单垃圾候选，再由用户勾选并确认清理。

6.3.1开始扫描按钮

页面功能描述：点击“开始扫描”后只读取白名单范围，生成带风险、边界和证据的垃圾清理候选列表。

6.3.1.1"开始扫描"控件类型为文本按钮，功能是生成垃圾清理候选。

控件编号：6.3.1.1
控件名称：开始扫描
控件类型：文本按钮
所在页面或弹框：系统优化页 / 垃圾清理标签 / 垃圾清理卡片
可见条件：进入“系统优化”模块并停留在“垃圾清理”标签时可见。
启用条件：`IsJunkBusy=false` 时可点击。
点击或输入行为：调用 `StartJunkScanCommand`，后台执行 `JunkCleanupService.ScanAsync`；扫描阶段不删除文件、不清空回收站、不启停服务、不执行 UAC 提权脚本。
成功反馈：`JunkStatusMessage` 显示候选总数、默认勾选数量、需手动确认数量和预计释放空间；扫描结果表刷新。
失败反馈：扫描异常时弹出“垃圾扫描失败”，状态文本显示失败原因。
校验规则：扫描范围只允许白名单目录；递归枚举跳过 junction/symlink，并确认文件仍在批准根目录内。
跳转逻辑：无跨页面跳转。
权限边界：扫描不请求 UAC，不修改系统。
关联接口或命令：`StartJunkScanCommand`、`JunkCleanupService.ScanAsync()`。
关联业务场景：先扫描再确认清理白名单垃圾
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/JunkCleanupService.cs`
待确认项：未在当前机器执行真实清理。

6.3.2垃圾扫描结果表

页面功能描述：表格展示每个垃圾候选项的勾选状态、类别、大小、风险、建议、安全边界、扫描证据、路径和说明。

6.3.2.1"垃圾扫描结果"控件类型为数据表格，功能是让用户选择本次要清理的白名单项目。

控件编号：6.3.2.1
控件名称：垃圾扫描结果
控件类型：数据表格
所在页面或弹框：系统优化页 / 垃圾清理标签 / 垃圾清理卡片
可见条件：进入“系统优化”模块并停留在“垃圾清理”标签时可见；无扫描结果时显示空表头。
启用条件：每行“选择”复选框可由用户勾选或取消；诊断证据类和浏览器/回收站项默认不勾选。
点击或输入行为：用户勾选或取消勾选后，`RunJunkCleanupCommand` 的可执行状态立即重算。
成功反馈：勾选状态在表格中即时变化。
失败反馈：无独立失败弹框；清理前未勾选任何项目时，“立即清理”不可用。
校验规则：表格必须展示“边界”和“证据”列；不再展示“导出报告”按钮。
跳转逻辑：无跨页面跳转。
权限边界：勾选本身不请求 UAC；清理系统缓存时由清理流程请求管理员权限。
关联接口或命令：`JunkCandidates`、`JunkCandidate.SafetyBoundary`、`JunkCandidate.Evidence`、`RunJunkCleanupCommand`。
关联业务场景：先扫描再确认清理白名单垃圾
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/JunkCleanupService.cs`
待确认项：无。

6.3.3立即清理按钮

页面功能描述：点击“立即清理”后只处理已勾选候选，并在执行前弹窗确认。

6.3.3.1"立即清理"控件类型为文本按钮，功能是执行本次已勾选垃圾候选清理。

控件编号：6.3.3.1
控件名称：立即清理
控件类型：文本按钮 + 确认弹窗
所在页面或弹框：系统优化页 / 垃圾清理标签 / 垃圾清理卡片
可见条件：进入“系统优化”模块并停留在“垃圾清理”标签时可见。
启用条件：`IsJunkBusy=false` 且至少 1 条 `JunkCandidate.IsSelected=true`。
点击或输入行为：弹出确认框，显示将清理数量和预计释放空间；用户确认后调用 `JunkCleanupService.CleanupAsync()`。
成功反馈：状态文本和完成弹窗显示已清理数量与释放空间；历史报告列表新增 `JunkCleanup` 报告。
失败反馈：执行异常时弹出“垃圾清理失败”并保留错误状态文本；用户取消确认时不执行清理。
校验规则：普通文件发送到回收站；系统缓存提权脚本必须包含根内路径校验和 reparse point 跳过。
跳转逻辑：无跨页面跳转。
权限边界：系统临时目录、Windows Update 缓存和 Delivery Optimization 缓存会请求管理员 UAC；用户取消 UAC 时对应步骤标记失败。
关联接口或命令：`RunJunkCleanupCommand`、`JunkCleanupService.CleanupAsync()`、`ElevatedScriptRunner.RunElevatedScriptAsync()`、`OptimizationReportService.SaveAsync()`。
关联业务场景：先扫描再确认清理白名单垃圾
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/JunkCleanupService.cs`
待确认项：未在当前机器执行真实清理或 UAC 授权。

### 7. 已移除控件

7.1 SQL 导出工具（已移除）
模块功能描述：原 SQL 导出 / SQL 查询工具已取消，当前桌面端不再提供数据库连接、测试连接、表导出、SQL 查询或连接历史控件。

7.1.1顶部菜单与 SQL 导出页
页面功能描述：当前顶部菜单和主内容区不再出现 SQL 导出入口或 SQL 导出页面。

7.1.1.1"SQL 导出"菜单项，控件类型为菜单项，当前状态为已删除，用户在顶部菜单中不再看到该入口。
可见条件：无。
启用条件：无。
点击或输入行为：无。
成功反馈：无。
失败反馈：无。
校验规则：`MainWindow.xaml` 不再绑定 `ShowSqlExportCommand`，`scripts/codex-eval.ps1 -Quick` 会检查该入口保持移除。
跳转逻辑：无。
权限边界：无。
关联接口或命令：无。
关联业务场景：取消不再需要的数据库导出与查询工具。
证据来源：`src/MyTools/MainWindow.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`scripts/codex-eval.ps1`。
待确认项：无。

7.1.1.2"SQL 导出工具 / SQL 查询"页面，控件类型为页面 + 标签页 + 表格，当前状态为已删除，程序不再创建服务器、端口、用户名、密码、数据库、数据表、测试连接、导出 Excel、取消、查询输入、复制结果或导出查询结果控件。
可见条件：无。
启用条件：无。
点击或输入行为：无。
成功反馈：无。
失败反馈：无。
校验规则：`Views/MainModulesView.xaml` 不再包含 `SqlExportHost`，`MainViewModel` 不再维护 SQL 连接和查询状态。
跳转逻辑：无。
权限边界：无。
关联接口或命令：无。
关联业务场景：取消不再需要的数据库导出与查询工具。
证据来源：`src/MyTools/Views/MainModulesView.xaml`、`src/MyTools/Views/MainModulesView.xaml.cs`、`src/MyTools/ViewModels/MainViewModel.cs`。
待确认项：无。

### 7.2 C++ Native Shell（阶段 1/2/3 起步）

模块功能描述：C/C++ 原生重写轨道的 Native 窗口壳与模块承载点，当前用于验证 Win32 + Direct2D + DirectWrite 主窗口、菜单、状态栏、托盘、日志、DPAPI smoke test、基础服务和 Codex Profiles 阶段 3 入口及后端 service 骨架。

7.2.1"Native 主窗口"，控件类型为 Win32 顶层窗口，当前状态为已新增。
可见条件：用户启动 `MyToolsNative.exe` 后显示；当前机器需先具备 CMake/MSVC 构建工具链才能生成可运行 exe。
启用条件：Native 程序通过单实例 Mutex 和 DPAPI smoke test。
点击或输入行为：窗口内容区展示当前 Native 模块信息；默认 Home 模块展示阶段 1/2 基线和不迁回功能清单。
成功反馈：窗口显示菜单栏、内容占位区和底部状态栏。
失败反馈：窗口创建失败时弹窗提示查看 `MyToolsNative.log`。
校验规则：`scripts/native-eval.ps1 -Quick` 检查 Native 主窗口源码结构，`scripts/native-eval.ps1 -Build` 在工具链可用时执行真实构建。
跳转逻辑：通过 Tools 菜单可在 Home 与 Codex Profiles 之间切换；FRP 和 Screenshot 仍为灰置待迁移项。
权限边界：普通用户启动；不在启动期执行提权操作。
关联接口或命令：`src/MyTools.Native/ui/main_window.cpp`。
关联业务场景：C/C++ 原生重写阶段 1/2/3 起步。
证据来源：`src/MyTools.Native/ui/main_window.cpp`、`src/MyTools.Native/ui/renderer_d2d.cpp`、`scripts/native-eval.ps1`。
待确认项：本机尚未安装 CMake/MSVC，未完成真实运行截图。

7.2.2"File / System / Tools / Help"菜单栏，控件类型为原生菜单栏，当前状态为已新增。
可见条件：Native 主窗口显示时可见。
启用条件："Exit"、"About"、"Home" 和 "Codex Profiles" 可用；FRP Tunnel 与 Screenshot 当前为灰置待迁移项。
点击或输入行为：点击 File → Exit 退出 Native 程序；点击 Tools → Home 切回 Home 模块；点击 Tools → Codex Profiles 切换到 Codex Profiles 状态页；点击 Help → About 显示阶段 1/2 与 Codex Profiles scaffold 说明弹窗。
成功反馈：Exit 关闭窗口并释放托盘；Home / Codex Profiles 会重绘内容区和底部状态栏；About 显示信息弹窗。
失败反馈：无专用失败反馈。
校验规则：`scripts/codex-eval.ps1 -Quick` 的 `native shell scaffold` 守卫 Native 菜单相关源码存在。
跳转逻辑：Home 与 Codex Profiles 当前为同窗口内容切换，不打开新窗口，不执行账号切换。
权限边界：普通用户操作。
关联接口或命令：`MainWindow::CreateMenuBar()`。
关联业务场景：C/C++ 原生重写阶段 1/2/3 起步。
证据来源：`src/MyTools.Native/ui/main_window.cpp`。
待确认项：真实菜单交互需工具链完成后人工 smoke。

7.2.3"系统托盘 Show window / Exit"，控件类型为托盘图标和右键菜单，当前状态为已新增。
可见条件：Native 主窗口创建成功后添加托盘图标。
启用条件：托盘图标成功注册到 Windows 通知区域。
点击或输入行为：双击托盘恢复窗口；右键打开菜单；Show window 恢复窗口；Exit 退出程序。
成功反馈：窗口隐藏、恢复或退出。
失败反馈：托盘添加失败时当前阶段仅不显示托盘，不阻断主窗口。
校验规则：`scripts/native-eval.ps1 -Quick` 检查 `Shell_NotifyIconW`、`NIM_ADD` 和 `NIM_DELETE`。
跳转逻辑：无。
权限边界：普通用户操作。
关联接口或命令：`TrayHost::Add()`、`TrayHost::ShowMenu()`、`MainWindow::HideToTray()`。
关联业务场景：C/C++ 原生重写阶段 1。
证据来源：`src/MyTools.Native/app/tray_host.cpp`、`src/MyTools.Native/ui/main_window.cpp`。
待确认项：真实托盘交互需工具链完成后人工 smoke。

7.2.4"底部状态栏"，控件类型为 Direct2D 绘制状态栏，当前状态为已新增。
可见条件：Native 主窗口显示时可见。
启用条件：Direct2D/DirectWrite 初始化成功。
点击或输入行为：无输入行为，展示当前模块位置，例如 `Current location: Home` 或 `Current location: Tools / Codex Profiles`。
成功反馈：底部状态文字可见。
失败反馈：Direct2D/DirectWrite 初始化失败时窗口创建失败并写日志。
校验规则：`scripts/native-eval.ps1 -Quick` 检查 Direct2D/DirectWrite 初始化 token。
跳转逻辑：无。
权限边界：无。
关联接口或命令：`RendererD2D::Render()`。
关联业务场景：C/C++ 原生重写阶段 1。
证据来源：`src/MyTools.Native/ui/renderer_d2d.cpp`。
待确认项：真实渲染截图需工具链完成后人工 smoke。

7.2.5"Codex Profiles"菜单项与状态页，控件类型为菜单项 + Direct2D 绘制状态页，当前状态为已更新。
可见条件：Native 主窗口显示时，Tools 菜单中可见。
启用条件：Native 主窗口已创建；该入口不要求本地已经存在 Codex 档案。
点击或输入行为：点击 Tools → Codex Profiles 后，程序解析 `LOCALAPPDATA` 与 `USERPROFILE`，探测 `%LOCALAPPDATA%\MyTools\Codex\profiles.json`、`active.json`、`Backups`、`~\.codex\config.toml` 和 `auth.json` 是否存在；若 `profiles.json` 存在，按 WPF 版 Base64 + 当前用户 DPAPI 格式解开外层档案库 JSON，按 `items` 对象逐条仅抽取 `DisplayName / Name / Status / LastImportedAt` 摘要；若 `active.json` 存在，读取 `ActiveDisplayName` 并在对应摘要行显示 active；摘要读取会解析 JSON `\uXXXX` 转义，保留中文档案名。
成功反馈：内容区标题变为 `Codex Profiles`，底部状态栏显示 `Current location: Tools / Codex Profiles`，列表展示档案库、当前 Codex 配置、首个显式动作目标、切换备份目录、当前可用显式菜单动作、`.codexbox` service 状态、档案摘要是否加载、档案数量、最多 5 条档案摘要和后续迁移项。
失败反馈：当前阶段没有单独失败弹窗；路径环境变量缺失时显示由路径拼接得到的空/相对结果；档案库无法解密或格式不符合当前 Windows 用户时，在内容区展示 `profile summary error` 摘要。
校验规则：当前阶段允许解开外层档案库元数据；页面导航不得自动触发备份、差异、元数据编辑、导入、导出、加密包导入导出、恢复或切换，不得读取或展示 `ProtectedConfigTomlBase64`、`ProtectedAuthJsonBase64`、`access_token`、`refresh_token` 等敏感字段，不得联网测试中转；写入类能力只能由 7.2.6 的显式菜单动作触发并先弹出确认；`scripts/native-eval.ps1 -Quick` 和 `scripts/codex-eval.ps1 -Quick` 守卫该边界。
跳转逻辑：同窗口切换到 Codex Profiles 模块；FRP、Screenshot 等模块仍不跳转。
权限边界：普通用户操作；页面导航只读取环境变量、文件存在状态和外层档案库元数据，不请求管理员权限，不修改文件。
关联接口或命令：`MainWindow::SwitchModule()`、`CodexProfileModule::ProbeLocalState()`、`CodexProfileBackupService::CreateCurrentFolderBackup()`、`CodexProfileBackupService::RestoreLatestBackup()`、`CodexProfileDiffService::BuildProfileDiffSummary()`、`CodexProfileEditService::UpdateProfileMetadata()`、`CodexProfileImportService::ImportCurrentFolderProfile()`、`CodexProfileExportService::ExportProfileByDisplayName()`、`CodexProfileBoxService::ExportBox()`、`CodexProfileBoxService::ImportBox()`、`CodexProfileSwitchService::ApplyProfileByDisplayName()`、`RendererD2D::Render()`。
关联业务场景：C/C++ 原生重写阶段 3 Codex 档案管理起步。
证据来源：`src/MyTools.Native/ui/main_window.cpp`、`src/MyTools.Native/modules/codex_profiles/codex_profile_module.cpp`、`src/MyTools.Native/services/codex_profile_edit_service.cpp`、`src/MyTools.Native/services/codex_profile_import_service.cpp`、`src/MyTools.Native/services/codex_profile_box_service.cpp`、`scripts/native-eval.ps1`。
待确认项：本机尚未安装 CMake/MSVC，未完成真实菜单点击和渲染截图；首个 active profile 改名已接入 `active.json` 同步但尚未做真实文件写入 smoke；`.codexbox` 冲突选择弹框已接入但尚未做真实导入 smoke；中转测试尚未实现；普通文件导出、首个档案改名、`.codexbox` 路径与密码弹框已接入显式菜单，但尚未做真实文件写入 smoke。

7.2.6"Codex Profiles 显式动作子菜单"，控件类型为 Win32 子菜单项 + MessageBox / 文本输入 / 文件选择 / 密码弹窗，当前状态为已更新。
可见条件：Native 主窗口显示时，Tools → Codex Profiles 子菜单中可见。
启用条件：Native 主窗口已创建；`Open overview`、刷新、差异、备份、应用、导入当前目录、恢复、普通文件导出、改名、note/remark/tags 编辑、`.codexbox` 导入导出均可点击但会按本地档案状态返回成功或失败提示。
点击或输入行为：子菜单包含 `Open overview`、`Refresh summaries`、`Diff first profile`、`Backup current .codex folder`、`Apply first profile...`、`Import current folder...`、`Restore latest backup...`、`Export first profile files...`、`Rename first profile...`、`Edit first profile note...`、`Edit first profile remark...`、`Edit first profile tags...`、`Export .codexbox...`、`Import .codexbox...`。点击 `Apply first profile...`、`Import current folder...`、`Restore latest backup...`、`Export first profile files...`、`Rename first profile...`、`Edit first profile note...`、`Edit first profile remark...`、`Edit first profile tags...`、`Export .codexbox...`、`Import .codexbox...` 时先显示确认弹框；用户选择 No 时取消，不调用 service。点击 `Export first profile files...` 会先选择输出文件夹，目标固定为当前摘要中的第一条可读档案，选择当前 Codex 目录 `~\.codex` 会被拒绝以避免无备份覆盖当前账号文件；点击 `Rename first profile...` 会输入新显示名，文本会 trim，不能为空，最长 120 字符，不能包含控制字符；若目标是当前 active profile，`CodexProfileEditService::UpdateProfileMetadata()` 会在写回 `profiles.json` 后同步更新 `active.json` 的 `ActiveDisplayName`。点击 `Edit first profile note...`、`Edit first profile remark...`、`Edit first profile tags...` 会通过 `PromptText` 输入文本，文本 trim，允许空值以清空字段，note/remark 最长 500 字符，tags 最长 200 字符，均拒绝 C0/C1 控制字符；通过校验后把字段名和值交给 `CodexProfileModule::RunUiAction()`，由 `CodexProfileEditService::UpdateProfileMetadata()` 写回 DPAPI 保护的 `profiles.json`。点击 `Export .codexbox...` 会先弹 `.codexbox` 保存路径，再要求输入加密密码并再次输入确认，两次非空且一致后把路径和密码交给 `CodexProfileModule::RunUiAction()`；点击 `Import .codexbox...` 会先弹 `.codexbox` 打开路径，再弹 `.codexbox import conflicts` 三按钮弹框选择 `Rename / Skip / Replace` 整包冲突策略，然后要求输入一次非空密码，随后通过 action 路由导入。点击 `Apply first profile...` 时目标固定为当前摘要中的第一条可读档案；点击 `Import current folder...` 时显示名自动生成为 `Native current yyyyMMdd_HHmmss`。
成功反馈：动作完成后显示 MessageBox 摘要；刷新会显示档案数量和 active 档案；差异会显示 config/auth 的存在性、相等性、字节数、行数和 12 位 SHA-256 摘要；备份显示 `.bak.dpapi` 路径或提示当前无可备份文件；应用显示目标档案和切换前备份路径；导入当前目录显示新档案名并提示创建新库或追加到已有库；恢复显示来源备份路径及 config/auth 是否恢复；普通文件导出显示输出文件夹及 `config.toml` / `auth.json` 导出结果；改名显示旧显示名和新显示名，若目标是 active profile 会额外提示 active marker 已写入 `active.json`；note/remark/tags 编辑显示目标字段已更新；`.codexbox` 导出显示输出路径和导出数量；`.codexbox` 导入显示导入、跳过、重命名冲突和替换数量。状态变更后内容区重新读取并刷新。
失败反馈：动作失败时显示 MessageBox 警告，内容来自 service 返回的非敏感错误文本；普通文件导出取消路径、选择当前 Codex 目录、改名为空、超过 120 字符或包含控制字符时不会继续调用对应写入 service；若 active profile 改名时 `profiles.json` 已写回但 `active.json` 同步失败，会提示局部失败并刷新摘要；note/remark 超过 500 字符、tags 超过 200 字符或包含 C0/C1 控制字符时不会继续调用对应写入 service；`.codexbox` 密码为空、两次导出密码不一致、冲突策略弹框取消、口令错误、文件损坏或路径取消时不会继续调用导入/导出 service；不显示 config/auth 正文、token 或完整 auth 载荷。
校验规则：主窗口只能调用 `CodexProfileModule::RunUiAction()`，不得直接调用 `CodexProfileBackupService`、`CodexProfileDiffService`、`CodexProfileImportService`、`CodexProfileExportService`、`CodexProfileEditService`、`CodexProfileBoxService`、`CodexProfileSwitchService` 等底层 service；写入类动作必须保留 `MB_YESNO | MB_ICONWARNING | MB_DEFBUTTON2` 确认；普通文件导出不得选择当前 Codex 目录；改名输入必须 trim 后非空、最长 120 字符且无控制字符，active profile 改名必须由 service 同步 `active.json`，不得在 UI 或 module 里直接写该文件；note/remark/tags 输入必须由 `PromptText` 收集，trim 后允许空值清空字段，note/remark 最长 500 字符、tags 最长 200 字符，均拒绝 C0/C1 控制字符；`.codexbox` 的 CDXB/PBKDF2/AES/HMAC 包格式只能由 action 后的 service 处理，Native UI 只负责路径、文本、密码和冲突策略选择；`.codexbox` 导入必须把 `options.box_conflict_policy` 传给 service，不得硬编码自动 Rename；模块动作层不得直接调用低层文件写入 API，不得联网、启动进程或输出 secret-bearing payload。
跳转逻辑：所有动作执行前先切换到 Codex Profiles 状态页；动作完成后仍留在该页面。
权限边界：普通用户权限；备份写入 `%LOCALAPPDATA%\MyTools\Codex\Backups`，导入当前目录、改名、note/remark/tags 编辑和 `.codexbox` 导入写入 `%LOCALAPPDATA%\MyTools\Codex\profiles.json`，active profile 改名还会同步写入 `%LOCALAPPDATA%\MyTools\Codex\active.json`，普通文件导出写入用户选择的非当前 Codex 目录，`.codexbox` 导出写入用户选择的目标文件，应用和恢复会覆盖当前用户 `~\.codex\config.toml` 与 `auth.json`。
关联接口或命令：`MainWindow::HandleCodexProfileAction()`、`MainWindow::ConfirmCodexProfileWrite()`、`PromptText()`、`MainWindow::PromptCodexBoxPassword()`、`ChooseCodexBoxConflictPolicy()`、`PickCodexBoxSavePath()`、`PickCodexBoxOpenPath()`、`CodexProfileModule::RunUiAction()`、`CodexProfileBackupService::CreateCurrentFolderBackup()`、`CodexProfileBackupService::RestoreLatestBackup()`、`CodexProfileDiffService::BuildProfileDiffSummary()`、`CodexProfileImportService::ImportCurrentFolderProfile()`、`CodexProfileExportService::ExportProfileByDisplayName()`、`CodexProfileEditService::UpdateProfileMetadata()`、`CodexProfileBoxService::ExportBox()`、`CodexProfileBoxService::ImportBox()`、`CodexProfileSwitchService::ApplyProfileByDisplayName()`。
关联业务场景：C/C++ 原生重写阶段 3 Codex 档案管理显式动作迁移。
证据来源：`src/MyTools.Native/ui/main_window.cpp`、`src/MyTools.Native/ui/modal_dialogs.cpp`、`src/MyTools.Native/modules/codex_profiles/codex_profile_module.cpp`、`src/MyTools.Native/services/codex_profile_export_service.cpp`、`src/MyTools.Native/services/codex_profile_edit_service.cpp`、`src/MyTools.Native/services/codex_profile_box_service.cpp`、`scripts/native-eval.ps1`、`scripts/codex-eval.ps1`。
待确认项：本机尚未安装 CMake/MSVC，未完成真实点击、弹窗和文件写入 smoke；当前菜单只操作第一条可读档案，后续需要自绘列表选择或对话框支持指定目标；`.codexbox` 冲突策略当前为整包选择，不是逐条冲突询问。
## 八、与其它活文档的同步

1. 本文新增或调整控件后，必须确认对应业务流程清单是否需要同步入口、步骤或异常路径。
2. 本文新增或调整控件后，必须确认实现核查台账是否需要同步证据、状态、缺口或验证结果。
3. 实现核查发现页面真实行为与本文不一致时，必须更新本文或记录代码缺口，不能让控件说明停留在旧状态。

## 九、自检清单

每次修改本文后，自检以下事项：

- [ ] 新增控件有清晰编号。
- [ ] 控件名称和界面展示一致。
- [ ] 控件类型准确。
- [ ] 可见条件和启用条件明确。
- [ ] 成功反馈和失败反馈明确。
- [ ] 校验规则和后端校验边界明确。
- [ ] 跳转逻辑明确。
- [ ] 权限展示和后端授权边界明确。
- [ ] 关联业务场景已写入。
- [ ] 没有把计划中但未落地的控件写成事实。

## 十、变更日志

- 2026-06-30 · v30 · Native Codex Profiles 子菜单接入 `Edit first profile note...`、`Edit first profile remark...`、`Edit first profile tags...`；三项均先确认，再由 `PromptText` 输入文本并 trim，允许空值清空字段，note/remark 最长 500 字符、tags 最长 200 字符，均拒绝 C0/C1 控制字符；真正写回仍经 `CodexProfileModule::RunUiAction()` 路由到 `CodexProfileEditService::UpdateProfileMetadata()`。

- 2026-06-30 · v31 · Native Codex Profiles 的 `Rename first profile...` 支持 active profile 改名同步；目标等于 `active.json` 的 `ActiveDisplayName` 时，service 在写回 `profiles.json` 后同步更新 `active.json`，UI/module 仍不直接写 active marker。

- 2026-06-30 · v32 · Native Codex Profiles 的 `Import .codexbox...` 接入同名冲突策略弹框，导入前由用户选择 Rename / Skip / Replace，`CodexProfileModule` 将策略传给 `CodexProfileBoxService::ImportBox()`，并移除 `.codexbox conflict choice dialog pending` 灰置菜单项。

- 2026-06-30 · v29 · Native Codex Profiles 子菜单接入 `Export first profile files...` 与 `Rename first profile...`；普通文件导出确认后选择非当前 Codex 目录并导出首个档案的 `config.toml` / `auth.json`，改名确认后采集 120 字符以内且无控制字符的新显示名；当时 active profile 改名仍未接入，v31 已迁移为同步更新 `active.json`。

- 2026-06-30 · v28 · Native Codex Profiles 子菜单接入 `Export .codexbox...` 与 `Import .codexbox...`；导出弹保存路径和两次密码确认，导入弹打开路径和一次密码，包格式处理仍经 `CodexProfileModule` action 路由；当时导入冲突仍自动重命名，v32 已迁移为导入前选择 Rename / Skip / Replace。

- 2026-06-30 · v27 · Native Codex Profiles 子菜单新增刷新、首个档案差异、当前目录备份、应用首个档案、导入当前目录、恢复最近备份显式动作；当时 `.codexbox` 路径/密码和冲突选择弹框仍待迁移，v28 接入路径/密码弹框，v32 接入冲突策略弹框。

- 2026-06-30 · v26 · Native Codex Profiles 状态页同步展示 `.codexbox` 加密包 service 已脚手架化但未由导航触发；页面导航继续禁止备份、差异、编辑、导入、导出、加密包导入导出、恢复、切换、展示 token 或联网。

- 2026-06-30 · v25 · Native Codex Profiles 状态页同步展示元数据编辑 service 已脚手架化但未由导航触发；页面导航继续禁止备份、差异、编辑、导入、导出、恢复、切换、展示 token 或联网。

- 2026-06-30 · v24 · Native Codex Profiles 状态页同步展示当前目录导入 service 已脚手架化但未由导航触发；页面导航继续禁止备份、差异、导入、导出、恢复、切换、展示 token 或联网。

- 2026-06-30 · v23 · Native Codex Profiles 状态页同步展示差异摘要 service 已脚手架化但未由导航触发；页面导航继续禁止备份、差异、导出、恢复、切换、展示 token 或联网。

- 2026-06-30 · v22 · Native Codex Profiles 状态页同步展示普通文件导出 service 已脚手架化但未由导航触发；页面导航继续禁止备份、导出、恢复、切换、展示 token 或联网。

- 2026-06-30 · v21 · Native Codex Profiles 状态页同步展示最近备份恢复 service 已脚手架化但未由导航触发；页面导航继续禁止备份、恢复、切换、展示 token 或联网。

- 2026-06-30 · v20 · Native Codex Profiles 状态页可读取 WPF 版 DPAPI 外层档案库摘要，只展示档案名、状态和导入时间等元数据，并继续禁止展示 token、联网或切换账号。

- 2026-06-30 · v19 · Native 菜单新增 Tools / Codex Profiles 入口和 Codex Profiles 状态页控件事实；当前只探测本地档案文件存在状态，不解密、不联网、不切换账号。

- 2026-06-30 · v18 · 新增 C++ Native Shell 阶段 1 控件事实：Win32 主窗口、菜单栏、托盘菜单和 Direct2D 状态栏；注明本机缺少 CMake/MSVC 时只能完成静态守卫。

- 2026-06-30 · v17 · 移除 Codex 顶部轮换控件、档案卡片轮换池控件和导入本地模型按钮，并新增 Quick 守卫。

- 2026-06-30 · v16 · 移除 SQL 导出工具控件事实：顶部菜单不再提供入口，主内容区不再加载 SQL 导出 / SQL 查询页面，Quick 校验守卫该移除状态。

- 2026-06-27 · v14 · 补充截图设置标签页截图范围、录像输出模式、帧率、画质、录音格式和编辑器开关的持久化说明。
- 2026-06-26 · v13 · 补充系统优化垃圾清理扫描结果表、边界证据列、取消导出报告按钮和提权脚本安全边界。
- 2026-06-26 · v12 · 补充系统优化自动优化扫描结果表、执行勾选、风险证据列、优化确认弹窗和提权脚本成功判定。
- 2026-06-26 · v11 · 补充系统优化自动更新停止/恢复按钮的读回校验、成功失败反馈和 UAC 权限边界。
- 2026-06-30 · v15 · 移除系统信息实时传感器标签页，改为硬件摘要-only；同步 FFmpeg 外置、SQL Server-only 和 Codex 429 自动监控移除后的控件事实。
- 2026-06-24 · v10 · 补充系统信息实时传感器按需异步采集和 Codex 本地中转 OpenAI-compatible 客户端兼容行为说明。
- 2026-06-22 · v9 · 补充排班"可上"汇总列说明，明确界面和 Excel 导出均按空白单元格 + 已设置上班单元格统计最多可上班天数。
- 2026-06-22 · v8 · 补充排班"末休"汇总列说明，明确界面和 Excel 导出均统计当前月份周六、周日休息天数。
- 2026-06-21 · v7 · 补充排班工具栏"导出"按钮说明，明确当前排班存在时可随时导出，不依赖 `GeneratedAt`。
- 2026-06-21 · v6 · 补充排班班次单元格多选、Delete 清空选区和"填充"按钮控件说明。
- 2026-06-21 · v5 · 更新排班班次菜单说明，记录"产假"菜单项、"产"显示、浅橙色底色和按 1 天休息统计。
- 2026-06-20 · v4 · 补充排班工具栏"导入排班"按钮说明，记录排班 Excel 导入成功、失败和保存提示行为。
- 2026-06-20 · v3 · 更新排班班次菜单说明，记录"午"选项隐藏且"公/午"底色一致。
- 2026-06-20 · v2 · 补充排班表格班次单元格说明，明确专用班次底色优先于节假日列底色。
- 2026-06-08 · v1 · 按通用场景驱动开发范式重建为可复用控件交互逻辑说明模板，后续由 `开qg` 场景执行实时维护。
