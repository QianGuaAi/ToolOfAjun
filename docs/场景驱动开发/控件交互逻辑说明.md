# 控件交互逻辑说明

> 文档定位：本文是场景驱动开发范式自动生成并持续维护的用户界面事实说明书。它记录用户可见页面、弹框、控件、按钮、筛选项、表格列、提示文案、禁用状态、校验规则、跳转逻辑和权限展示。
> 本文件使用新项目推荐文件名；请按项目实际页面、弹框、控件和权限展示补齐界面事实。
> 本文只回答“用户看得见、点得到、会收到什么反馈”，不记录完整业务流程或实现状态。

## 一、维护规则

1. 用户输入 `开qg` 并确认执行后，若本轮新增、删除或编辑任何用户可见控件，必须同步维护本文。
2. 控件说明必须来自源码、运行界面、截图、测试或用户确认，禁止凭空补写不存在的控件。
3. 删除控件时，必须同步删除或标注对应说明，避免留下孤儿控件说明。
4. 控件权限、禁用状态、校验规则和跳转逻辑必须和真实代码行为一致。
5. 每次只修改相关页面或控件条目；除非用户明确要求重建模板，不得整文件重写。

## 二、格式规范

所有控件说明按以下层级编号格式编写：

```text
X.业务域或端
X.X模块名
模块功能描述。
X.X.X页面或弹框名
页面或弹框功能描述。
X.X.X.X"控件名"控件类型，功能是XXX。
```

示例：

```text
1.系统管理
1.1用户管理
维护系统用户、角色和启用状态。
1.1.1用户列表页
展示用户列表并提供查询、新建、编辑和禁用入口。
1.1.1.1"关键词"输入框，功能是按用户名、姓名或角色筛选用户列表。
1.1.1.2"查询"按钮，功能是按当前筛选条件刷新用户列表。
```

## 三、控件记录字段

复杂控件、权限控件或高风险控件必须记录以下字段：

```text
控件编号：
控件名称：
控件类型：
所在页面或弹框：
可见条件：
启用条件：
点击或输入行为：
成功反馈：
失败反馈：
校验规则：
跳转逻辑：
权限边界：
关联接口或命令：
关联业务场景：
证据来源：
待确认项：
```

## 四、控件类型参考

常见控件类型包括：

1. 输入框。
2. 文本域。
3. 下拉选择。
4. 单选组。
5. 复选框。
6. 开关。
7. 日期或时间选择器。
8. 文件上传。
9. 表格。
10. 表格列。
11. 分页条。
12. 标签页。
13. 弹框。
14. 抽屉。
15. 图标按钮。
16. 文本按钮。
17. 主按钮。
18. 危险操作按钮。
19. 菜单项。
20. 跳转链接。
21. 状态标签。
22. 提示文案。
23. 空状态。
24. 加载状态。
25. 错误提示。

## 五、权限与状态说明规则

涉及权限或状态的控件必须说明：

1. 什么角色或权限可见。
2. 什么角色或权限可点击。
3. 无权限时隐藏、禁用还是展示只读提示。
4. 什么业务状态下可点击。
5. 什么业务状态下禁用。
6. 禁用时显示什么提示。
7. 后端是否仍会独立校验。

禁止只写“按权限控制”而不说明具体表现。

## 六、页面章节索引

按项目实际端和业务域拆分章节。没有对应页面时，不要创建空章节；有真实控件后再追加。

| 编号 | 端或业务域 | 模块 | 页面或弹框 | 关联场景 | 状态 |
|---|---|---|---|---|---|
| 1 | [待确认] | [待确认] | [待确认] | QG-001 | 待核对 |
| 2 | 桌面端 | 截图工具 | 截图工具页顶部操作区 | 录音实时反馈 | 已落地 |
| 3 | 桌面端 | 日程 / 排班 | 排班工具栏 / 排班表格 | 排班 Excel 导入、排班单元格编辑 | 已落地 |
| 4 | 桌面端 | 系统信息 | 实时传感器标签页 | 启动后按需查看硬件传感器 | 已落地 |
| 5 | 桌面端 | Codex 账号切换器 | 本地中转控制区 / 档案列表 | 本地中转接入 Cursor 与 OpenAI-compatible 客户端 | 已落地 |
| 6 | 桌面端 | 系统优化 | 系统维护卡片 / 自动更新行 | 停止或恢复 Windows 自动更新 | 已落地 |
| 7 | 桌面端 | 系统优化 | 自动优化页 / 扫描结果表 | 先扫描再勾选执行自动优化 | 已落地 |
| 8 | 桌面端 | 系统优化 | 垃圾清理页 / 扫描结果表 | 先扫描再确认清理白名单垃圾 | 已落地 |

## 七、控件说明正文

### 1. 待整理界面

1.1待确认模块

模块功能描述：[待确认]

1.1.1待确认页面

页面功能描述：[待确认]

1.1.1.1"待确认控件"控件类型，功能是[待确认]。

控件编号：1.1.1.1
控件名称：待确认控件
控件类型：[待确认]
所在页面或弹框：待确认页面
可见条件：[待确认]
启用条件：[待确认]
点击或输入行为：[待确认]
成功反馈：[待确认]
失败反馈：[待确认]
校验规则：[待确认]
跳转逻辑：[待确认]
权限边界：[待确认]
关联接口或命令：[待确认]
关联业务场景：QG-001
证据来源：[待确认]
待确认项：[待确认]

### 2. 截图工具

2.1截图 / 录像 / 录音模块

模块功能描述：提供全屏截图、区域录像和独立录音入口，并在录音期间显示实时状态。

2.1.1截图工具页顶部操作区

页面功能描述：截图工具页顶部展示模块标题、录音状态条和截图/录像/录音/剪贴板编辑按钮。

2.1.1.1"录音中"状态条，控件类型为状态标签 + 波形指示器，功能是展示独立录音是否正在进行、录音时长和实时音量波形。

控件编号：2.1.1.1
控件名称：录音中状态条
控件类型：状态标签 + 波形指示器
所在页面或弹框：截图工具页顶部操作区
可见条件：`HasAudioRecordingIndicator` 为 true，即独立录音正在进行。
启用条件：只读状态控件，不可点击。
点击或输入行为：无。
成功反馈：显示红色圆点、"录音中"文案、`mm:ss` 或 `hh:mm:ss` 录音时长，以及 28 根随系统声音短时峰值滚动变化的波形柱。
失败反馈：无独立失败反馈；录音启动或停止失败由"开启录音/停止录音"按钮流程弹窗提示。
校验规则：波形仅展示 WASAPI loopback 的实时采样峰值，不写入日志，不展示或保存音频内容。
跳转逻辑：无。
权限边界：本地桌面功能，无角色权限；录音本身依赖 Windows 默认播放设备和可写输出目录。
关联接口或命令：`MainViewModel.AudioRecordingWaveformBars`、`MainViewModel.AudioRecordingDurationText`、`RecordingService.GetActiveAudioLevelSnapshot()`。
关联业务场景：录音实时反馈
证据来源：`src/MyTools/Views/MainModulesView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/RecordingService.cs`
待确认项：无。

### 3. 日程 / 排班

3.1排班模块

模块功能描述：维护月份排班版本、人员、每日总休目标和班次单元格。

3.1.1排班工具栏

页面功能描述：排班页顶部提供新建、保存、人员导入、排班 Excel 导入导出、清空和缩放等高频操作入口。

3.1.1.1"导入排班"控件类型为文本按钮，功能是导入排班 Excel 并替换当前表格内容。

控件编号：3.1.1.1
控件名称：导入排班
控件类型：文本按钮
所在页面或弹框：排班工具栏
可见条件：排班页可见时常驻显示，位置在"复制上月"和"导出"之间。
启用条件：始终可点击；没有当前排班时，导入文件名需包含可识别的 `YYYY-MM` 年月；已有当前排班时可使用当前年月兜底。
点击或输入行为：点击后弹出 `.xlsx` 文件选择框；若当前已有排班，会先提示导入将替换当前表格内容；导入成功后将 Excel 中的年月、每日总休、人员和班次载入表格，并询问是否立即保存为本地排班版本。
成功反馈：状态栏显示导入年月、版本名、人员数量和文件名；导入成功的表格进入可保存、可导出状态；若选择立即保存，保存成功后刷新左侧版本列表并显示保存路径。
失败反馈：文件结构缺失、日期列不连续、年月无法识别、总休非 0.5 步进或班次不在白名单时弹出错误提示，并在状态栏显示失败原因。
校验规则：A1 必须为"姓名"，A2 必须为"日期"，A4 必须为"总休"；B2 起必须是连续日期；班次支持"白 / 卡 / 副 / 感 / 大 / 小 / 休 / 公 / 产 / 午"，旧"夜 / 大夜"归一化为"大"，"产假"归一化为"产"；"午"虽不在弹出菜单中显示，仍允许导入并按 0.5 天休息统计；仅当日期后统计列表头为"上班"时，才按该列把导出时显示为空白的白班还原为"白"。
跳转逻辑：无跨页面跳转；导入成功后停留在当前排班页并重建表格。
权限边界：本地桌面功能，无角色权限；保存动作写入当前 Windows 用户的 `%LOCALAPPDATA%\MyTools\Schedules`。
关联接口或命令：`ScheduleViewModel.ImportExcelCommand`、`ScheduleExcelImporter.ImportAsync()`、`ScheduleService.SaveAsync()`。
关联业务场景：排班 Excel 导入
证据来源：`src/MyTools/Views/SchedulePage.xaml`、`src/MyTools/ViewModels/ScheduleViewModel.cs`、`src/MyTools/Services/ScheduleExcelImporter.cs`
待确认项：无。

3.1.1.2"导出"控件类型为文本按钮，功能是将当前排班表格内容导出为 Excel。

控件编号：3.1.1.2
控件名称：导出
控件类型：文本按钮
所在页面或弹框：排班工具栏
可见条件：排班页可见时常驻显示，位置在"导入排班"右侧。
启用条件：已加载当前排班版本；不要求先执行自动设置休息日，`GeneratedAt` 为空时也可点击。
点击或输入行为：点击后弹出 `.xlsx` 保存对话框，默认文件名按当前排班年月和版本名生成；用户确认保存路径后，按当前界面表格内容写入 Excel。
成功反馈：状态栏显示导出文件路径；弹窗提示导出成功并询问是否打开该文件。
失败反馈：保存路径无效、文件被占用或导出异常时弹出错误提示，并在状态栏显示失败原因。
校验规则：导出内容来自当前排班版本的年月、每日总休、人员和班次单元格；班次显示与界面一致，旧"夜 / 大夜"归一化为"大"，"白"在 Excel 中显示为空白；人员汇总列包含"上班 / 休息 / 连上 / 末休 / 可上"，"末休"统计当前月份内周六和周日休息天数，"可上"统计当前行空白单元格与已设置上班单元格的合计。
跳转逻辑：无跨页面跳转；用户选择打开文件时交由系统默认程序打开 `.xlsx`。
权限边界：本地桌面功能，无角色权限；只写入用户在保存对话框中选择的文件。
关联接口或命令：`ScheduleViewModel.ExportExcelCommand`、`ScheduleExcelExporter.ExportAsync()`。
关联业务场景：排班 Excel 导出
证据来源：`src/MyTools/Views/SchedulePage.xaml`、`src/MyTools/ViewModels/ScheduleViewModel.cs`、`src/MyTools/Services/ScheduleExcelExporter.cs`
待确认项：无。

3.1.2排班表格

页面功能描述：按月份日期展示姓名、星期、日期、实休、总休、人员班次格和行末汇总，支持点击单元格弹出班次菜单。

3.1.2.1"班次单元格"控件类型为表格单元格按钮，功能是展示并编辑某人员某日期的班次。

控件编号：3.1.2.1
控件名称：班次单元格
控件类型：表格单元格按钮
所在页面或弹框：排班表格
可见条件：排班版本已加载，且对应人员行位于当前滚动可视区域或缓冲区域内。
启用条件：排班版本已加载；人员行和日期列存在。
点击或输入行为：单击后当前格进入选区并弹出班次菜单，可选择"白 / 卡 / 副 / 感 / 大夜 / 小 / 休 / 公 / 产假 / 小1 / 清空"；菜单隐藏"午"，已有"午"数据仍可显示和统计；按住鼠标拖拽可选择矩形区域，`Shift` 点击按源格到目标格扩展矩形选区，`Ctrl` 点击追加或取消单格选择；选区存在时按 `Delete` 清空选中班次格。
成功反馈：单元格文字立即刷新；空白格显示"·"，"白"不显示文字，旧"夜"归一化显示为"大"，"产假"写入后显示为"产"；"大 / 小 / 副"使用白字深底，"休 / 卡 / 感 / 公 / 产 / 午"使用各自班次底色，其中"公"和"午"底色一致，"产"使用浅橙色；周末和其它节假日的空白格与"白"格使用蓝色列底色，已有专用班次色的格子不被节假日列底色覆盖；蓝色边框表示选中单元格，绿色边框表示填充源单元格；行末汇总显示"上班 / 休息 / 连上 / 末休 / 可上"，其中"末休"为当前月份周六和周日休息天数合计，"可上"为空白单元格与已设置上班单元格合计。
失败反馈：无独立失败反馈；保存、导出或自动生成失败由对应按钮流程提示。
校验规则：班次代码必须来自 `ShiftCodes` 支持的集合；界面刷新时按 `ShiftCodes.Normalize` 归一化旧"夜"；"产"计入 1 天休息，不计入上班和连续上班。
跳转逻辑：冲突侧栏点击定位时可滚动并高亮对应班次单元格。
权限边界：本地桌面功能，无角色权限；数据保存到当前用户本地排班目录。
关联接口或命令：`ScheduleViewModel.SetCell()`、`ScheduleViewModel.ApplySmallNight1()`、`ScheduleViewModel.FillCellsFromSource()`、`ScheduleViewModel.ClearCells()`、`SchedulePage.ResolveCellBg()`、`SchedulePage.ResolveCellFg()`。
关联业务场景：排班单元格编辑
证据来源：`src/MyTools/Views/SchedulePage.xaml.cs`、`src/MyTools/Services/ScheduleModels.cs`
待确认项：无。

3.1.2.2"填充"控件类型为工具栏按钮，功能是用源单元格班次批量填充当前选区。

控件编号：3.1.2.2
控件名称：填充
控件类型：工具栏按钮
所在页面或弹框：排班工具栏
可见条件：排班页面可见。
启用条件：按钮常驻可点击；无选区时点击会提示先选择单元格。
点击或输入行为：读取绿色边框源单元格的班次代码，将该代码写入当前选区内所有有效班次单元格；源格为空白时，目标选区同步清空。
成功反馈：状态栏显示已填充的单元格数量；目标格文字、底色、手动标记、上班/休息/连上/末休/可上统计和冲突侧栏随局部刷新更新。
失败反馈：未选择单元格时弹出提示"请先在排班表中选择要填充的单元格。"；源坐标无效时弹出提示"请先选择一个源单元格。"。
校验规则：只接受排班表中有效的人员行和日期列；非空代码写入后标记为 `IsManual=true`，空白写入后标记为 `IsManual=false`。
跳转逻辑：无跨页面跳转。
权限边界：本地桌面功能，无角色权限；数据保存到当前用户本地排班目录。
关联接口或命令：`SchedulePage.FillSelectedCells_Click()`、`ScheduleViewModel.FillCellsFromSource()`。
关联业务场景：排班单元格批量编辑
证据来源：`src/MyTools/Views/SchedulePage.xaml`、`src/MyTools/Views/SchedulePage.xaml.cs`、`src/MyTools/ViewModels/ScheduleViewModel.cs`
待确认项：无。

### 4. 系统信息

4.1硬件与实时传感器模块

模块功能描述：展示本机硬件摘要和 LibreHardwareMonitor 实时传感器读数，传感器采集在进入系统信息页后按需异步启动。

4.1.1实时传感器标签页

页面功能描述：系统信息页的"实时传感器"标签展示刷新模式、刷新按钮、停止/启动采集按钮、状态标签和 CPU/显卡/主板/风扇/CPU 占用摘要。

4.1.1.1"实时传感器采集控件组"控件类型为下拉框 + 图标按钮 + 文本按钮 + 状态标签，功能是控制和反馈传感器采集。

控件编号：4.1.1.1
控件名称：实时传感器采集控件组
控件类型：下拉框 + 图标按钮 + 文本按钮 + 状态标签
所在页面或弹框：系统信息页 / 实时传感器标签
可见条件：进入"系统信息"模块后，切换到"实时传感器"标签时可见。
启用条件：刷新模式下拉框始终可选择"手动 / 2 秒 / 5 秒"；"刷新"按钮在 `_sensorService != null` 时可点击；"停止采集"按钮始终绑定 `ToggleHardwareSensorsCommand`，传感器未启动时点击会异步启动采集，已启动时点击会停止并释放采集服务。
点击或输入行为：进入"系统信息"模块时异步加载硬件摘要，并通过 `StartSensorsBackgroundAsync()` 在后台启动传感器；用户点击"刷新"时通过后台线程执行一次 `ReadAll()`；用户选择"手动"时停止自动计时刷新，选择"2 秒"或"5 秒"时按对应间隔刷新；用户点击"停止采集"时停止定时器、解绑 Tick、清空传感器读数并释放服务。
成功反馈：状态标签显示"正在启动传感器..."、"已启用 · N 项 · X刷新"、"已刷新 · N 项 · 手动"或"已刷新 · N 项 · 每 X"；CPU 温度、显卡温度、主板温度、风扇转速和 CPU 占用摘要随读数刷新；首页温度风险摘要同步更新为正常或异常提示。
失败反馈：启动失败时显示"传感器启动失败：..."；非管理员或硬件不支持时显示"当前为非管理员模式..."或"未读取到传感器数据..."；读取异常时状态摘要显示"传感器读取失败：..."。
校验规则：启动和读取均不得在 UI 线程同步探测硬件；重复启动通过 `_isSensorStarting`、`_sensorService` 和 `_isSensorsRunning` 防重；重复刷新通过 `_isSensorRefreshing` 防重；温度不低于 85 或负载不低于 95 时生成异常提示。
跳转逻辑：无跨页面跳转；离开系统信息页时暂停传感器计时刷新，再进入时恢复或重新按需启动。
权限边界：本地桌面功能，无角色权限；温度、风扇、电压等读数依赖 LibreHardwareMonitor 驱动权限，非管理员模式通常只能读取有限数据。
关联接口或命令：`ShowSystemInfoCommand`、`SwitchModule("SystemInfo")`、`StartSensorsBackgroundAsync()`、`ToggleHardwareSensorsCommand`、`RefreshSensorsOnceCommand`、`RefreshSensorsAsync()`。
关联业务场景：启动后按需查看硬件传感器
证据来源：`src/MyTools/Views/SystemInfoView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`docs/规划/启动基线-启动后菜单响应-2026-06-24.md`
待确认项：未覆盖安装当前运行版做人工菜单点击实测；按用户要求保留为后续允许替换安装版后补测。

### 5. Codex 账号切换器

5.1本地中转模块

模块功能描述：管理 Codex 账号档案、本地 relay 启停、通过本地中转切换上游，以及为 Cursor / OpenAI-compatible 客户端提供模型列表和 Chat Completions 兼容兜底。

5.1.1本地中转控制区

页面功能描述：Codex 账号切换器顶部展示本地中转状态圆点、状态文本、启动本地中转、停止使用本地中转和重启 Codex 使用中转按钮；档案列表中的切换按钮在本地中转模式下显示为"本地切换"，每条档案提供"测试"按钮单独检测该档案中转是否可达。

5.1.1.1"本地中转控制区"控件类型为状态标签 + 文本按钮组，功能是启停本地中转并切换 Codex 档案到本地中转上游。

控件编号：5.1.1.1
控件名称：本地中转控制区
控件类型：状态标签 + 文本按钮组
所在页面或弹框：Codex 账号切换器顶部控制区
可见条件：进入"Codex 账号切换器"模块后常驻可见。
启用条件："启动本地中转"始终可点击；"停止使用本地中转"仅 `CanDisableCodexLocalRelay` 为 true 时可点击；"重启 Codex 使用中转"仅本地中转状态为绿色且不在检测中时可点击；档案行按钮在本地中转已启用时显示"本地切换"；档案行"测试"按钮在未进行批量中转测试、且该档案未处于中转测试或切换执行中时可点击。
点击或输入行为：点击"启动本地中转"会从当前 Codex 配置生成本地 relay 设置，启动 relay 并通过 relay 测试当前 NewAPI；点击"停止使用本地中转"会停用 MyTools 本地 relay 设置并把档案按钮恢复为普通"切换"；点击"重启 Codex 使用中转"会把 Codex 配置固定到本地 relay 后重启 Codex App；点击档案行"测试"只测试当前档案中转，更新该档案中转灯号、测试时间和摘要，不切换、不备份、不覆盖当前 `~/.codex`；点击档案行"本地切换"会先测试目标 NewAPI，测试通过后只切换本地中转上游，不覆盖当前 `~/.codex`。
成功反馈：状态圆点按未知/测试/成功/失败显示不同颜色；状态文本显示"本地中转：..."和检查时间；档案行"测试"通过后该档案中转灯号变绿并在 `CodexProfilesStatusMessage` 显示通过摘要；启动成功后状态消息提示当前本地固定连接与测试结果；本地切换成功后弹窗提示 Codex 固定连接、当前上游和是否需要重启 Codex。
失败反馈：启动、停用、配置固定或目标上游测试失败时，状态圆点转失败色，状态文本和 `CodexProfilesStatusMessage` 显示失败原因，并弹出对应错误或警告提示；档案行"测试"失败时只把该档案中转灯号转红并显示失败摘要。
校验规则：本地中转请求需要本地 token 授权；日志只记录主机、路径、状态码和错误类型，不记录上游 key、本地 relay token、请求体或响应正文；OpenAI-compatible 客户端访问 `/v1/models` 时可返回本地模型列表，访问 `/v1/chat/completions` 且上游拒绝或返回非 OpenAI 内容类型时可转换为 `/v1/responses` 并把 JSON 或 SSE 结果转回 Chat Completions 格式。
跳转逻辑：无跨页面跳转；"重启 Codex 使用中转"会关闭并重新打开 Codex App；本地切换成功后当前 Codex 后续请求使用新的上游。
权限边界：本地桌面功能，无角色权限；会读取和写入当前用户 `~/.codex` 配置及 MyTools 本地 relay 设置，敏感配置内容按既有 DPAPI 保护规则保存。
关联接口或命令：`EnableCodexLocalRelayCommand`、`DisableCodexLocalRelayCommand`、`RestartCodexWithLocalRelayCommand`、`SwitchCodexProfileCommand`、`TestCodexProfileRelayCommand`、`CodexLocalRelayService`、`CodexLocalRelayCompatibility`。
关联业务场景：本地中转接入 Cursor 与 OpenAI-compatible 客户端
证据来源：`src/MyTools/Views/MainModulesView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/CodexLocalRelayService.cs`、`src/MyTools/Services/CodexLocalRelayCompatibility.cs`
待确认项：按用户要求未停止或替换当前正在使用的安装版 local relay 做真实 Cursor 端到端接管实测；当前验证为 Release build 与 `scripts/codex-eval.ps1` 程序集级兼容回归。

### 6. 系统优化

6.1系统维护模块

模块功能描述：在系统优化页的“系统维护”标签中集中展示系统锁定、安全防护和自动更新状态，并提供需要 UAC 授权的系统级操作。

6.1.1自动更新行

页面功能描述：自动更新行展示 Windows Update 状态徽章、“停止更新 / 恢复更新”按钮和“立即更新”按钮。

6.1.1.1"停止更新 / 恢复更新"控件类型为文本按钮，功能是停止或恢复 Windows 自动更新。

控件编号：6.1.1.1
控件名称：停止更新 / 恢复更新
控件类型：文本按钮
所在页面或弹框：系统优化页 / 系统维护卡片 / 自动更新行
可见条件：进入“系统优化”模块并停留在“系统维护”标签时可见。
启用条件：按钮始终绑定 `ToggleAutoUpdateCommand`；具体是否能修改系统取决于用户是否同意 UAC 授权和当前 Windows 策略权限。
点击或输入行为：状态为自动更新中时，按钮显示“停止更新”，点击后请求 UAC 授权，写入停止更新策略，禁用常见 Windows Update 计划任务，并停止、禁用 `wuauserv`、`UsoSvc`、`BITS`、`DoSvc`、`WaaSMedicSvc` 等更新相关服务；状态为已停止时，按钮显示“恢复更新”，点击后移除停止策略，把服务启动类型和计划任务恢复为 MyTools 默认更新配置并启动必要服务。
成功反馈：停止成功时徽章显示“已停止”，按钮变为“恢复更新”，`SystemStatusMessage` 显示“自动更新已停止，Windows Update 相关服务和计划任务已禁用。”；恢复成功时徽章显示“自动更新中”，按钮变为“停止更新”，`SystemStatusMessage` 显示“自动更新已恢复为 MyTools 默认更新服务配置。”。
失败反馈：用户取消 UAC 时显示“操作已取消（UAC 未授权）。”；命令异常时弹出“操作失败”；停止后未读回策略和核心服务禁用状态时显示“已提交停止命令，但未读回完整停止状态”；恢复后仍读回停止策略或禁用服务时显示“已提交恢复命令，但自动更新策略或服务仍处于停止状态”。
校验规则：状态刷新读取 `NoAutoUpdate` 策略、核心更新服务启动类型和声明处理的 Windows Update 计划任务状态；停止成功提示必须满足停止策略存在、核心服务启动类型为 Disabled 且计划任务读回为 Disabled；恢复成功提示必须满足停止策略已移除、核心更新服务未保持 Disabled 且计划任务读回为 Enabled。
跳转逻辑：无跨页面跳转。
权限边界：本地桌面功能，无角色权限；修改 HKLM 策略、Windows 服务和计划任务需要管理员 UAC 授权。
关联接口或命令：`ToggleAutoUpdateCommand`、`WindowsSecurityService.SetAutoUpdateAsync()`、`WindowsSecurityService.IsAutoUpdateStopEnforced()`、`WindowsSecurityService.IsAutoUpdateRestoreReady()`。
关联业务场景：停止或恢复 Windows 自动更新
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/WindowsSecurityService.cs`
待确认项：未在当前机器执行真实 UAC 停止/恢复 Windows Update，以避免改变用户系统更新状态；当前验证为 Release build 和脚本级快速检查。

6.2自动优化模块

模块功能描述：在系统优化页的“自动优化”标签中先生成可审计扫描计划，再由用户勾选要执行的白名单优化项。

6.2.1扫描按钮

页面功能描述：点击“扫描”后只读取系统状态，生成包含执行勾选、风险、边界和证据的优化计划。

6.2.1.1"扫描"控件类型为文本按钮，功能是生成自动优化计划。

控件编号：6.2.1.1
控件名称：扫描
控件类型：文本按钮
所在页面或弹框：系统优化页 / 自动优化标签 / 自动优化与报告卡片
可见条件：进入“系统优化”模块并停留在“自动优化”标签时可见。
启用条件：`IsAutoOptimizeBusy=false` 时可点击。
点击或输入行为：调用 `ScanAutoOptimizeCommand`，后台执行 `SystemOptimizationService.ScanAsync`；扫描阶段不删除文件、不清空回收站、不重启 Explorer、不启停服务、不执行 UAC 提权脚本、不写入系统设置。
成功反馈：`AutoOptimizeStatusMessage` 显示可优化数量、默认勾选数量、需手动确认数量和预计释放空间；“本次扫描结果”表格刷新为 14 步计划。
失败反馈：扫描异常时弹出“自动优化扫描失败”，状态文本显示失败原因，扫描结果清空。
校验规则：扫描计划只允许白名单步骤；递归扫描跳过 junction/symlink，并确认枚举路径始终在批准根目录内。
跳转逻辑：无跨页面跳转。
权限边界：扫描不请求 UAC，不修改系统。
关联接口或命令：`ScanAutoOptimizeCommand`、`SystemOptimizationService.ScanAsync()`。
关联业务场景：先扫描再勾选执行自动优化
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/SystemOptimizationService.cs`
待确认项：未在当前机器执行真实系统清理，仅验证 Release build 和脚本级快速检查。

6.2.2本次扫描结果表

页面功能描述：表格展示每个优化项的执行勾选、状态、项目、风险、类型、预计释放空间、安全边界、扫描证据和说明。

6.2.2.1"本次扫描结果"控件类型为数据表格，功能是让用户选择本次实际执行的优化项。

控件编号：6.2.2.1
控件名称：本次扫描结果
控件类型：数据表格
所在页面或弹框：系统优化页 / 自动优化标签 / 自动优化与报告卡片
可见条件：进入“系统优化”模块并停留在“自动优化”标签时可见；无扫描结果时显示空表头。
启用条件：每行“执行”复选框仅在 `CanOptimize=true` 时可勾选；不可优化项禁用勾选。
点击或输入行为：用户勾选或取消勾选“执行”列后，`HasRunnableAutoOptimizePlan` 立即重算并刷新“优化”按钮启用状态。
成功反馈：已勾选行状态显示“已选择”；未勾选可执行行状态显示“可选择”；不可执行行状态显示“无需处理”。
失败反馈：无独立失败弹框；执行前未勾选任何可优化项时，状态文本提示先扫描并确认存在可优化项目。
校验规则：低风险且收益明确的项目可默认勾选；中高风险、会重启 Explorer、清理诊断证据、清空回收站、优化磁盘或执行 DISM 的项目默认不勾选。
跳转逻辑：无跨页面跳转。
权限边界：勾选本身不请求 UAC；实际执行时由对应步骤决定是否需要 UAC。
关联接口或命令：`AutoOptimizePlanItems`、`OptimizationPlanItem.IsSelected`、`StartAutoOptimizeCommand`。
关联业务场景：先扫描再勾选执行自动优化
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/SystemOptimizationService.cs`
待确认项：无。

6.2.3优化按钮与确认弹窗

页面功能描述：点击“优化”后仅执行已勾选且可优化的白名单步骤，并在执行前展示风险摘要。

6.2.3.1"优化"控件类型为文本按钮，功能是执行本次已勾选的自动优化步骤。

控件编号：6.2.3.1
控件名称：优化
控件类型：文本按钮 + 确认弹窗
所在页面或弹框：系统优化页 / 自动优化标签 / 自动优化与报告卡片
可见条件：进入“系统优化”模块并停留在“自动优化”标签时可见。
启用条件：`IsAutoOptimizeBusy=false` 且扫描结果中至少 1 行满足 `CanOptimize=true && IsSelected=true`。
点击或输入行为：弹出确认框，汇总执行数量、预计释放空间、需 UAC 数量、中高风险数量；存在高风险项时列出具体项目名称。用户确认后调用 `SystemOptimizationService.RunAsync(..., enabledStepKeys)`，仅传入已勾选步骤键。若包含缩略图缓存项，会再询问是否允许临时重启 Explorer；选择“否”时只跳过该步骤。
成功反馈：状态文本显示自动优化完成摘要和释放空间；历史报告列表新增本次 `OptimizationReportItem`；本次扫描结果清空。
失败反馈：执行异常时弹出“自动优化失败”并保留错误状态文本；用户取消确认时显示“已取消自动优化。”；用户取消 UAC 时对应步骤在报告中标记 Failed。
校验规则：提权 PowerShell 脚本必须在超时内退出且退出码为 0 才视为成功；脚本超时、退出码非 0 或 UAC 取消均不能记为成功；提权清理脚本也必须跳过 junction/symlink，并在删除前校验目标仍位于批准根目录内。
跳转逻辑：无跨页面跳转。
权限边界：不同步骤可能请求管理员 UAC；高风险项目必须由用户在表格中手动勾选，并在确认弹窗中再次看到项目名称。
关联接口或命令：`StartAutoOptimizeCommand`、`SystemOptimizationService.RunAsync()`、`ElevatedScriptRunner.RunElevatedScriptAsync()`、`OptimizationReportService.SaveAsync()`。
关联业务场景：先扫描再勾选执行自动优化
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/SystemOptimizationService.cs`
待确认项：未在当前机器执行真实清理、UAC 授权或 Explorer 重启，以避免改变用户系统状态；当前验证为 Release build 和脚本级快速检查。

6.3垃圾清理模块

模块功能描述：在系统优化页的“垃圾清理”标签中先扫描白名单垃圾候选，再由用户勾选并确认清理。

6.3.1开始扫描按钮

页面功能描述：点击“开始扫描”后只读取白名单范围，生成带风险、边界和证据的垃圾清理候选列表。

6.3.1.1"开始扫描"控件类型为文本按钮，功能是生成垃圾清理候选。

控件编号：6.3.1.1
控件名称：开始扫描
控件类型：文本按钮
所在页面或弹框：系统优化页 / 垃圾清理标签 / 垃圾清理卡片
可见条件：进入“系统优化”模块并停留在“垃圾清理”标签时可见。
启用条件：`IsJunkBusy=false` 时可点击。
点击或输入行为：调用 `StartJunkScanCommand`，后台执行 `JunkCleanupService.ScanAsync`；扫描阶段不删除文件、不清空回收站、不启停服务、不执行 UAC 提权脚本。
成功反馈：`JunkStatusMessage` 显示候选总数、默认勾选数量、需手动确认数量和预计释放空间；扫描结果表刷新。
失败反馈：扫描异常时弹出“垃圾扫描失败”，状态文本显示失败原因。
校验规则：扫描范围只允许白名单目录；递归枚举跳过 junction/symlink，并确认文件仍在批准根目录内。
跳转逻辑：无跨页面跳转。
权限边界：扫描不请求 UAC，不修改系统。
关联接口或命令：`StartJunkScanCommand`、`JunkCleanupService.ScanAsync()`。
关联业务场景：先扫描再确认清理白名单垃圾
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/JunkCleanupService.cs`
待确认项：未在当前机器执行真实清理。

6.3.2垃圾扫描结果表

页面功能描述：表格展示每个垃圾候选项的勾选状态、类别、大小、风险、建议、安全边界、扫描证据、路径和说明。

6.3.2.1"垃圾扫描结果"控件类型为数据表格，功能是让用户选择本次要清理的白名单项目。

控件编号：6.3.2.1
控件名称：垃圾扫描结果
控件类型：数据表格
所在页面或弹框：系统优化页 / 垃圾清理标签 / 垃圾清理卡片
可见条件：进入“系统优化”模块并停留在“垃圾清理”标签时可见；无扫描结果时显示空表头。
启用条件：每行“选择”复选框可由用户勾选或取消；诊断证据类和浏览器/回收站项默认不勾选。
点击或输入行为：用户勾选或取消勾选后，`RunJunkCleanupCommand` 的可执行状态立即重算。
成功反馈：勾选状态在表格中即时变化。
失败反馈：无独立失败弹框；清理前未勾选任何项目时，“立即清理”不可用。
校验规则：表格必须展示“边界”和“证据”列；不再展示“导出报告”按钮。
跳转逻辑：无跨页面跳转。
权限边界：勾选本身不请求 UAC；清理系统缓存时由清理流程请求管理员权限。
关联接口或命令：`JunkCandidates`、`JunkCandidate.SafetyBoundary`、`JunkCandidate.Evidence`、`RunJunkCleanupCommand`。
关联业务场景：先扫描再确认清理白名单垃圾
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/JunkCleanupService.cs`
待确认项：无。

6.3.3立即清理按钮

页面功能描述：点击“立即清理”后只处理已勾选候选，并在执行前弹窗确认。

6.3.3.1"立即清理"控件类型为文本按钮，功能是执行本次已勾选垃圾候选清理。

控件编号：6.3.3.1
控件名称：立即清理
控件类型：文本按钮 + 确认弹窗
所在页面或弹框：系统优化页 / 垃圾清理标签 / 垃圾清理卡片
可见条件：进入“系统优化”模块并停留在“垃圾清理”标签时可见。
启用条件：`IsJunkBusy=false` 且至少 1 条 `JunkCandidate.IsSelected=true`。
点击或输入行为：弹出确认框，显示将清理数量和预计释放空间；用户确认后调用 `JunkCleanupService.CleanupAsync()`。
成功反馈：状态文本和完成弹窗显示已清理数量与释放空间；历史报告列表新增 `JunkCleanup` 报告。
失败反馈：执行异常时弹出“垃圾清理失败”并保留错误状态文本；用户取消确认时不执行清理。
校验规则：普通文件发送到回收站；系统缓存提权脚本必须包含根内路径校验和 reparse point 跳过。
跳转逻辑：无跨页面跳转。
权限边界：系统临时目录、Windows Update 缓存和 Delivery Optimization 缓存会请求管理员 UAC；用户取消 UAC 时对应步骤标记失败。
关联接口或命令：`RunJunkCleanupCommand`、`JunkCleanupService.CleanupAsync()`、`ElevatedScriptRunner.RunElevatedScriptAsync()`、`OptimizationReportService.SaveAsync()`。
关联业务场景：先扫描再确认清理白名单垃圾
证据来源：`src/MyTools/Views/SystemOptimizationView.xaml`、`src/MyTools/ViewModels/MainViewModel.cs`、`src/MyTools/Services/JunkCleanupService.cs`
待确认项：未在当前机器执行真实清理或 UAC 授权。

## 八、与其它活文档的同步

1. 本文新增或调整控件后，必须确认对应业务流程清单是否需要同步入口、步骤或异常路径。
2. 本文新增或调整控件后，必须确认实现核查台账是否需要同步证据、状态、缺口或验证结果。
3. 实现核查发现页面真实行为与本文不一致时，必须更新本文或记录代码缺口，不能让控件说明停留在旧状态。

## 九、自检清单

每次修改本文后，自检以下事项：

- [ ] 新增控件有清晰编号。
- [ ] 控件名称和界面展示一致。
- [ ] 控件类型准确。
- [ ] 可见条件和启用条件明确。
- [ ] 成功反馈和失败反馈明确。
- [ ] 校验规则和后端校验边界明确。
- [ ] 跳转逻辑明确。
- [ ] 权限展示和后端授权边界明确。
- [ ] 关联业务场景已写入。
- [ ] 没有把计划中但未落地的控件写成事实。

## 十、变更日志

- 2026-06-26 · v13 · 补充系统优化垃圾清理扫描结果表、边界证据列、取消导出报告按钮和提权脚本安全边界。
- 2026-06-26 · v12 · 补充系统优化自动优化扫描结果表、执行勾选、风险证据列、优化确认弹窗和提权脚本成功判定。
- 2026-06-26 · v11 · 补充系统优化自动更新停止/恢复按钮的读回校验、成功失败反馈和 UAC 权限边界。
- 2026-06-24 · v10 · 补充系统信息实时传感器按需异步采集和 Codex 本地中转 OpenAI-compatible 客户端兼容行为说明。
- 2026-06-22 · v9 · 补充排班"可上"汇总列说明，明确界面和 Excel 导出均按空白单元格 + 已设置上班单元格统计最多可上班天数。
- 2026-06-22 · v8 · 补充排班"末休"汇总列说明，明确界面和 Excel 导出均统计当前月份周六、周日休息天数。
- 2026-06-21 · v7 · 补充排班工具栏"导出"按钮说明，明确当前排班存在时可随时导出，不依赖 `GeneratedAt`。
- 2026-06-21 · v6 · 补充排班班次单元格多选、Delete 清空选区和"填充"按钮控件说明。
- 2026-06-21 · v5 · 更新排班班次菜单说明，记录"产假"菜单项、"产"显示、浅橙色底色和按 1 天休息统计。
- 2026-06-20 · v4 · 补充排班工具栏"导入排班"按钮说明，记录排班 Excel 导入成功、失败和保存提示行为。
- 2026-06-20 · v3 · 更新排班班次菜单说明，记录"午"选项隐藏且"公/午"底色一致。
- 2026-06-20 · v2 · 补充排班表格班次单元格说明，明确专用班次底色优先于节假日列底色。
- 2026-06-08 · v1 · 按通用场景驱动开发范式重建为可复用控件交互逻辑说明模板，后续由 `开qg` 场景执行实时维护。
